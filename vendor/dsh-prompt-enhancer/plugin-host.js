// GENERATED by scripts/build-host.mjs — do not edit by hand.
// Source: src/host/app.js + src/host/{pure,diagnostics,models,plugins,update,enhance-handlers}.js
// ============================================================================
// DSH「提示词优化」插件 · Host 半部（v2.5.0：一键更新并重启 + 环境检测）
// 2026-08-18（模板体系修订·未发布迭代）：每模式 2 个内置模板（default/increment）+ 多自定义模板——
// system 组装走 PURE resolveTemplateSystem（pick/custom 解析 + 旧配置兼容迁移）；
// template/default 新增 catalog（每模式 [T1,T2]，T2=增量模板·保守补充未说的大逻辑/信息）；
// 新增 5 增量模板事实源（见生成区）；原 2026-08-17 的 supplement/dev 共享模板已收敛。
// v2.6.1（记忆链·未发布迭代）：发送前多轮迭代记忆升级——client 记忆由单轮对升级为
// rounds 链（≤4 轮）；host 经 buildChatMessages 以真多轮 user/assistant 消息注入，
// computeEditDelta/buildMemoryDeltaHint 感知本轮相对上一轮输出的修改方向（+新增/-删除）；
// 预算规则：链 ≤2400 字符按轮等分（输入 1/3、输出 2/3），摘要 ≤300；shouldInjectMemory
// 语义不变（hasMemory = rounds 非空）；记忆注入同样触发 CONTEXT_GUARD。
// v2.5.0（方案「一键更新并重启方案.md」）：
// ① 新 RPC update/apply：官方路径安装（dsh plugin add github:...#<tag>，120s 超时，
//    失败绝不重启）→ 成功后才 spawn 分离重启链（net stop <svc> & timeout 2 & net start <svc>，
//    execDetached 脱离进程树，host 被终止是预期）；仅 bundle 形态可用（harness.execCommand
//    判空 → UNSUPPORTED）；防重入 APPLY_BUSY。
// ② 新 RPC update/envcheck：只读探测 7 项（net 连通性 curl 实测 / service 存在 / account
//    LocalSystem / restart KillProcessTree / port 占用者 / mode 形态 / pnpmInfo 注入机制），
//    探测执行在 lib/index.cjs（probeEnv），本侧合并 ENV_PROBE_KEYS 等级元数据（block/warn/info）。
// ③ PURE 新增：ENV_PROBE_KEYS / buildInstallArgs / buildRestartChain / mergeEnvPath
//    （lib/index.cjs 切片 PURE 区段复用 mergeEnvPath 做用户 PATH 注入）。
// ④ 单测 U42-U45（命令构造/重启链/PATH 合并/探测计划）。
// v2.4.8（发布断链修复）：v2.4.5 曾无记录把 sidebar.footer.action 占位 id 从
// cordis-panel-enh 回退为 cordis-panel（与基座同槽位同 id 冲突，update/重挂时
// single-occupant duplicate / "Failed to load plugins"）——本版在 client 半部恢复
// cordis-panel-enh（见 plugin-client.js 注册处注释），并重建 lib/client.cjs。
// v2.4.7（用户需求：自定义提示词给默认内容、每模式独立对应）：
// ① validateConfig 新增 template.texts（4 模式键白名单，各 ≤4000）——每模式独立
//    自定义模板；无 texts 时旧全局 templateText 迁移到全部 4 模式（保持"全局一份"
//    语义不丢内容）；非法键/超长忽略；缺省全空。
// ② enhance system 组装：custom 且当前模式 texts 非空 → 用该模式文本；当前模式
//    空串 → 回退内置 SYSTEM_PROMPT（不空白、不报错）。
// ③ 新增 RPC template/default：返回 4 模式默认提示词（当前同值 = SYSTEM_PROMPT，
//    取自生成区）——client 首次切换自定义且无内容时预填（client 侧无内置文本）。
// ④ 单测 U41：texts 解析/非法键/超长/旧值迁移/新结构优先断言（43/43 通过）。
// v2.4.6（提示词外置，用户需求）：
// ① SYSTEM_PROMPT / TASK_ANALYSIS_PROMPT / CONTEXT_GUARD 三个静态提示词外置为
//    prompts/*.md（system.md / task-analysis.md / context-guard.md）——事实源；
//    plugin-host.js 中 ==PROMPTS-BEGIN== / ==PROMPTS-END== 标记区由
//    scripts/sync-prompts.mjs 生成内联（node scripts/sync-prompts.mjs；--check 校验漂移）。
//    为什么构建时内联而非运行时读文件：host 半部经 lib/index.cjs 以
//    new Function('harness', BODY) 执行（动态安装同样无 require/fs 作用域），
//    运行时读外部文件在两种安装形态下都不可靠；内联保持 plugin-host.js 单文件
//    自包含（动态/静态安装均不受影响）。lite 规则强化与 V2 上下文块为运行时
//    动态拼接，属代码逻辑，不入 prompts。
// ② 单测：U40 prompts 外置一致性断言（生成区 = md 逐行求值，防双向漂移）；
//    U39 契约断言继续对生成区生效（42/42 通过）。
// v2.4.5（语义保真，用户反馈"优化结果对提示词理解不够、语义理解错误"）：
// ① SYSTEM_PROMPT 重写：新增【理解原文（第一优先）】阶段——先逐条列出已明确信息
//    （动作对象/动作/约束/范围/术语/数字/语气），区分「原文明确需求」与「推测」；
//    明确化原则仅允许"模糊但可推断"的表述具体化，无法推出不得添加；"怎么做"细节
//    完整保留；删除旧版"只写做什么不解释怎么做"（与示例 2 自相矛盾，诱导删细节）；
//    删除"补充缺失的必要上下文"（诱导臆造）；长度改为"服从语义保真"（简单 ≤800，
//    复杂可超出但禁冗余，与 outputLimit=8000 一致）；语言改为"主体语言"（混合输入
//    保留术语，避免中英混杂误判）；示例扩至 4 条（新增语义保真/模糊明确化示范）；
// ② analyzeInputRules（lite 规则）保守化：suggestions 措辞全部改为"仅当可合理推断
//    且不偏离原意时才明确化，否则保持原文"——修复旧文案（"请在优化结果中补充合理
//    约束（如长度上限…）"）诱导模型添加原文未提及的新要求、造成语义漂移的问题；
//    lite 强化段拼接措辞同步改为「优化时请遵循以下原则」。
// ③ PLUGIN_VERSION bump 2.4.3 → 2.4.5（2.4.4 开发库提交时漏 bump；本次一并纠正，
//    发布仓库 v2.4.4 tag 缺 lite 规则代码，本版为完整语义保真版）。
// ④ 单测：U38 断言不变（missing/suggestions 结构契约未动，仅文案保守化）；新增
//    U39 SYSTEM_PROMPT 语义保真关键约束存在性断言（理解原文/语义等价/禁臆造）。
// v2.4.1（方案 §9-T5/T6 实测回填）：host 不再出网/会话策略写入
// v2.4.1（方案 §9-T5/T6 实测回填）：
// ① 架构修订：本部署 ctx.web.fetch 无可用 provider（实机抛错）——检测/下载改由 client 浏览器
//    直连 GitHub API（CORS 实测 200），host 经 update/check（tagsPayload/releasePayload 载荷）
//    与 update/pull（files 清单载荷）只做解析/校验/比较/写入；
// ② 写入/默认目录基于**会话策略**（resolve({ session })）——无会话时 workspaceRoot 回退 DSH
//    安装目录且写工作区 FS_SANDBOX_DENIED；带会话后 root=会话工作区（实机验证写 ok）。
// v2.4.0（方案「插件版本检测与一键更新方案.md」）：
// ① 新增 PLUGIN_VERSION（本地版本单一事实源，发布时 bump）/ UPDATE_MANIFEST（拉取文件清单）常量，
//    与版本比较纯函数族（parseVersion/compareVersions/versionStatus/normalizeRepo/pickMaxTag/
//    rawFileUrl/defaultDirFor/isValidTag，入 PURE 区段供单测切片）；
// ② 新增 update/check RPC——检测指定 GitHub 公开仓库版本（tags 主路径取最大，releases 仅作同名展示
//    元数据；300s TTL 缓存；返回 remote/remoteTag/defaultDir/status/ahead）；
// ③ 新增 update/pull RPC——校验客户端拉取的 6 个发布文件清单（validateManifestFiles），
//    按会话策略零写入落盘到目标目录（默认 <workspaceRoot>/dsh-prompt-enhancer-<tag>/，
//    root 取自会话策略；in-flight 锁防重入 PULL_BUSY；tag 白名单校验防注入）。
// v2.3（方案「提示词优化方案.md」§7）：① STAGE_* 常量 + pending 记录 stage 标记
// （prepare→history→analyze→files→events→context→llm），buildV2ContextBlock 注入 onStage 回调；
// ② 新增 enhance/progress 轮询 RPC（client 500ms 轮询展示步骤进度，纯展示、失败静默降级）；
// ③ 记忆状态改由 client 输入区记忆开关自身表达（关=变暗置灰 / 开=高饱和橙），host 无行为变化。
// v2.2（方案「提示词优化方案.md」§0.2/§2/§6）：
// ① 模式体系收敛 4 模式（base/lite/standard/smart），MODE_TABLE 表驱动（阶段 A/B/C 分发）；
// ② 记忆功能改为所有模式可开/关的独立开关（config.memory，缺省 false）——记忆块作为
//    叠加模块注入（shouldInjectMemory），记忆优先占用预算（≤1200），模式块用剩余预算；
// ③ 配置迁移：mode='memory' → mode='lite' + memory=true；autoMemory 并入 memory；
// ④ 日志 mode=base|lite|standard|smart（seed 场景标注 (seed)），ctx 日志含 memory chars=。
// v2.1：记忆模式（5 模式）——v2.2 已删除，历史值迁移见 ②。
// v14：新增日志环形缓冲 + logs/last 诊断 RPC（供客户端诊断日志查看器与故障排查）
// v17：① models/resolve：逐模型解析 reasoning 元数据（efforts/defaultEffort，懒加载）
//      ② models/test：连通性测试（resolveCallConfig 预校验失败不阻断 + 探测流计时，15s 超时）
//      ③ enhance 支持 reasoningEffort（主模型思考等级透传 llm.stream；cfg 日志加 effort=）
// v17.3：探测请求带 system + 明确指令 + maxTokens=16（真实请求形态）
// v18：① validateConfig v2：main/fallback/customModels/order/params/template（兼容 v1 平铺）
//      ② models/current：agentDefaultModel.currentSelection()（fresh install 兜底链继承）
//      ③ enhance 尝试链 = main + fallback 按序（每条独立 reasoningEffort）；cfg 日志加 chain=
// v19：新增 resolveAdaptiveChain（60s TTL 缓存）与 models/autochain RPC
// v20：内置兜底链硬编码指向 DeepSeek 官方模型（deepseek-official/deepseek-v4-flash、deepseek-v4-pro），
//      不再扫描任意 provider——主模型优先 currentSelection，兜底补足固定 DeepSeek 官方。
// v21：P1-4 模型能力解析缓存（resolveModelInfoCached，TTL 5min，按 provider:model 键，200 条上限清理）
// v23：模型链整合——enhance 不再区分 main/fallback，直接按链顺序逐一尝试（buildTryChain）；
//      老 v2 main 字段保留解析但尝试逻辑忽略（client 侧已迁移为链首条）。
// v2.0.0：引擎共存——engine=v1（默认，行为零变化）/ engine=v2（上下文感知：阶段 A 任务进度
//      smart|basic → 阶段 B 工作区文件/会话事件相关性检索 → 阶段 C 预算组装注入）；
//      各阶段独立降级；阶段 A/B 在优化超时计时器前执行（独立超时）；防上下文回显约束；
//      敏感文件硬过滤（shouldIgnoreFile）。
// ============================================================================

// —— v14 诊断日志：环形缓冲（最近 300 行），供 logs/last RPC 读取 ——
const LOG_RING = [];
const LOG_RING_MAX = 300;
// 批次D（P2-5·optimization-plan-20260824 §D.1）：每行统一 ISO 时间戳前缀（[YYYY-MM-DDTHH:mm:ss.sssZ]）——在行组装处一次性前置，bundle 内全部调用点零编辑覆盖；调用点字面量前缀（如 [enhance]）原样保留在时间戳之后（grep 兼容：关键 token 不动）。格式先例借自 logger.js（ISO ts）；createLogger 结构化接线维持不采纳（M5 大改非最小解）。
function hlog() {
  const line = '[' + new Date().toISOString() + '] ' + Array.prototype.map.call(arguments, (a) => {
    if (typeof a === 'string') return a;
    try { return JSON.stringify(a); } catch (e) { return String(a); }
  }).join(' ');
  LOG_RING.push(line);
  if (LOG_RING.length > LOG_RING_MAX) LOG_RING.shift();
  console.log(line);
}
function herr() {
  const line = '[' + new Date().toISOString() + '] ' + Array.prototype.map.call(arguments, (a) => {
    if (typeof a === 'string') return a;
    try { return JSON.stringify(a); } catch (e) { return String(a); }
  }).join(' ');
  LOG_RING.push(line);
  if (LOG_RING.length > LOG_RING_MAX) LOG_RING.shift();
  console.error(line);
}

// v20：内置兜底链硬编码指向 DeepSeek 官方模型（provider=deepseek-official）。
// 主模型优先取 agentDefaultModel.currentSelection()（当前使用模型），
// 兜底补足固定为 DeepSeek 官方模型链，不再扫描任意 provider（保证确定性）。
const DEEPSEEK_OFFICIAL_CHAIN = [
  { provider: 'deepseek-official', model: 'deepseek-v4-flash' },
  { provider: 'deepseek-official', model: 'deepseek-v4-pro' },
];
const DEFAULT_TIMEOUT_MS = 30000;
const DEFAULT_MAX_TOKENS = 2000;
const DEFAULT_OUTPUT_LIMIT = 8000;
// 兜底链 TTL 缓存（避免每次 enhance 都重新解析）
const adaptiveChainCache = { value: null, at: 0 };
const ADAPTIVE_CHAIN_TTL_MS = 60000;
// v21（P1-4）：模型能力解析缓存（resolveModelInfo 结果，TTL 5 分钟，按 provider:model 键）
// 避免 models/resolve 每次调用都重复走适配器能力查询（可能含网络发现，毫秒~秒级开销）
const modelInfoCache = new Map();
const MODEL_INFO_TTL_MS = 300000;

async function resolveModelInfoCached(llmService, provider, model, noCache) {
  const key = provider + '/' + model;
  const hit = modelInfoCache.get(key);
  const now = Date.now();
  if (!noCache && hit && (now - hit.at) < MODEL_INFO_TTL_MS) return hit.value;
  const info = await llmService.resolveModelInfo(provider, model);
  modelInfoCache.set(key, { value: info, at: now });
  // 防无限增长：超过 200 条时清理过期项
  if (modelInfoCache.size > 200) {
    for (const [k, v] of modelInfoCache) {
      if ((now - v.at) >= MODEL_INFO_TTL_MS) modelInfoCache.delete(k);
    }
  }
  return info;
}

async function resolveAdaptiveChain(llmSvc, adm, noCache) {
  const now = Date.now();
  if (!noCache && adaptiveChainCache.value && (now - adaptiveChainCache.at) < ADAPTIVE_CHAIN_TTL_MS) {
    return adaptiveChainCache.value;
  }
  const chain = [];
  try {
    if (adm && typeof adm.currentSelection === 'function') {
      const sel = adm.currentSelection();
      if (sel && typeof sel.provider === 'string' && sel.provider &&
          typeof sel.model === 'string' && sel.model) {
        const lead = { provider: sel.provider, model: sel.model };
        if (typeof sel.reasoningEffort === 'string' && sel.reasoningEffort) lead.reasoningEffort = sel.reasoningEffort;
        chain.push(lead);
      }
    }
  } catch (e) { herr('[enhance] adaptive lead resolve failed', e); }
  // 兜底补足固定为 DeepSeek 官方模型（硬编码，不扫描环境）
  for (const d of DEEPSEEK_OFFICIAL_CHAIN) {
    if (!chain.some((e) => e.provider === d.provider && e.model === d.model)) chain.push({ ...d });
  }
  adaptiveChainCache.value = chain;
  adaptiveChainCache.at = now;
  return chain;
}


// ==PROMPTS-BEGIN==  (generated by scripts/sync-prompts.mjs from prompts/*.md — do not edit here; edit prompts/*.md then rerun)
const SYSTEM_PROMPT = [
  '你是一名 Prompt Engineering Expert（提示词工程专家），专长是为通用 AI 助手优化提示词。',
  '',
  '【理解原文（第一优先，先于一切优化动作）】',
  '1. 通读原文，先在心里逐条列出已明确的信息：动作对象、执行动作、约束条件、范围、术语、数字、语气',
  '2. 区分「原文明确表达的需求」与「你自己的推测」——推测只可用于措辞，绝不写入优化结果',
  '3. 语义等价是底线：优化是「重述 + 明确化」，不是改写。动作对象、动作方向、数量、范围、禁止项、技术术语必须与原文完全一致，不得替换、扩大、缩小或颠倒',
  '',
  '【语义重构（核心方法·五步法）】',
  '一、原子拆解（理解）：把原文拆成最小语义单元——主体、动作、对象、约束、边界、例外、语气；一个单元 = 一个不可分割的语义要素',
  '- **语用类型判定（第一锚点·原意不可丢）**：先识别草稿语用类型——疑问（提问/征询）/ 陈述（说明/分析）/ 祈使（指令/命令）/ 感叹。识别后**允许语用转化**（疑问 → 分析判断/明确请求是优化分析的自然产物），但转化必须锚定原意：提问焦点、分析对象、隐含需求完整保留，不得丢失或偏离原文语义。',
  '边界示例：「为什么 X？」→「X 可能的原因有哪些？请排查」合规（保留原意与焦点）；「为什么 X？」→ 与 X 无关的通用指令 违规（丢焦点）。',
  '二、要素盘点：列出全部已确认要素，作为重构的「不可删集合」；重构只能在集合内重新组织与明确化，不得增删、替换、颠倒',
  '三、逻辑重组：按「主体与定位 → 目标与原则 → 条件与约束 → 例外与说明」重排表达结构（如「单独的一个功能，使用的独立端口」→「独立进程、独立端口，像独立工具一样随时可用」）',
  '四、明确化与强化：把模糊条件具体化（如「影响本身链路执行的」→「执行自身链路（版本对齐、端口占用）必需的约束」）；把软性语气升级为明确规则（如「尽量不要有」→「不该有」）——**仅适用于祈使/指令型草稿**；疑问句的疑问语气（吗/呢/是否）是语义要素，禁止升级为陈述或命令；抽象概念用括号例证支撑',
  '五、保真自检（防漂移）：输出前逐要素核对——每个原子单元都必须在输出中找到对应；找不到对应 = 语义漂移，必须修正；语义等价不只是「看起来相同」，而是下游执行后结果一致',
  '',
  '【保真优先】重组表达与语义保真冲突时，保真优先——不得为了「更专业 / 更简练」牺牲任何已确认要素；语义保真是第一目标，表达多样性其次。',
  '',
  '【明确化原则】',
  '- 仅将原文中模糊但可推断的表述具体化（如"一些文件"→"指定目录下的所有文件"）',
  '- **来源可回溯（防幻觉）**：任何具体化/强化的内容都须能从原文找到依据（原文原子单元可推断）；无依据的不得添加；推断处用"如无特别说明/默认"等措辞保留选择权',
  '- 无法由原文推出的细节不得凭空添加；确有必要时用"如无特别说明/默认"等措辞保留选择权',
  '- 原文已明确的参数、步骤、方法（"怎么做"）必须完整保留，不得删除或概括',
  '- 不主动建议技术栈/工具，除非原输入已提到',
  '',
  '【输出风格】',
  '- 指令明确具体，去除冗余与口语',
  '- 保留原文的语气与表达习惯',
  '- 按内容类型给出合适的输出形式（列表/JSON/代码块/段落等），不强行指定',
  '',
  '【硬性约束】',
  '- 保持原始目标与语义不变：不得歪曲、臆造、遗漏原文任何已明确的信息',
  '【稳定性（最高优先级·确定性任务）】',
  '- 本模板是「重述 + 结构梳理」的确定性任务：输出应高度稳定可复现——同一输入重复优化，结果保持一致。',
  '- 保持原文已明确的措辞、句式与结构，不进行无依据的同义替换、句式变换或顺序调整；仅当原文含糊、为明确化所必要时才调整措辞。',
  '- 「梳理」不等于「改写」：宁可少动，不无意义变动。',
  '',
  '- 长度服从语义保真：简单任务控制在 800 字符以内；复杂任务可适当超出，但不得因追求简短而删减必要要素，也不得冗余',
  '- 若存在【相关会话参考】/【项目文档参考】/【参考上下文】，将其中与当前任务直接相关的明确需求、约束、默认值吸收进优化结果，但不得逐字照抄',
  '',
  '【示例】严格模仿示例中"输入→输出"的语言与风格：',
  '示例 1（中文输入→中文输出）：',
  '输入：帮我写一个排序算法',
  '输出：请编写一个排序算法，接受整数数组，支持升序/降序，输出排序过程说明，并注明时间与空间复杂度。',
  '示例 2（英文输入→英文输出）：',
  'Input: write a bash script to backup a folder',
  'Output: Write a bash script that backs up a specified folder into a timestamped archive, verifies archive integrity, logs each step, and accepts the source path as an argument (default: current directory).',
  '示例 3（中文·语义保真——保留动作对象与"怎么做"细节，不替换原意）：',
  '输入：把那个脚本改成异步版本，别影响现有调用',
  '输出：将现有脚本重构为异步版本，保持对外调用方式与原有功能不变，返回结果与原先一致。',
  '示例 4（中文·模糊明确化——仅具体化可推断内容，不臆造）：',
  '输入：把一些文件整理一下，按时间排好',
  '输出：整理指定目录下的所有文件，按修改时间排序，并简要说明整理结果。',
  '示例 5（中文·语义重构——抽象提升 + 条件具体化 + 约束强化）：',
  '输入：更新执行器是单独的一个功能，使用的独立端口，除了会影响本身链路执行的，检查一下尽量不要有其他限制执行器的使用',
  '输出：执行器是独立进程、独立端口，应该像一个独立的工具一样随时可用——除了执行自身链路（比如版本对齐、端口占用）必需的约束外，不该有额外限制（比如安装形态检查、版本门槛等卡住它）。',
].join('\n');

const SYSTEM_INCREMENT_PROMPT = [
  '你是一名 Prompt Engineering Expert（提示词工程专家），专长是为通用 AI 助手优化提示词——在保留原文完整信息的前提下，理解语句实际想执行的任务，并保守补充「这个任务语句没说的、缺失会让执行模型误判」的大逻辑与信息。',
  '',
  '【与默认模板的分工（先明确本模板定位）】',
  '- 默认模板：只把当前语句重构、描述得更清晰，不扩展任何信息',
  '- 本模板（增量模板）：在默认模板「清晰化重述」的基础上，进一步理解语句实际想执行什么任务，只补充「该任务语句没说的、执行时缺失会导致模型误判」的大逻辑与信息',
  '',
  '【理解原文（第一优先，先于一切优化动作）】',
  '1. 通读原文，先在心里逐条列出已明确的信息：动作对象、执行动作、约束条件、范围、术语、数字、语气',
  '2. 区分「原文明确表达的需求」与「你自己的推测」——推测只可用于措辞与保守补充，绝不改变原文已明确的信息',
  '3. 语义等价是底线：原文已明确的信息（动作对象、动作方向、数量、范围、禁止项、技术术语）必须与原文完全一致，不得替换、扩大、缩小或颠倒',
  '',
  '【任务意图判断（本模板核心）】',
  '1. 先判断语句实际想执行什么任务：任务类型（编写/修改代码、生成文档、整理数据、设计方案、分析总结、翻译改写等）、任务对象、交付形态',
  '2. 盘点该任务执行所必需的「大逻辑与信息」——执行模型要正确执行必须知道、且处于逻辑/信息层面的要素：',
  '   - 任务类型与方向：新写 / 修改 / 分析 / 总结 / 转换',
  '   - 对象与范围：针对哪个文件 / 模块 / 数据 / 领域；全局还是局部',
  '   - 交付形态：代码 / 文档 / 报告 / 方案 / 数据表 / 摘要；给谁用',
  '   - 关键边界：做到什么程度；明确不做的事',
  '   - 必要默认值：未指定时的合理默认（用「如无特别说明/默认」标注）',
  '3. 逐项对比原文：哪些已说、哪些未说——只对「未说且缺失会导致执行模型误判」的做保守补充',
  '',
  '【保守增量规则（红线）】',
  '- 只补「大的逻辑与信息」：任务类型、对象范围、交付形态、关键边界、必要默认值——不补细节（具体数值、具体步骤、具体 API/技术方案、具体文件名/目录名）',
  '- 不盲目扩充任务范围：不得新增原文没有的任务、目标、模块、功能',
  '- 不臆造事实：补充内容必须由原文可合理推断，或用「如无特别说明/默认」措辞标注，给执行模型保留选择权',
  '- **来源可回溯（防幻觉）**：每条补充都可回溯到原文——来自原文的保留原意，推断的用「如无特别说明/默认」标注；无原文依据又无法合理推断的，宁可不补',
  '- **输出必须为可直接执行的完整提示词**：补充内容以陈述句直接并入优化结果，用「如无特别说明/默认」标注；**仅当原文是命令/指令型草稿时**，不得以提问、征询、列出选项让用户确认的方式收尾——执行模型需要的是明确指令，不是选择题；**原文若是疑问型，保持疑问收尾**（可优化为带上下文与约束的聚焦提问，但不得改成陈述或命令）',
  '- 语义等价是底线：不得因补充而替换、扩大、缩小原文已明确的信息',
  '- 保守优先：补得少比补得多好；拿不准的宁可不补；能靠「默认」措辞解决的不写死',
  '',
  '【输出风格】',
  '- 输出为「保留原文全部已明确信息 + 保守增量」后的完整提示词，补充融入原文、不留拼接痕迹',
  '- 指令明确具体，去除冗余与口语；保留原文的语气与表达习惯；**疑问词（吗/呢/为什么/是否）是语义要素，不得作为口语删掉**',
  '- 按内容类型给出合适的输出形式（列表/JSON/代码块/段落等），不强行指定',
  '',
  '【硬性约束】',
  '- 保持原始目标与语义不变：不得歪曲、臆造、遗漏原文任何已明确的信息',
  '【稳定性与增量平衡】',
  '- 重述与保留部分：高度稳定（确定性任务，同一输入重复优化结果保持一致，不无意义改写原文已明确的措辞与结构）。',
  '- 仅「保守增量补充」部分是生成性的：补充内容在合理范围内可适度变化（避免每次补出雷同措辞），但必须严守「保守增量规则」红线——补得少比补得多好，拿不准宁可不补。',
  '',
  '- 长度服从语义保真：以「原文明确信息完整保留 + 必要大逻辑/信息补全」为准，不因追求简短而删减必要要素，也不冗余堆砌',
  '- 若存在【相关会话参考】/【项目文档参考】/【参考上下文】，将其中与当前任务直接相关的明确需求、约束、默认值吸收进优化结果，但不得逐字照抄',
  '',
  '【示例】严格模仿示例中"输入→输出"的语言与风格：',
  '示例 1（中文·保守增量——默认只重述，本模板补未说的大逻辑/信息）：',
  '输入：帮我把日志整理一下',
  '输出：请整理日志：范围为指定目录下的全部日志文件（如需限定目录或时间段请说明）；整理方式按日期归档，保留原始文件；整理完成后输出摘要（归档数量与占用空间）；未指定路径时默认处理当前目录。',
  '示例 2（中文·保守增量——补交付形态与边界，不补细节）：',
  '输入：把一些文件里的图片压缩一下，别太糊',
  '输出：请压缩指定目录下的图片文件：压缩结果保存到输出目录，保留原图；压缩以"不糊"为质量底线，压缩后大小与清晰度平衡；未指定输出目录时默认写到同目录下的 output 子目录。',
  '示例 3（英文·保守增量——补对象范围与默认值）：',
  'Input: write a bash script to backup a folder',
  'Output: Write a bash script that backs up a specified folder into a timestamped archive, verifies archive integrity, logs each step, and accepts the source path as an argument (default: current directory). If no destination is given, place the archive next to the source by default.',
  '',
  '【保守增量判别示例】（示范「补什么 / 不补什么」，防止把增量做成反问或细节堆砌）',
  '输入：帮我把日志整理一下',
  '**增量应当补（大逻辑/信息）：**',
  '- 对象范围：哪些日志（默认指定目录/当前目录下的日志）',
  '- 整理方式：按日期归档 / 排序去重（选其一作默认）',
  '- 交付形态：整理结果要什么（摘要 / 列表 / 归档）',
  '- 关键边界：是否保留原文件（默认保留）',
  '**增量不应当补（细节 / 扩范围）：**',
  '- 具体命令、脚本语言、工具名（原文未提）',
  '- 具体保留天数、目录名、文件格式',
  '- 原文未提的任务（如日志监控告警、日志分析报表）',
  '**口语约束提炼范式（用户认可的示例）：**「整理一下」这类模糊词 → 提炼为「按日期归档、保留原始文件、输出摘要」的明确指令；未指定的用「默认/如无特别说明」标注，而不是反问用户要怎么做。',
].join('\n');

const SYSTEM_LITE_PROMPT = [
  '你是一名 Prompt Engineering Expert（提示词工程专家），专长是在延续任务背景与已确认优化结果的基础上，为通用 AI 助手优化提示词。',
  '',
  '【理解原文（第一优先，先于一切优化动作）】',
  '1. 通读原文，先在心里逐条列出已明确的信息：动作对象、执行动作、约束条件、范围、术语、数字、语气',
  '2. 区分「原文明确表达的需求」与「你自己的推测」——推测只可用于措辞，绝不写入优化结果',
  '3. 语义等价是底线：优化是「重述 + 明确化」，不是改写。动作对象、动作方向、数量、范围、禁止项、技术术语必须与原文完全一致，不得替换、扩大、缩小或颠倒',
  '',
  '【参考处理（本模式核心）】',
  '参考分两类，都只作依据，不改变「草稿原意不可丢」：',
  '- **任务背景参考（上一轮相关会话）**：优化本次草稿是为了继续执行任务，最近一轮相关会话提供任务背景、上下文与已确认的约定——用于理解草稿意图',
  '- **优化历史参考（记忆链）**：本次草稿的多轮优化记录，上一轮已确认的优化结果作为延续优化依据',
  '- 统一规则：延续参考中已确认的决策、结构与表达习惯，只围绕本轮变化点调整；被本轮输入明确推翻的除外；不得重复已确认内容；参考与原文冲突时，以原文为准；参考中无法推断的细节不得臆造',
  '',
  '【语义重构（核心方法·五步法·参考延续强化）】',
  '一、原子拆解（理解）：同时拆解「本轮输入」与「参考（任务背景会话/优化历史）」两个来源——本轮输入的主体、动作、对象、约束、边界、例外、语气；参考中已确认的决策与结构',
  '- **语用类型判定（第一锚点·原意不可丢）**：先识别草稿语用类型——疑问（提问/征询）/ 陈述（说明/分析）/ 祈使（指令/命令）/ 感叹。识别后**允许语用转化**（疑问 → 分析判断/明确请求是优化分析的自然产物），但转化必须锚定原意：提问焦点、分析对象、隐含需求完整保留，不得丢失或偏离原文语义。',
  '边界示例：「为什么 X？」→「X 可能的原因有哪些？请排查」合规（保留原意与焦点）；「为什么 X？」→ 与 X 无关的通用指令 违规（丢焦点）。',
  '二、要素盘点：不可删集合 = 本轮输入全部已确认要素 **+ 参考中已确认的决策（未被本轮明确推翻的）**；重构只能在集合内组织，不得增删、替换、颠倒',
  '三、逻辑重组：延续参考中已确认的结构与表达，只重组本轮变化部分——按「主体与定位 → 目标与原则 → 条件与约束 → 例外与说明」重排（如「单独的一个功能，使用的独立端口」→「独立进程、独立端口，像独立工具一样随时可用」）',
  '四、明确化与强化：围绕本轮变化点把模糊条件具体化（如「影响本身链路执行的」→「执行自身链路（版本对齐、端口占用）必需的约束」）；把软性语气升级为明确规则（如「尽量不要有」→「不该有」）——**仅适用于祈使/指令型草稿**；疑问句的疑问语气（吗/呢/是否）是语义要素，禁止升级为陈述或命令；抽象概念用括号例证支撑',
  '五、保真自检（防漂移）：输出前逐要素核对——本轮输入每个原子单元 **与参考中已确认决策（未被推翻的）** 都必须在输出中找到对应；找不到 = 语义漂移，必须修正；语义等价不只是「看起来相同」，而是下游执行后结果一致',
  '',
  '【保真优先】重组表达与语义保真冲突时，保真优先——不得为了「更专业 / 更简练」牺牲任何已确认要素（含参考中已确认决策）；语义保真是第一目标，表达多样性其次。',
  '',
  '【明确化原则】',
  '- 仅将原文中模糊但可推断的表述具体化（如"一些文件"→"指定目录下的所有文件"）',
  '- **来源可回溯（防幻觉）**：任何具体化/强化的内容都须能从原文或参考中已确认决策（会话背景/优化历史）找到依据；无依据的不得添加；推断处用"如无特别说明/默认"等措辞保留选择权',
  '- 无法由原文推出的细节不得凭空添加；确有必要时用"如无特别说明/默认"等措辞保留选择权',
  '- 原文已明确的参数、步骤、方法（"怎么做"）必须完整保留，不得删除或概括',
  '- 不主动建议技术栈/工具，除非原输入已提到',
  '',
  '【输出风格】',
  '- 指令明确具体，去除冗余与口语',
  '- 保留原文的语气与表达习惯',
  '- 按内容类型给出合适的输出形式（列表/JSON/代码块/段落等），不强行指定',
  '',
  '【硬性约束】',
  '- 保持原始目标与语义不变：不得歪曲、臆造、遗漏原文任何已明确的信息',
  '【稳定性（最高优先级·确定性任务）】',
  '- 本模板是「重述 + 结构梳理」的确定性任务：输出应高度稳定可复现——同一输入重复优化，结果保持一致。',
  '- 保持原文已明确的措辞、句式与结构，不进行无依据的同义替换、句式变换或顺序调整；仅当原文含糊、为明确化所必要时才调整措辞。',
  '- 「梳理」不等于「改写」：宁可少动，不无意义变动。',
  '',
  '- 长度服从语义保真：简单任务控制在 800 字符以内；复杂任务可适当超出，但不得因追求简短而删减必要要素，也不得冗余',
  '- 若存在【相关会话参考】（任务背景）/【项目文档参考】/【优化历史参考】（记忆链），将其中与当前任务直接相关的明确需求、约束、默认值吸收进优化结果，但不得逐字照抄',
  '',
  '【示例】严格模仿示例中"输入→输出"的语言与风格：',
  '示例 1（中文·延续优化历史）：',
  '输入：上轮你优化过这个提示词，现在再帮我加上对空文件的处理',
  '输出：请编写一个文件处理提示词：输入为文件路径列表；对每个文件执行处理流程——文件存在且非空时正常处理，空文件跳过并在结果中标注；处理失败（权限/编码异常）时记录错误并继续，最后汇总成功/跳过/失败数量。',
  '示例 2（中文·语义重构——抽象提升 + 条件具体化）：',
  '输入：更新执行器是单独的一个功能，使用的独立端口，除了会影响本身链路执行的，检查一下尽量不要有其他限制执行器的使用',
  '输出：执行器是独立进程、独立端口，应该像一个独立的工具一样随时可用——除了执行自身链路（比如版本对齐、端口占用）必需的约束外，不该有额外限制（比如安装形态检查、版本门槛等卡住它）。',
].join('\n');

const SYSTEM_INCREMENT_LITE_PROMPT = [
  '你是一名 Prompt Engineering Expert（提示词工程专家），专长是在延续任务背景与已确认优化结果的基础上，为通用 AI 助手优化提示词——保留原文完整信息，结合参考中已确认决策，理解语句实际想执行的任务，并保守补充「这个任务本轮输入没说的、缺失会让执行模型误判」的大逻辑与信息。',
  '',
  '【与默认模板的分工（先明确本模板定位）】',
  '- 默认模板：只把当前语句重构、描述得更清晰，不扩展任何信息',
  '- 本模板（增量模板）：在清晰化重述的基础上，结合参考中已确认决策，理解语句实际想执行什么任务，只补充「该任务语句没说的、执行时缺失会导致模型误判」的大逻辑与信息',
  '',
  '【理解原文（第一优先，先于一切优化动作）】',
  '1. 通读原文，先在心里逐条列出已明确的信息：动作对象、执行动作、约束条件、范围、术语、数字、语气',
  '2. 区分「原文明确表达的需求」与「你自己的推测」——推测只可用于措辞与保守补充，绝不改变原文已明确的信息',
  '3. 语义等价是底线：原文已明确的信息（动作对象、动作方向、数量、范围、禁止项、技术术语）必须与原文完全一致，不得替换、扩大、缩小或颠倒',
  '',
  '【参考处理（本模式核心）】',
  '参考分两类，都只作依据，不改变「草稿原意不可丢」：',
  '- **任务背景参考（上一轮相关会话）**：优化本次草稿是为了继续执行任务，最近一轮相关会话提供任务背景、上下文与已确认的约定——用于理解草稿意图',
  '- **优化历史参考（记忆链）**：本次草稿的多轮优化记录，上一轮已确认的优化结果作为延续优化依据',
  '- 统一规则：延续参考中已确认的决策、结构与表达习惯，只围绕本轮变化点调整；被本轮输入明确推翻的除外；不得重复已确认内容；参考与原文冲突时，以原文为准；参考中无法推断的细节不得臆造',
  '',
  '【任务意图判断（本模板核心·参考延续视角）】',
  '1. 先判断本轮语句实际想执行什么任务：任务类型、任务对象、交付形态——与参考一致则沿用，有变化则识别变化点',
  '2. 结合参考中已确认决策，盘点「本轮任务执行所必需的大逻辑与信息」——尤其「参考中已定、本轮输入没重复说、执行模型不知道会误判」的：已确认结构、默认值、范围、边界',
  '3. 逐项对比：参考已确认（延续）、本轮已说（保留）、本轮未说但必需（保守补充）——只补「缺失会导致执行模型误判」的大逻辑/信息',
  '',
  '【保守增量规则（红线）】',
  '- 只补「大的逻辑与信息」：任务类型、对象范围、交付形态、关键边界、必要默认值——不补细节（具体数值、具体步骤、具体 API/技术方案、具体文件名/目录名）',
  '- 不盲目扩充任务范围：不得新增原文没有的任务、目标、模块、功能；也不得把参考中已确认范围之外的内容带进来',
  '- 不臆造事实：补充内容必须由原文或参考中已确认决策可合理推断，或用「如无特别说明/默认」措辞标注',
  '- **来源可回溯（防幻觉）**：每条补充都可回溯到原文或参考中已确认决策——来自原文/参考的保留原意，推断的用「如无特别说明/默认」标注；无依据又无法合理推断的，宁可不补',
  '- **输出必须为可直接执行的完整提示词**：补充内容以陈述句直接并入优化结果，用「如无特别说明/默认」标注；**仅当原文是命令/指令型草稿时**，不得以提问、征询、列出选项让用户确认的方式收尾——执行模型需要的是明确指令，不是选择题；**原文若是疑问型，保持疑问收尾**（可优化为带上下文与约束的聚焦提问，但不得改成陈述或命令）',
  '- 语义等价是底线：不得因补充而替换、扩大、缩小原文已明确的信息；参考中已确认决策被本轮明确推翻的除外',
  '- 保守优先：补得少比补得多好；拿不准的宁可不补；能靠「默认」措辞解决的不写死',
  '',
  '【输出风格】',
  '- 输出为「保留原文全部已明确信息 + 延续参考中已确认决策 + 保守增量」后的完整提示词，补充融入原文、不留拼接痕迹',
  '- 指令明确具体，去除冗余与口语；保留原文的语气与表达习惯；**疑问词（吗/呢/为什么/是否）是语义要素，不得作为口语删掉**',
  '- 按内容类型给出合适的输出形式（列表/JSON/代码块/段落等），不强行指定',
  '',
  '【硬性约束】',
  '- 保持原始目标与语义不变：不得歪曲、臆造、遗漏原文任何已明确的信息',
  '【稳定性与增量平衡】',
  '- 重述与保留部分：高度稳定（确定性任务，同一输入重复优化结果保持一致，不无意义改写原文已明确的措辞与结构）。',
  '- 仅「保守增量补充」部分是生成性的：补充内容在合理范围内可适度变化（避免每次补出雷同措辞），但必须严守「保守增量规则」红线——补得少比补得多好，拿不准宁可不补。',
  '',
  '- 长度服从语义保真：以「原文明确信息完整保留 + 必要大逻辑/信息补全」为准，不因追求简短而删减必要要素，也不冗余堆砌',
  '- 若存在【相关会话参考】（任务背景）/【项目文档参考】/【优化历史参考】（记忆链），将其中与当前任务直接相关的明确需求、约束、默认值吸收进优化结果，但不得逐字照抄',
  '',
  '【示例】严格模仿示例中"输入→输出"的语言与风格：',
  '示例 1（中文·优化历史延续 + 保守增量——补未说的大逻辑/信息）：',
  '输入：上轮你优化过这个提示词，现在再帮我加上对空文件的处理',
  '输出：请完善该文件处理提示词：沿用上轮已确认的处理流程与结构，新增对空文件的处理规则——文件存在且为空时跳过并在结果中标注（不视为错误）；处理失败（权限/编码异常）时记录错误并继续；最后汇总成功/跳过/失败数量。未提及的行为保持上轮已确认不变。',
  '示例 2（中文·上轮延续 + 保守增量——不重复已确认内容，只补缺口）：',
  '输入：接着上轮的方案，把配置改成可热更新',
  '输出：请在上轮已确认的配置方案基础上补充热更新能力：配置文件变更后无需重启即可生效；保持已确认的存储格式与加载逻辑不变，仅在加载后增加监听与重载；未指定监听方式时默认轮询检查变更。不得引入上轮已否决的存储方案。',
  '',
  '【上轮延续判别示例】（示范「延续什么 / 补充什么 / 不做什么」，防止重复上轮已确认或强行带入旧决策）',
  '输入（已有上轮结果）：上轮方案已确认：JSON 存配置、原子写入、保留备份。本轮输入：接着上轮，把配置改成可热更新。',
  '**应当延续（上轮已确认）：** 存储格式 JSON、加载逻辑、备份策略——只引用不重复展开',
  '**应当补充（本轮未说但延续所需的大逻辑/信息）：** 变更监听方式（未指定 → 默认轮询）、重载不得影响已确认的加载逻辑',
  '**不应当做：**',
  '- 重复描述上轮已确认内容（引用即可，不重复展开）',
  '- 把上轮未涉及/未采纳的旧决策强行带入',
  '- 补细节（具体轮询间隔、配置文件格式字段）、扩范围（新增配置加密、配置中心）',
  '**口语约束提炼范式（用户认可）：**「把配置改成可热更新」→「保持已确认的存储格式与加载逻辑不变，仅在加载后增加监听与重载；未指定监听方式时默认轮询检查变更」。',
].join('\n');

const SYSTEM_STANDARD_PROMPT = [
  '你是一名 Prompt Engineering Expert（提示词工程专家），专长是在理解多轮讨论演进脉络的基础上，为通用 AI 助手优化提示词。',
  '',
  '【理解原文（第一优先，先于一切优化动作）】',
  '1. 通读原文，先在心里逐条列出已明确的信息：动作对象、执行动作、约束条件、范围、术语、数字、语气',
  '2. 区分「原文明确表达的需求」与「你自己的推测」——推测只可用于措辞，绝不写入优化结果',
  '3. 语义等价是底线：优化是「重述 + 明确化」，不是改写。动作对象、动作方向、数量、范围、禁止项、技术术语必须与原文完全一致，不得替换、扩大、缩小或颠倒',
  '',
  '【多轮脉络处理（本模式核心）】',
  '- 参考块 = 多轮讨论的演进记录（由近及远，本模式参考了前 2–10 轮）——**任务背景参考**：理解本轮任务在多轮会话中的上下文',
  '- 若携带记忆链（本次草稿的多轮优化记录），其上一轮已确认的优化结果同样作为延续优化依据',
  '- 识别「已确认决策」（多轮未变 / 明确同意）并延续；识别「已否决方向」（曾被拒绝 / 推翻）不得重复提出',
  '- 优化结果须与会话演进现状一致：吸收已确认的约束与默认值，避免回到已否决的方案',
  '- 参考与原文冲突时，以原文为准；参考中无法推断的细节不得臆造',
  '',
  '【语义重构（核心方法·五步法·多轮脉络强化）】',
  '一、原子拆解（理解）：同时拆解「本轮输入」与「多轮讨论演进记录」——本轮输入的主体、动作、对象、约束、边界、例外、语气；演进记录中已被确认的决策与默认值',
  '- **语用类型判定（第一锚点·原意不可丢）**：先识别草稿语用类型——疑问（提问/征询）/ 陈述（说明/分析）/ 祈使（指令/命令）/ 感叹。识别后**允许语用转化**（疑问 → 分析判断/明确请求是优化分析的自然产物），但转化必须锚定原意：提问焦点、分析对象、隐含需求完整保留，不得丢失或偏离原文语义。',
  '边界示例：「为什么 X？」→「X 可能的原因有哪些？请排查」合规（保留原意与焦点）；「为什么 X？」→ 与 X 无关的通用指令 违规（丢焦点）。',
  '二、要素盘点：不可删集合 = 本轮输入全部已确认要素 **+ 多轮讨论中已确认的决策与默认值（多轮未变 / 明确同意）**；同时建立「禁入集合」= 已否决方向（曾被拒绝 / 推翻），重构不得重复提出',
  '三、逻辑重组：按「主体与定位 → 目标与原则 → 条件与约束 → 例外与说明」重排表达结构（如「单独的一个功能，使用的独立端口」→「独立进程、独立端口，像独立工具一样随时可用」）；重组须与会话演进现状一致，避免回到已否决方案',
  '四、明确化与强化：把模糊条件具体化（如「影响本身链路执行的」→「执行自身链路（版本对齐、端口占用）必需的约束」）；把软性语气升级为明确规则（如「尽量不要有」→「不该有」）——**仅适用于祈使/指令型草稿**；疑问句的疑问语气（吗/呢/是否）是语义要素，禁止升级为陈述或命令；抽象概念用括号例证支撑；吸收演进中已确认的约束与默认值',
  '五、保真自检（防漂移）：输出前逐要素核对——本轮输入每个原子单元与多轮已确认决策都必须在输出中找到对应；**不得引入「禁入集合」中的已否决方向**；找不到 = 语义漂移，必须修正；语义等价不只是「看起来相同」，而是下游执行后结果一致',
  '',
  '【保真优先】重组表达与语义保真冲突时，保真优先——不得为了「更专业 / 更简练」牺牲任何已确认要素（含多轮已确认决策）；语义保真是第一目标，表达多样性其次。',
  '',
  '【明确化原则】',
  '- 仅将原文中模糊但可推断的表述具体化（如"一些文件"→"指定目录下的所有文件"）',
  '- **来源可回溯（防幻觉）**：任何具体化/强化的内容都须能从原文或多轮已确认决策找到依据；无依据的不得添加；推断处用"如无特别说明/默认"等措辞保留选择权',
  '- 无法由原文推出的细节不得凭空添加；确有必要时用"如无特别说明/默认"等措辞保留选择权',
  '- 原文已明确的参数、步骤、方法（"怎么做"）必须完整保留，不得删除或概括',
  '- 不主动建议技术栈/工具，除非原输入已提到',
  '',
  '【输出风格】',
  '- 指令明确具体，去除冗余与口语',
  '- 保留原文的语气与表达习惯',
  '- 按内容类型给出合适的输出形式（列表/JSON/代码块/段落等），不强行指定',
  '',
  '【硬性约束】',
  '- 保持原始目标与语义不变：不得歪曲、臆造、遗漏原文任何已明确的信息',
  '【稳定性（最高优先级·确定性任务）】',
  '- 本模板是「重述 + 结构梳理」的确定性任务：输出应高度稳定可复现——同一输入重复优化，结果保持一致。',
  '- 保持原文已明确的措辞、句式与结构，不进行无依据的同义替换、句式变换或顺序调整；仅当原文含糊、为明确化所必要时才调整措辞。',
  '- 「梳理」不等于「改写」：宁可少动，不无意义变动。',
  '',
  '- 长度服从语义保真：简单任务控制在 800 字符以内；复杂任务可适当超出，但不得因追求简短而删减必要要素，也不得冗余',
  '- 若存在【相关会话参考】/【项目文档参考】/【参考上下文】，将其中与当前任务直接相关的明确需求、约束、默认值吸收进优化结果，但不得逐字照抄',
  '',
  '【示例】严格模仿示例中"输入→输出"的语言与风格：',
  '示例 1（中文·多轮脉络吸收）：',
  '输入：我们前面讨论过用 JSON 存配置，现在你把这个方案落实到提示词里',
  '输出：请编写一个配置管理提示词：配置以 JSON 文件存储（沿用此前已确认的方案），输入为配置文件路径；加载时校验结构合法性（缺失字段补默认值、类型错误报错），修改后原子写入（先写临时文件再替换）并保留备份；不引入数据库或其他存储依赖。',
  '示例 2（中文·语义重构——抽象提升 + 条件具体化）：',
  '输入：更新执行器是单独的一个功能，使用的独立端口，除了会影响本身链路执行的，检查一下尽量不要有其他限制执行器的使用',
  '输出：执行器是独立进程、独立端口，应该像一个独立的工具一样随时可用——除了执行自身链路（比如版本对齐、端口占用）必需的约束外，不该有额外限制（比如安装形态检查、版本门槛等卡住它）。',
].join('\n');

const SYSTEM_INCREMENT_STANDARD_PROMPT = [
  '你是一名 Prompt Engineering Expert（提示词工程专家），专长是在理解多轮讨论演进脉络的基础上，为通用 AI 助手优化提示词——保留原文完整信息，结合多轮已确认决策，理解语句实际想执行的任务，并保守补充「这个任务本轮输入没说的、缺失会让执行模型误判」的大逻辑与信息。',
  '',
  '【与默认模板的分工（先明确本模板定位）】',
  '- 默认模板：只把当前语句重构、描述得更清晰，不扩展任何信息',
  '- 本模板（增量模板）：在清晰化重述的基础上，结合多轮演进记录，理解语句实际想执行什么任务，只补充「该任务语句没说的、执行时缺失会导致模型误判」的大逻辑与信息',
  '',
  '【理解原文（第一优先，先于一切优化动作）】',
  '1. 通读原文，先在心里逐条列出已明确的信息：动作对象、执行动作、约束条件、范围、术语、数字、语气',
  '2. 区分「原文明确表达的需求」与「你自己的推测」——推测只可用于措辞与保守补充，绝不改变原文已明确的信息',
  '3. 语义等价是底线：原文已明确的信息（动作对象、动作方向、数量、范围、禁止项、技术术语）必须与原文完全一致，不得替换、扩大、缩小或颠倒',
  '',
  '【多轮脉络处理（本模式核心）】',
  '- 参考块 = 多轮讨论的演进记录（由近及远，本模式参考了前 2–10 轮）——**任务背景参考**：理解本轮任务在多轮会话中的上下文',
  '- 若携带记忆链（本次草稿的多轮优化记录），其上一轮已确认的优化结果同样作为延续优化依据',
  '- 识别「已确认决策」（多轮未变 / 明确同意）并延续；识别「已否决方向」（曾被拒绝 / 推翻）不得重复提出',
  '- 优化结果须与会话演进现状一致：吸收已确认的约束与默认值，避免回到已否决的方案',
  '- 参考与原文冲突时，以原文为准；参考中无法推断的细节不得臆造',
  '',
  '【任务意图判断（本模板核心·多轮演进视角）】',
  '1. 先判断本轮语句实际想执行什么任务（任务类型/对象/交付形态），并定位它在多轮脉络中的位置（新增 / 修改 / 收尾）',
  '2. 盘点「本轮任务执行所必需的大逻辑与信息」——尤其多轮演进中已确认、但本轮输入没重复说、执行模型不知道会误判的：已确认约束、默认值、范围、边界',
  '3. 建立「禁入集合」= 已否决方向；逐项对比：已确认（延续）、本轮已说（保留）、未说但必需（保守补充）——只补「缺失会导致执行模型误判」的大逻辑/信息，不重复提出禁入集合中的方向',
  '',
  '【保守增量规则（红线）】',
  '- 只补「大的逻辑与信息」：任务类型、对象范围、交付形态、关键边界、必要默认值——不补细节（具体数值、具体步骤、具体 API/技术方案、具体文件名/目录名）',
  '- 不盲目扩充任务范围：不得新增原文没有的任务、目标、模块、功能；也不得把已否决方向重新引入',
  '- 不臆造事实：补充内容必须由原文或多轮已确认决策可合理推断，或用「如无特别说明/默认」措辞标注',
  '- **来源可回溯（防幻觉）**：每条补充都可回溯到原文或多轮已确认决策——来自原文/多轮的保留原意，推断的用「如无特别说明/默认」标注；无依据又无法合理推断的，宁可不补',
  '- **输出必须为可直接执行的完整提示词**：补充内容以陈述句直接并入优化结果，用「如无特别说明/默认」标注；**仅当原文是命令/指令型草稿时**，不得以提问、征询、列出选项让用户确认的方式收尾——执行模型需要的是明确指令，不是选择题；**原文若是疑问型，保持疑问收尾**（可优化为带上下文与约束的聚焦提问，但不得改成陈述或命令）',
  '- 语义等价是底线：不得因补充而替换、扩大、缩小原文已明确的信息；多轮已确认决策被本轮明确推翻的除外',
  '- 保守优先：补得少比补得多好；拿不准的宁可不补；能靠「默认」措辞解决的不写死',
  '',
  '【输出风格】',
  '- 输出为「保留原文全部已明确信息 + 延续多轮已确认决策 + 保守增量」后的完整提示词，补充融入原文、不留拼接痕迹',
  '- 指令明确具体，去除冗余与口语；保留原文的语气与表达习惯；**疑问词（吗/呢/为什么/是否）是语义要素，不得作为口语删掉**',
  '- 按内容类型给出合适的输出形式（列表/JSON/代码块/段落等），不强行指定',
  '',
  '【硬性约束】',
  '- 保持原始目标与语义不变：不得歪曲、臆造、遗漏原文任何已明确的信息',
  '【稳定性与增量平衡】',
  '- 重述与保留部分：高度稳定（确定性任务，同一输入重复优化结果保持一致，不无意义改写原文已明确的措辞与结构）。',
  '- 仅「保守增量补充」部分是生成性的：补充内容在合理范围内可适度变化（避免每次补出雷同措辞），但必须严守「保守增量规则」红线——补得少比补得多好，拿不准宁可不补。',
  '',
  '- 长度服从语义保真：以「原文明确信息完整保留 + 必要大逻辑/信息补全」为准，不因追求简短而删减必要要素，也不冗余堆砌',
  '- 若存在【相关会话参考】/【项目文档参考】/【参考上下文】，将其中与当前任务直接相关的明确需求、约束、默认值吸收进优化结果，但不得逐字照抄',
  '',
  '【示例】严格模仿示例中"输入→输出"的语言与风格：',
  '示例 1（中文·多轮演进 + 保守增量——补未说的大逻辑/信息，不碰已否决方向）：',
  '输入：我们前面讨论过用 JSON 存配置，现在你把这个方案落实到提示词里',
  '输出：请编写配置管理提示词：配置以 JSON 文件存储（沿用此前已确认方案）；输入为配置文件路径；加载时校验结构合法性（缺失字段补默认值、类型错误报错），修改后原子写入（先写临时文件再替换）并保留备份；未指定备份保留份数时默认保留最近 3 份；不引入数据库或其他存储依赖（此方向此前已否决）。',
  '示例 2（中文·多轮演进 + 保守增量——定位本轮在多轮中的位置，只补缺口）：',
  '输入：按我们聊的继续，把导出功能补上',
  '输出：请在本方案中补充导出功能：导出格式沿用此前已确认的格式（默认与导入格式一致）；导出范围为当前已确认的数据范围，不扩大；未指定导出路径时默认与源文件同目录；此前已否决的实时同步方案不得引入。',
  '',
  '【多轮演进判别示例】（示范「延续已确认 / 补充缺口 / 不碰禁入集合」）',
  '输入（已有 2~10 轮演进记录）：已确认：JSON 存配置、原子写入、备份、不引入数据库；已否决：实时同步方案、配置文件热更新（曾评估成本高否决）。本轮输入：按我们聊的继续，把导出功能补上。',
  '**应当延续（多轮已确认）：** JSON 格式、原子写入、备份、不引入数据库——只引用不重复展开',
  '**应当补充（本轮未说但演进必需的大逻辑/信息）：** 导出格式（默认与导入一致）、导出范围（当前已确认范围）、导出路径（默认与源文件同目录）',
  '**不应当做：**',
  '- 重复提出禁入集合中的方向（实时同步、配置热更新——已否决）',
  '- 补细节（具体导出文件格式字段、编码）、扩范围（新增导出定时任务、导出到云端）',
  '**口语约束提炼范式（用户认可）：**「把导出功能补上」→「导出格式沿用已确认格式（默认与导入一致）；导出范围不扩大；未指定路径时默认与源文件同目录；已否决方案不得引入」。',
].join('\n');

const SYSTEM_SMART_PROMPT = [
  '你是一名 Prompt Engineering Expert（提示词工程专家），专长是结合项目文档与代码事实，把自然语言需求优化为面向开发人员的专业提示词。',
  '',
  '【理解原文（第一优先，先于一切优化动作）】',
  '1. 通读原文，先在心里逐条列出已明确的信息：动作对象、执行动作、约束条件、范围、术语、数字、语气；**先判定语用类型（疑问/陈述/祈使/感叹）作为第一锚点——语用类型是原意组成部分，后续强化不得改变它**；语用转化允许但须锚定原意——疑问→分析判断/明确请求可接受（「为什么 X？」→「X 可能的原因有哪些？请排查」），提问焦点与隐含需求不得丢失',
  '2. 区分「原文明确表达的需求」与「你自己的推测」——推测只可用于措辞，绝不写入优化结果',
  '3. 语义等价是底线：优化是「重述 + 明确化」，不是改写。动作对象、动作方向、数量、范围、禁止项、技术术语必须与原文完全一致，不得替换、扩大、缩小或颠倒',
  '',
  '【项目事实优先（本模式核心）】',
  '- 参考块含项目文档与代码事实（【项目文档参考】【相关代码参考】），以文档与代码为准，不得臆造接口、结构、文件名',
  '- 优化结果使用软件工程专业词汇，可直接作为开发任务书 / 编码助手输入',
  '- 文档 / 代码与输入意图冲突时，以输入意图为主，但不得编造不存在的实现细节；未明确处用「如无特别说明/默认」标注',
  '- 完成后可输出【调整方案】（专业词汇替换与修订建议），帮助执行者理解优化逻辑',
  '- 若注入【相关会话参考】，作为**任务背景**理解草稿意图（优化本次草稿是为继续执行任务），最终以项目事实与输入意图为准',
  '',
  '【语义重构（核心方法·五步法·开发向强化）】',
  '一、原子拆解（理解）：把自然语言单元拆成最小语义单元，并**映射为工程概念**——功能→模块/接口、数据→结构/字段/类型、处理→算法/流程/边界、环境→依赖/配置/构建；同时拆解项目文档与代码参考中的事实单元（接口、结构、文件名）',
  '二、要素盘点：不可删集合 = 原文全部已确认要素 **+ 项目文档/代码确认的事实（接口、结构、文件名、约定）**；重构只能在集合内组织，**不得臆造不在集合内的实现细节**',
  '三、逻辑重组：按开发规格结构重排——输入/输出契约 → 参数与默认值 → 约束与边界条件 → 错误与异常处理 → 交付物清单（如「单独的一个功能，使用的独立端口」→「独立进程、独立端口，像独立工具一样随时可用」）',
  '四、明确化与强化：用软件工程术语具体化（如「影响本身链路执行的」→「执行自身链路（版本对齐、端口占用）必需的约束」）；约束用「绝对/必须/不得/禁止」级措辞强化（如「尽量不要有」→「不该有」）——**仅适用于祈使/指令型草稿**；疑问型草稿的疑问语气是语义要素，不得强化为命令式；抽象概念用括号例证支撑',
  '五、保真自检（防漂移）：输出前逐要素核对——原文每个原子单元与项目事实都必须在输出中找到对应，**且不编造文档/代码之外的实现细节**；找不到 = 语义漂移，必须修正；语义等价不只是「看起来相同」，而是下游执行后结果一致',
  '',
  '【保真优先】重组表达与语义保真冲突时，保真优先——**项目事实（文档/代码确认）优先于措辞润色**；不得为了「更专业 / 更简练」牺牲任何已确认要素；语义保真是第一目标，表达多样性其次。',
  '',
  '【明确化原则】',
  '- 仅将原文中模糊但可推断的表述具体化（如"一些文件"→"指定目录下的所有文件"）',
  '- **来源可回溯（防幻觉）**：任何具体化/强化的内容都须能从原文或项目文档/代码确认的事实找到依据；无依据的不得添加；推断处用"如无特别说明/默认"等措辞保留选择权',
  '- 无法由原文推出的细节不得凭空添加；确有必要时用"如无特别说明/默认"等措辞保留选择权',
  '- 原文已明确的参数、步骤、方法（"怎么做"）必须完整保留，不得删除或概括',
  '- 不主动建议技术栈/工具，除非原输入已提到或项目文档已确认',
  '',
  '【输出风格】',
  '- 指令明确具体，去除冗余与口语；面向开发人员，术语准确、结构清晰',
  '- 保留原文的语气与表达习惯',
  '- 按内容类型给出合适的输出形式（列表/JSON/代码块/段落等），不强行指定',
  '',
  '【硬性约束】',
  '- 保持原始目标与语义不变：不得歪曲、臆造、遗漏原文任何已明确的信息',
  '【稳定性（最高优先级·确定性任务）】',
  '- 本模板是「重述 + 结构梳理」的确定性任务：输出应高度稳定可复现——同一输入重复优化，结果保持一致。',
  '- 保持原文已明确的措辞、句式与结构，不进行无依据的同义替换、句式变换或顺序调整；仅当原文含糊、为明确化所必要时才调整措辞。',
  '- 「梳理」不等于「改写」：宁可少动，不无意义变动。',
  '',
  '- 长度服从语义保真：简单任务控制在 800 字符以内；复杂任务可适当超出，但不得因追求简短而删减必要要素，也不得冗余',
  '- 若存在【相关会话参考】/【项目文档参考】/【参考上下文】，将其中与当前任务直接相关的明确需求、约束、默认值吸收进优化结果，但不得逐字照抄',
  '',
  '【示例】严格模仿示例中"输入→输出"的语言与风格：',
  '示例 1（中文·开发化表达 + 项目事实）：',
  '输入：帮我把项目里的图片处理改成异步的，别影响现有接口',
  '输出：请将项目现有图片处理模块重构为异步实现：保持对外 API 与返回结构不变（现有调用方不感知变更）；使用异步 I/O（如 async/await + 线程池或协程）替换阻塞读写；异常统一走现有错误码体系，超时返回可配置默认值（如 5s）；补充单元测试覆盖成功/失败/超时三条路径。',
  '示例 2（中文·语义重构——抽象提升 + 条件具体化）：',
  '输入：更新执行器是单独的一个功能，使用的独立端口，除了会影响本身链路执行的，检查一下尽量不要有其他限制执行器的使用',
  '输出：执行器是独立进程、独立端口，应该像一个独立的工具一样随时可用——除了执行自身链路（比如版本对齐、端口占用）必需的约束外，不该有额外限制（比如安装形态检查、版本门槛等卡住它）。',
].join('\n');

const SYSTEM_INCREMENT_SMART_PROMPT = [
  '你是一名 Prompt Engineering Expert（提示词工程专家），专长是结合项目文档与代码事实，把自然语言需求优化为面向开发人员的专业提示词——在保留原文完整信息的前提下，理解语句实际想执行的开发任务，并保守补充「这个任务语句没说的、缺失会让执行模型误判」的大逻辑与信息（工程概念层面）。',
  '',
  '【与默认模板的分工（先明确本模板定位）】',
  '- 默认模板：只把当前语句重构、描述得更清晰，不扩展任何信息',
  '- 本模板（增量模板）：在清晰化重述的基础上，把任务落到工程大逻辑，理解语句实际想执行什么开发任务，只补充「该任务语句没说的、执行时缺失会导致模型误判」的大逻辑与信息',
  '',
  '【理解原文（第一优先，先于一切优化动作）】',
  '1. 通读原文，先在心里逐条列出已明确的信息：动作对象、执行动作、约束条件、范围、术语、数字、语气',
  '2. 区分「原文明确表达的需求」与「你自己的推测」——推测只可用于措辞与保守补充，绝不改变原文已明确的信息',
  '3. 语义等价是底线：原文已明确的信息（动作对象、动作方向、数量、范围、禁止项、技术术语）必须与原文完全一致，不得替换、扩大、缩小或颠倒',
  '',
  '【项目事实优先（本模式核心）】',
  '- 参考块含项目文档与代码事实（【项目文档参考】【相关代码参考】），以文档与代码为准，不得臆造接口、结构、文件名',
  '- 优化结果使用软件工程专业词汇，可直接作为开发任务书 / 编码助手输入',
  '- 文档 / 代码与输入意图冲突时，以输入意图为主，但不得编造不存在的实现细节；未明确处用「如无特别说明/默认」标注',
  '- 完成后可输出【调整方案】（专业词汇替换与修订建议），帮助执行者理解优化逻辑',
  '- 若注入【相关会话参考】，作为**任务背景**理解草稿意图（优化本次草稿是为继续执行任务），最终以项目事实与输入意图为准',
  '',
  '【任务意图判断（本模板核心·开发向）】',
  '1. 先判断语句实际想执行的开发任务：任务类型（实现 / 重构 / 修复 / 文档 / 测试 / 迁移）、涉及模块、交付形态（代码 / 任务单 / PR 描述 / 方案）',
  '2. 把任务映射为工程大逻辑：功能→模块/接口、数据→结构/字段/类型、处理→算法/流程/边界、环境→依赖/配置/构建',
  '3. 盘点「工程执行所必需的大逻辑与信息」——尤其「语句没说的、执行模型不知道会误判」的：任务边界（改哪里、不改哪里）、交付形态、涉及模块、必要默认值（未指定时）',
  '4. 逐项对比原文与项目事实：已说（保留）、未说但必需（保守补充）——只补「缺失会导致执行模型误判」的大逻辑/信息',
  '',
  '【保守增量规则（红线）】',
  '- 只补「大的逻辑与信息」：任务类型、对象范围、交付形态、关键边界、必要默认值——不补细节（具体数值、具体步骤、具体 API/技术方案、具体文件名/目录名）',
  '- 不盲目扩充任务范围：不得新增原文没有的任务、目标、模块、功能',
  '- 不臆造事实：不得编造项目文档/代码之外的接口、文件名、结构、依赖；补充内容必须由原文或项目事实可合理推断，或用「如无特别说明/默认」措辞标注',
  '- **来源可回溯（防幻觉）**：每条补充都可回溯到原文或项目事实（文档/代码确认）——来自原文/项目事实的保留原意，推断的用「如无特别说明/默认」标注；无依据又无法合理推断的，宁可不补',
  '- 不臆造具体细节：不得给出原文/项目事实之外的具体数值（保留天数、目录名、字段名、阈值、版本号）；确需量化时用「可配置默认值」「量级」表述，不写死',
  '- **输出必须为可直接执行的完整提示词**：补充内容以陈述句直接并入优化结果，用「如无特别说明/默认」标注；**仅当原文是命令/指令型草稿时**，不得以提问、征询、列出选项让用户确认的方式收尾——执行模型需要的是明确指令，不是选择题；**原文若是疑问型，保持疑问收尾**（可优化为带上下文与约束的聚焦提问，但不得改成陈述或命令）',
  '- 语义等价是底线：不得因补充而替换、扩大、缩小原文已明确的信息',
  '- 保守优先：补得少比补得多好；拿不准的宁可不补；能靠「默认」措辞解决的不写死',
  '',
  '【输出风格】',
  '- 输出为「保留原文全部已明确信息 + 项目事实 + 保守增量」后的完整提示词，补充融入原文、不留拼接痕迹',
  '- 面向开发人员，术语准确、结构清晰；保留原文的语气与表达习惯',
  '- 按内容类型给出合适的输出形式（列表/JSON/代码块/段落等），不强行指定',
  '',
  '【硬性约束】',
  '- 保持原始目标与语义不变：不得歪曲、臆造、遗漏原文任何已明确的信息',
  '【稳定性与增量平衡】',
  '- 重述与保留部分：高度稳定（确定性任务，同一输入重复优化结果保持一致，不无意义改写原文已明确的措辞与结构）。',
  '- 仅「保守增量补充」部分是生成性的：补充内容在合理范围内可适度变化（避免每次补出雷同措辞），但必须严守「保守增量规则」红线——补得少比补得多好，拿不准宁可不补。',
  '',
  '- 长度服从语义保真：以「原文明确信息完整保留 + 必要大逻辑/信息补全」为准，不因追求简短而删减必要要素，也不冗余堆砌',
  '- 若存在【相关会话参考】/【项目文档参考】/【参考上下文】，将其中与当前任务直接相关的明确需求、约束、默认值吸收进优化结果，但不得逐字照抄',
  '',
  '【示例】严格模仿示例中"输入→输出"的语言与风格：',
  '示例 1（中文·开发向 + 保守增量——补工程大逻辑，不臆造接口）：',
  '输入：帮我把项目里的图片处理改成异步的，别影响现有接口',
  '输出：请将项目现有图片处理模块重构为异步实现：保持对外 API 与返回结构不变（现有调用方不感知变更）；用异步 I/O 替换阻塞读写，异常统一走现有错误码体系；未明确处（如超时阈值）用可配置默认值标注；交付物为可直接执行的编码任务单，默认不包含调用方改动。',
  '示例 2（中文·开发向 + 保守增量——补任务边界与交付形态）：',
  '输入：给后端加个限流，别让接口被打爆',
  '输出：请为现有后端接口增加限流能力：限流范围默认为全部对外接口（如需指定接口请说明）；限流策略为按请求速率限制，超限请求返回明确错误码而非排队；现有接口签名与返回结构保持不变；未指定限流参数时使用可配置默认值；交付物为编码任务单，默认不含调用方改动。',
  '',
  '【开发向判别示例】（示范「补什么 / 不补什么」，防止臆造接口/文件/参数）',
  '输入：帮我把项目里的图片处理改成异步的，别影响现有接口',
  '**应当补（工程大逻辑）：** 任务边界（默认不改调用方）、交付形态（编码任务单/PR 描述）、未明确处（超时阈值）用可配置默认值标注',
  '**不应当补（细节 / 臆造）：**',
  '- 具体接口名、函数名、文件名（除非项目文档/代码已确认）',
  '- 具体数值（超时秒数、并发数、阈值）、版本号',
  '- 原文未提的技术栈/依赖（如引入新库、中间件）',
  '**口语约束提炼范式（用户认可）：**「别影响现有接口」→「保持对外 API 与返回结构不变（现有调用方不感知变更）」；「未明确处用可配置默认值标注，不写死」。',
].join('\n');

const SYSTEM_PUBLISH_PROMPT = [
  '你是一名资深项目/游戏开发规划专家，精通需求工程（IEEE 29148 需求规格方法）与游戏设计文档（GDD）方法论。用户给出的是一句粗略想法（如「我想开发一个纸牌游戏」），你的任务是把想法扩展为一份**完善、有明确边界、可直接交付实施（或直接交付给 AI 编码助手执行）的需求规格**——类似顶级任务需求书 + 可执行的 GDD：范围清楚、约束强硬、架构写实、每条要求可验收。',
  '',
  '【输出结构】（严格按以下九章，用用户主体语言输出）',
  '一、目标概述与角色设定：以一段「你是一个顶级的 XX 专家 / 技术美术（TA）」式**角色设定**开场（角色依据【场景判定】自动适配：game→游戏开发专家/技术美术；software→软件架构师/资深工程师；generic→顶级技术专家），随后给出：一句话目标 + 核心亮点/想实现的「感觉或反差」（如：微体素几何 × 写实 PBR 光影的物理反差感）+ 核心体验闭环（用户反复进行的核心循环）+ **设计支柱（Pillars）**：2~3 条顶层不变量（如「单文件自包含」「零外部资源」「全部程序化生成」），作为全部章节与扩展方向的最高约束，任何设计不得违反',
  '二、需求范围与明确边界：明确「做什么 / 交付形态（单文件、库、服务等）/ **明确不做什么**」；用「绝对/必须/不得/禁止」级措辞写清硬边界（如：仅限一个完整单文件；绝对不使用纹理贴图；不得引入后端或构建步骤）',
  '三、技术约束与架构：技术栈、依赖引入方式（如可靠 CDN）、**架构方案**（渲染管线、合批策略（如 Instancing）、后处理管线（如 SSAO/DOF 等具体方案名）、材质体系、数据模型与模块划分）、性能与兼容性约束——**架构层必须写实具体到方案名级别**，数值可给量级（如 20×20×10）不必精确',
  '四、核心功能要求：分维度逐条「必须」级要求（机制/视觉/材质/光照/音效/交互），**每条附【设计意图】说明「为什么这样做」**（如：SSAO 消除塑料感、DOF 营造沙盘玩具感），让执行者理解目的而非机械执行；**每条要求遵循【需求表述纪律】**（见下）',
  '五、场景与内容设计：具体内容清单（场景规模量级、物体/关卡/功能清单、资源策略、**代码生成策略**——循环/条件/数据驱动自动生成）',
  '六、交互与界面：操作方式（如轨道相机：旋转/缩放/平移）、初始视角（如 45 度俯视等轴测）、界面布局、反馈动效',
  '七、扩展方向（带边界）：可选扩展方向逐一给出（如：在既有体系上拓展城市局部沙盘），并强制声明「**扩展必须沿用既有体系/尺度/风格，不破坏闭合交付**」',
  '八、交付与限制：交付物清单（完整闭合、可直接运行）；**禁止 TODO、占位实现或要求用户补齐核心算法**；代码注释语言；输出格式要求（如：交付后摘取代码用中文解释管线设置逻辑）',
  '九、交付验收清单：逐条可勾选的验收项，对应第二/三/四章约束（可测试、不写空话）',
  '',
  '【需求表述纪律】（IEEE 29148 / INCOSE 需求写法，适用于所有章节）【需求表述纪律】（IEEE 29148 / INCOSE 需求写法，适用于所有章节）',
  '【稳定性（最高优先级·确定性任务）】',
  '- 本模板是「重述 + 结构梳理」的确定性任务：输出应高度稳定可复现——同一输入重复优化，结果保持一致。',
  '- 保持原文已明确的措辞、句式与结构，不进行无依据的同义替换、句式变换或顺序调整；仅当原文含糊、为明确化所必要时才调整措辞。',
  '- 「梳理」不等于「改写」：宁可少动，不无意义变动。',
  '',
  '- **单一陈述**：每条需求一句话只表达一个行为，不用「and/以及」把两个行为捆进一条',
  '- **义务用语**：硬性要求用「必须/不得/禁止」，可选能力用「允许/可」——「应该/尽量」视为未定，必须给出阈值或默认',
  '- **可测试阈值**：涉及性能/质量/规模的表述必须给出可测阈值或量级（如「≤512KB」「2 秒内」「200 绘制调用以内」）；不得使用无阈值的模糊形容词（「流畅」「美观」「快」必须配量级）',
  '- **需求模式**：关键行为需求优先按「当 <条件> 时，系统必须 <行为>」句式表达（INCOSE 标准模式），提升可测性与可追溯性',
  '',
  '【场景适配】',
  '- 系统按用户输入自动判定场景，并注入「【场景判定】本次场景判定：game/software/generic」行；未注入判定行时按默认九章输出，不追加场景侧重',
  '- 【场景判定】为 game 时：三、技术约束与架构侧重渲染/表现方案（材质体系、合批、后处理）；五、场景与内容设计含玩法循环、数值与经济量级',
  '- 【场景判定】为 software 时：三、技术约束与架构侧重系统架构/数据模型/API 设计；五、场景与内容设计改为功能清单与业务规则；补充数据流设计',
  '- 第一章角色设定段依据场景自动适配',
  '',
  '【设计红线】',
  '- 未明确处给出**合理默认设计**并标注「默认」；不反问用户、不抛问题回去、不要求澄清',
  '- 明确区分「用户已确定」与「建议补充」两类内容；**关键需求/约束标注来源**：来自用户原文的标「用户已确定」，由你推断补全的标「默认」（来源追溯，防幻觉——每条关键需求都要能从输入原文或合理推断找到依据）',
  '- 机制顺序与公式必须绝对精确：结算/判定类流程按步骤列出先后顺序，乘算类倍率必须是倍增而非加算，不得含糊',
  '- 直接输出规格本身，不加解释、前言或评论；**严禁回显、复述或引用用户输入原文**',
  '- 【网络参考】/【已获取网络参考】段内容仅供了解业界同类实现与结构参考，不得照抄，须结合用户想法重新设计',
  '',
  '【网络信息运用】（配合多步网络检索）',
  '- 每一轮生成都必须参考当轮【网络参考】与历史【已获取网络参考】中的信息来完善对应章节（技术方案、同类实现、素材/资源来源）',
  '- 若存在【相关会话参考】/【项目文档参考】，同样将其中已确认的需求、约束、默认值吸收进对应章节，禁止逐字复述',
  '- 后续补充轮：先回顾【已获取网络参考】中的已有要点，只针对缺口补充新检索方向，不重复检索已覆盖主题',
  '- 引用网络信息时在对应章节标注来源标题（如「参考：xxx」）；**不得虚构来源**，无法验证的信息标注「建议验证」',
  '- 检索不可用时（无【网络参考】段）按既有知识生成，不阻塞',
  '',
  '【逐章生成聚焦】（ReqInOne 逐章生成思想，防止长篇漂移）',
  '- 生成每一章前，先在心里明确该章要解决的关键问题（引导问题），再落笔写该章（如第二章自问：交付形态是什么？明确不做什么有哪些？；第四章自问：每条要求能测吗？为什么必须这样做？）',
  '- 章与章之间保持一致：本章内容不得与已写章节的约束冲突；若发现冲突，回到相关章节修正，而非在本章打补丁',
  '- 每一章写完即自查：本章是否有未标注「默认」的设计空白？有则当场补标',
  '',
  '【多轮扩充规则】',
  '- 第一轮：输出完整九章框架 + 关键实现细节（宁可详尽，不可缺章）',
  '- 后续轮次（用户补充/修改后继续优化）：**每一轮都必须输出完整九章规格**（不得输出精简版、摘要或仅回应补充），在保持已确认设计不变的前提下，将补充内容融入对应章节并细化展开（如新增机制 → 展开其数据结构与算法）；不推翻已确认决策，除非用户明确要求改变',
  '- 每一轮都必须结合新一轮【网络参考】重新审视相关章节（检索到的新信息 → 修订对应章节）',
  '',
  '【方案自评判定】（输出九章规格后必须追加以下自评块，不改变九章框架；以【方案自评】开头，按一~四逐项输出）',
  '一、一致性核对：用户已明确需求逐条是否都有章节覆盖？缺项列出。',
  '二、完整性核对：九章是否全部展开？缺章列出。',
  '三、可实施性核对：是否存在未标注「默认」的设计空白？列出。**并做验收追溯（Chain-of-Verification 验证链）：第九节每条验收项都能从第二/三/四章约束找到对应，且第二/三/四章每条硬约束都有验收项覆盖——缺对应列出。**',
  '四、判定结论：【保留】（全部通过，可开工）或【调整】（列出差距项 + 调整方向，并在「调整建议」小节给出 ≤5 条修订要点）。',
  '锚点规则（防自嗨）：任一「用户明确需求未覆盖」或「九章缺章」→ 必须判【调整】。',
  '【自评段豁免条款】：自评段不受「不加解释/严禁回显」红线约束——允许概括引用用户需求点以核对覆盖，禁止逐字回显输入原文；其余正文仍严守红线。',
  '',
  '【用户示例】（用户认可的「口语约束 → 结构化规格」范式，模仿其提炼水平）',
  '示例（粗略想法 → 约束级规格的提炼风格）：',
  '输入：更新执行器是单独的一个功能，使用的独立端口，除了会影响本身链路执行的，检查一下尽量不要有其他限制执行器的使用',
  '输出（规格化约束提炼）：执行器是独立进程、独立端口，应该像一个独立的工具一样随时可用——除了执行自身链路（比如版本对齐、端口占用）必需的约束外，不该有额外限制（比如安装形态检查、版本门槛等卡住它）。',
  '提示：publish 九章中的「硬边界 / 技术约束 / 核心要求」应达到这种提炼水平——把用户的口语想法转成明确、具体、可执行、无歧义的需求陈述（抽象提升 + 条件具体化 + 约束强化 + 例证支撑）。',
].join('\n');

const SYSTEM_INCREMENT_PUBLISH_PROMPT = [
  '你是一名资深项目/游戏开发规划专家，精通需求工程（IEEE 29148 需求规格方法）与游戏设计文档（GDD）方法论。用户给出的是一句粗略想法或已有内容（可能是多轮补充后的现状），你的任务是在**保留原文全部已确认信息**的基础上，先理解用户实际想做什么任务，再对缺失的「大逻辑与信息」做**保守增量补充**——使规格可执行、不会让执行模型误判，但不补细节、不盲目扩大任务范围。',
  '',
  '【与默认模板的分工（先明确本模板定位）】',
  '- 默认模板：把一句粗略想法生成完整九章规格（未明确处给出合理默认并标注）',
  '- 本模板（增量模板）：在已有内容基础上，理解其实际想做的任务，只保守补充「该任务没说的、缺失会让执行模型误判」的大逻辑与信息——范围边界、交付形态、核心机制、验收维度；不补细节数值、不引入原文未提的新方向',
  '',
  '【增量补充规则】',
  '1. **以原内容为完整基础**：保留原有全部已确认设计（目标、范围、技术选型、数值、架构、交互等），不得推翻、替换或重写',
  '2. **任务意图判断**：先明确用户实际想做什么任务（游戏类型/软件类型/交付形态/目标用户），再判断执行此任务必须知道、但原内容没说的「大逻辑与信息」：',
  '   - 交付形态：单文件 / 库 / 服务 / 原型（未指定 → 默认合理形态）',
  '   - 范围边界：明确不做什么（未指定 → 给出合理默认边界）',
  '   - 核心机制：玩法/功能核心循环（未指定 → 给出合理默认并标注）',
  '   - 验收维度：可测试的验收方向（与第二/三/四章约束对应）',
  '3. **逐章检查缺口**：按下方九章结构对照——缺失或含糊的「大逻辑/信息」补齐；**不补细节**（具体数值、具体方案名、具体清单数量）；数值只给量级（如 20×20×10）不必精确',
  '4. **自洽优先**：补充内容不得与原设计冲突，不引入未确认的新方向；未明确处给出合理默认并标注「默认」',
  '5. **来源追溯（防幻觉）**：每条补充标注来源——「已确认（来自原内容）」或「默认（合理推断）」；无原内容依据又无法合理推断的，宁可不补',
  '6. **保留核对（输出前）**：逐条核对原内容每条已确认设计（目标、范围、技术选型、数值、架构、交互）都在九章中有对应且未被改变；漏改/被改 = 失败，回到对应章节修正',
  '7. **需求表述纪律**：每条要求遵循单一陈述、义务用语（必须/不得/禁止）、可测试阈值（给量级）；不用无阈值的模糊形容词（与默认模板同款纪律）',
  '8. **多轮扩充语义**：每一轮都必须输出**完整的九章规格**（在原文基础上），保持已确认设计、融入本次补充并细化',
  '',
  '【输出结构】（严格按以下九章，用用户主体语言输出）',
  '一、目标概述与角色设定：保留原文目标，以「你是一个顶级的 XX 专家 / 技术美术（TA）」式角色设定开场（角色依据【场景判定】自动适配），随后给出：一句话目标 + 核心亮点/想实现的「感觉或反差」+ 核心体验闭环',
  '二、需求范围与明确边界：明确「做什么 / 交付形态 / 明确不做什么」；用「绝对/必须/不得/禁止」级措辞写清硬边界',
  '三、技术约束与架构：技术栈、依赖引入方式、架构方案（渲染管线、合批策略、后处理管线、材质体系、数据模型与模块划分）、性能与兼容性约束——架构层写实具体到方案名级别，数值给量级',
  '四、核心功能要求：分维度逐条「必须」级要求（机制/视觉/材质/光照/音效/交互），每条附【设计意图】说明「为什么这样做」；**每条要求遵循【增量补充规则】第 7 条需求表述纪律**',
  '五、场景与内容设计：具体内容清单（场景规模量级、物体/关卡/功能清单、资源策略、代码生成策略）',
  '六、交互与界面：操作方式、初始视角、界面布局、反馈动效',
  '七、扩展方向（带边界）：可选扩展方向逐一给出，并强制声明「扩展必须沿用既有体系/尺度/风格，不破坏闭合交付」',
  '八、交付与限制：交付物清单（完整闭合、可直接运行）；禁止 TODO、占位实现或要求用户补齐核心算法；代码注释语言；输出格式要求',
  '九、交付验收清单：逐条可勾选的验收项，对应第二/三/四章约束（可测试、不写空话）',
  '',
  '【场景适配】',
  '- 系统按用户输入自动判定场景，并注入「【场景判定】本次场景判定：game/software/generic」行；未注入判定行时按默认九章输出，不追加场景侧重',
  '- 【场景判定】为 game 时：三、技术约束与架构侧重渲染/表现方案；五、场景与内容设计含玩法循环、数值与经济量级',
  '- 【场景判定】为 software 时：三、技术约束与架构侧重系统架构（模块划分、数据流、接口）；五、场景与内容设计含功能清单与数据模型',
  '',
  '【方案自评判定】（输出九章规格后必须追加以下自评块，不改变九章框架；以【方案自评】开头，按一~四逐项输出）',
  '一、一致性核对：用户已明确需求逐条是否都有章节覆盖？缺项列出。',
  '二、完整性核对：九章是否全部展开？缺章列出。',
  '三、保守性核对：补充内容是否都属于「缺失会导致执行模型误判的大逻辑/信息」？是否存在补细节、扩范围、引入未确认新方向的条目？列出。',
  '四、判定结论：【保留】（全部通过，可交付）或【调整】（列出差距项 + 调整方向，并在「调整建议」小节给出 ≤5 条修订要点）。',
  '锚点规则（防自嗨）：任一「用户明确需求未覆盖」「九章缺章」或「补充超出保守范围」→ 必须判【调整】。',
  '【自评段豁免条款】：自评段不受「不加解释/严禁回显」红线约束——允许概括引用用户需求点以核对覆盖，禁止逐字回显输入原文；其余正文仍严守红线。',
  '',
  '【保守增量判别示例】（示范「补什么 / 不补什么」，防止把增量做成反问或细节堆砌）',
  '输入（已有内容草稿）：做一个 2D 纸牌游戏，要支持拖拽出牌。已完成：单人接龙模式、基础 UI。希望补充：让界面更美观，加入音效。',
  '**增量应当补（大逻辑/信息）：**',
  '- 交付形态：默认给出合理形态并标注（如「默认沿用原技术栈；原技术栈未明确 → 默认合理默认」）',
  '- 范围边界：明确不做什么（如「禁止更改核心规则、禁止移除已有拖拽逻辑、除非已明确不自行扩展新模式」）',
  '- 验收维度：可测方向（如「界面美化必须覆盖全部已有界面」「音效必须覆盖核心交互事件」）',
  '**增量不应当补（细节 / 扩范围）：**',
  '- 具体数值（配色色值、字体大小、音频格式、具体帧率指标）',
  '- 具体方案名/引擎细节（若原文未提）',
  '- 原文未提的新功能（如计分系统、联机模式、关卡系统）',
  '**口语约束提炼范式（用户认可的示例）：**「让界面更美观」→「界面美化必须覆盖所有已有界面，风格统一，视觉反馈克制不干扰操作」；「加入音效」→「音效系统必须覆盖全部核心交互事件，音量可调，不依赖外部运行时下载」——把口语想法提炼成明确、具体、可执行、无歧义的需求陈述。',
  '',
  '【硬性约束】',
  '【稳定性与增量平衡】',
  '- 重述与保留部分：高度稳定（确定性任务，同一输入重复优化结果保持一致，不无意义改写原文已明确的措辞与结构）。',
  '- 仅「保守增量补充」部分是生成性的：补充内容在合理范围内可适度变化（避免每次补出雷同措辞），但必须严守「保守增量规则」红线——补得少比补得多好，拿不准宁可不补。',
  '',
  '- 不反问用户：未明确处给出合理默认并标注「默认」',
  '- 严禁回显、引用或复述输入原文（包括已有规格内容），直接输出补充完善后的完整九章规格',
  '- 顺序与公式绝对精确：乘算倍增非加算',
  '- 语言匹配：以用户主体语言输出，保留原文中的术语与专有名词',
  '- 若存在【相关会话参考】/【项目文档参考】/【参考上下文】，将其中与当前任务直接相关的明确需求、约束、默认值吸收进对应章节，但不得逐字照抄',
].join('\n');

const RELEVANCE_PROMPT = [
  '你是一个会话关联性判定器。判断给定的「历史会话片段」与「当前用户输入」是否相关。',
  '',
  '【关联的定义】',
  '历史片段与当前输入涉及相同或直接相关的主题、任务、对象、技术或上下文，能够为优化当前输入提供有效参考——例如：同一项目的讨论、同一提示词的迭代过程、与当前输入直接相关的需求说明或技术背景。',
  '',
  '【不关联的定义】',
  '历史片段与当前输入主题无关——例如：无关的闲聊、其他任务的讨论、与当前输入没有任何共同点的内容。',
  '',
  '【判定规则】',
  '1. 只依据历史片段与当前输入的实际内容判断，不臆测不存在的信息。',
  '2. 历史片段为空或仅剩截断残片时，判定为不关联。',
  '3. 宁可漏判（不关联），不可错判（把无关内容当作参考）。',
  '',
  '【输出格式】',
  '只输出一个 JSON 对象，不要输出任何其他内容：',
  '{"related": true 或 false, "reason": "一句话中文理由（不超过 30 字）"}',
  '',
  '【输入】',
  '历史会话片段：',
  '"""',
  '{history}',
  '"""',
  '',
  '当前用户输入：',
  '"""',
  '{current}',
  '"""',
].join('\n');

const REFERENCE_GUIDE = [
  '# 参考上下文使用规则（L2 条件注入：仅当本次输入附带参考/会话/检索上下文时适用）',
  '',
  '1. 参考上下文仅用于补充理解任务背景与已确认信息，将参考中明确的需求、约束、默认值、已确认决策**吸收进优化后的提示词**，使其更完整、可执行；但禁止逐字复述、引用或回显参考原文。',
  '2. 参考与原文冲突时，以原文为准；参考未明确、无法推断的细节不得臆造。',
  '3. 参考仅用于补充和明确化，不得扩大、缩小或颠倒原文范围。',
  '4. **参考来源分级（用法不同）**：① 优化历史（记忆链）= 上一轮已确认的优化结果，作为**延续优化依据**；② 任务背景（历史会话）= 优化本次草稿是为继续执行任务，会话提供任务背景与已确认约定，用于**理解草稿意图**；③ 项目事实（文档/代码）= 以文档与代码为准，不臆造。',
].join('\n');

const DEV_INTENT_PROMPT = [
  '你是一个提示词意图判定器。判断给定的「当前用户输入（提示词）」是否表达**项目开发意向**。',
  '',
  '【项目开发意向的定义】',
  '输入表达了对某个项目/软件/游戏/系统进行开发、实现、重构、修改、优化、调试、扩展、集成、部署等意图——例如：',
  '- 「帮我开发一个用户管理系统」',
  '- 「优化一下这个登录功能的实现」',
  '- 「重构这段代码」',
  '- 「给我的项目加一个功能」',
  '',
  '【非项目开发意向的定义】',
  '输入不涉及具体项目开发——例如：',
  '- 纯粹的写作/翻译/文案优化',
  '- 通用知识问答',
  '- 与代码/项目无关的日常请求',
  '',
  '【判定规则】',
  '1. 输入明确指向项目/代码/系统开发相关的任务 → true。',
  '2. 输入含糊或无法确定是否涉及项目开发 → false（宁停勿滥）。',
  '3. 只依据输入内容本身与给定的会话参考判断，不臆测。',
  '',
  '【输出格式】',
  '只输出一个 JSON 对象，不要输出任何其他内容：',
  '{"isDevIntent": true 或 false, "reason": "一句话中文理由（不超过 30 字）"}',
  '',
  '【输入】',
  '当前用户输入：',
  '"""',
  '{prompt}',
  '"""',
  '',
  '相关会话参考（可能为空）：',
  '"""',
  '{session}',
  '"""',
].join('\n');

const DOC_ANALYSIS_PROMPT = [
  '你是一个项目文档分析器。根据给定的「候选 .md 文档清单与内容摘要」、「当前用户输入（提示词）」与「相关会话参考」，执行两项任务：',
  '',
  '【任务一：检索相关参考信息】',
  '从候选 .md 文档中挑出与提示词**相关**的文档（主题/内容与提示词涉及的项目、功能、技术直接相关），并为每个相关文档提取一段要点摘要（≤200 字符）。无关文档不选。',
  '',
  '【任务二：分析项目地图结构】',
  '判断这些相关 .md 文档（以及候选清单整体）中，是否存在**项目地图或类似的可快速定位代码文档位置的结构**——例如：',
  '- README 中的目录树 / 模块结构说明',
  '- docs/map、INDEX、文档索引、导航文件',
  '- 文档中列出的源码目录结构 / 模块路径指引',
  '',
  '【输出格式】',
  '只输出一个 JSON 对象，不要输出任何其他内容：',
  '{',
  '  "relatedDocs": [{"path": "文档路径", "excerpt": "要点摘要（≤200字）"}],',
  '  "hasProjectMap": true 或 false,',
  '  "codePaths": ["文档中明确指向的代码目录/路径（如 src/、packages/core/，无则空数组）"],',
  '  "reason": "一句话中文理由（不超过 40 字）"',
  '}',
  '',
  '【输入】',
  '候选 .md 文档清单与内容摘要：',
  '"""',
  '{docs}',
  '"""',
  '',
  '当前用户输入：',
  '"""',
  '{prompt}',
  '"""',
  '',
  '相关会话参考（可能为空）：',
  '"""',
  '{session}',
  '"""',
].join('\n');

const WEBSEARCH_PLAN_PROMPT = [
  '你负责为一个「项目/游戏开发规格生成」流程规划网络检索主题。给定用户的最新想法、此前各轮已检索主题与要点摘要、场景判定，输出 2~3 个**差异化**检索主题（避开已检索主题），供后续逐个搜索并注入【网络参考】。',
  '',
  '【输入】',
  '- 用户最新想法：{text}',
  '- 已获取网络要点（此前轮次检索结果摘要，含已检索主题清单）：{memo}',
  '- 场景判定：{scenario}',
  '',
  '【输出要求】',
  '- 输出 2~3 个检索主题，覆盖用户想法中最需要业界参考的方面（技术方案/同类实现/素材与资源来源/市场参考等）',
  '- 与已检索主题不重复；若已有要点已覆盖充分，可只输出 1 个最关键缺口主题',
  '- 每个主题给出：query（可直接用于搜索的中文短语，≤20 字）+ note（一句检索目的）',
  '- 仅输出 JSON，格式：{"topics":[{"query":"…","note":"…"}]}',
  '- 除该 JSON 外不得输出任何内容（无解释、无前言、无代码块标记）',
].join('\n');

const TASK_ANALYSIS_PROMPT = [
  '你是一个会话任务分析器。根据给定的会话对话历史，输出当前任务的执行进度。',
  '只输出 JSON（不要任何其他文字），格式：',
  '{"task":"任务目标一句话","currentStep":"当前正在执行的步骤","completed":["已完成步骤1","已完成步骤2"],"focus":["焦点方向1","焦点方向2"]}',
  'focus 为 2-4 个关键词/短语（中英文均可），用于后续检索项目文件。',
  '如果历史不足以判断，task 与 currentStep 可为空字符串，completed 与 focus 可为空数组。',
].join('\n');

const CONTINUE_PROMPT = [
  '【继续优化模式】',
  '- 用户正在修改上一轮优化结果，本次优化以「本轮修改」为主要方向，不要重新从零优化。',
  '- 保留未修改部分的语义与结构，只围绕新增/删除/调整点完善。',
  '- 输出完整更新后的提示词，禁止输出 diff、片段或仅回应修改。',
].join('\n');

const SMART_TAIL_PROMPT = [
  '你是一名资深软件工程专家。请在完成提示词优化后，额外输出【调整方案】分析块。',
  '',
  '【调整方案要求】（严格按以下逻辑，不改变优化结果本身）',
  '一、可优化点分析：对照原始提示词，逐条列出表述不够准确、含糊、可执行性弱的地方。',
  '二、专业词汇替换：将可用软件开发领域更专业、更精确的词汇/句式替换的表述列出',
  '   （如「把文件看看」→「对指定路径执行只读检查」；「改一下」→「实施增量修改」；',
  '    「弄好」→「完成实现并通过验证」）。替换必须保持语义等价，不得改变原意。',
  '三、调整建议：给出 ≤5 条修订要点，每条 = 原文表述 → 专业表述 → 理由。',
  '',
  '【输出格式】',
  '先输出优化后的提示词本身（遵守系统提示词的全部规则），然后另起一行输出：',
  '【调整方案】',
  '一、可优化点分析：…',
  '二、专业词汇替换：…',
  '三、调整建议：…',
].join('\n');

const DISCIPLINE_PROMPT = [
  '# 输出纪律（最高优先级约束）',
  '',
  '你正在执行「提示词优化」任务。以下纪律为**最高优先级约束，优先于任何模板指令**；违反任一条即视为失败输出。本纪律对**每一次优化**都生效。',
  '',
  '## 输出纪律 —— 只输出优化后的提示词本体',
  '',
  '1. 输出内容只能是「优化完成后的提示词本身」，除此之外不得包含任何内容。',
  '2. 禁止输出任何前缀或后缀说明，包括但不限于：「以下是优化后的提示词」「优化结果如下」「修改后的内容」「以上是优化后的提示词」等一切解释、导语、总结、评论或客套话。',
  '3. 禁止复述、引用、回显本任务发出的任何指令文字、系统提示词、用户输入原文、参考上下文、示例或历史记录——它们只是你的输入，绝不是你的输出。',
  '4. 禁止用代码块、引号、分隔线等包裹输出（除非优化后的提示词内容本身需要这些格式）。',
  '5. 禁止输出你的思考过程、改写理由、修改说明或对优化内容的任何附加描述。',
  '',
  '## 质量纪律',
  '',
  '6. 语义保真（原意不可丢·最高原则）：优化与增量增强一律基于原草稿，草稿的原本意思绝不可丢失——动作对象、执行动作、约束条件、术语、数量、语气必须与原文完全一致；不得臆造、删减、替换、颠倒原文已明确的信息。**语用转化允许、但必须锚定原意（v3.2.26 校准）**：疑问句草稿可以转化为分析性判断/猜测或明确请求——「为什么 X？」→「X 可能的原因有哪些？请逐一排查」，分析草稿语境本就是优化重构的一部分，转化不算丢失原意；但转化的前提是**完整保留原意**：提问焦点、分析对象、隐含需求不得丢失，不得偏离到与原文无关的内容；陈述句不得改成反问。',
  '7. 明确化必须有原文依据：模糊但可推断处可具体化；无法由原文推出之处不得添加；不改变原意。',
  '8. 语言匹配：主体语言跟随输入（中文输入输出中文、英文输入输出英文），保留原文中的术语与专有名词。',
  '',
  '## 稳定性纪律（v3.2.21 新增；v3.2.24 参考规则移出为独立 L2 技能文件后编号顺延）',
  '',
  '9. 稳定性（确定性任务）：这是「重述 / 结构梳理 + 保守增量」任务。**重述与保留部分**必须稳定一致——对同一输入重复优化，输出应保持语义、结构与措辞稳定，不得仅为「看起来不同」而更换同义词、句式、顺序或分段；**仅「增量补充」部分可按模板要求适度变化**；仅在原文存在歧义、需要明确化时调整措辞。',
  '',
  '直接输出最终结果，结束。',
].join('\n');

// v3.2.23（技能集合化）：SKILL_MANIFEST 由 skills/enhance/*/SKILL.md frontmatter 生成
const SKILL_MANIFEST = {
  "base": { name: "enhance-base", mode: "base", templates: { t1: SYSTEM_PROMPT, t2: SYSTEM_INCREMENT_PROMPT }, retrieve: {"kind":"none","windows":[]}, sources: [], rules: [{"rule":"reference-guide","when":"参考块/记忆命中"}] },
  "lite": { name: "enhance-lite", mode: "lite", templates: { t1: SYSTEM_LITE_PROMPT, t2: SYSTEM_INCREMENT_LITE_PROMPT }, retrieve: {"kind":"rounds","windows":[[1,1]]}, sources: ["session"], rules: [{"rule":"reference-guide","when":"参考块/记忆命中"}] },
  "standard": { name: "enhance-standard", mode: "standard", templates: { t1: SYSTEM_STANDARD_PROMPT, t2: SYSTEM_INCREMENT_STANDARD_PROMPT }, retrieve: {"kind":"rounds","windows":[[1,2],[3,5],[6,10]]}, sources: ["session"], rules: [{"rule":"reference-guide","when":"参考块/记忆命中"}] },
  "smart": { name: "enhance-smart", mode: "smart", templates: { t1: SYSTEM_SMART_PROMPT, t2: SYSTEM_INCREMENT_SMART_PROMPT }, retrieve: {"kind":"rounds","windows":[[1,1],[2,3]]}, sources: ["session","workspace"], rules: [{"rule":"reference-guide","when":"参考块/记忆命中"},{"rule":"smart-tail","when":"smart+开发意向+代码阶段"}] },
  "publish": { name: "enhance-publish", mode: "publish", templates: { t1: SYSTEM_PUBLISH_PROMPT, t2: SYSTEM_INCREMENT_PUBLISH_PROMPT }, retrieve: {"kind":"v2","windows":[]}, sources: ["web"], rules: [{"rule":"reference-guide","when":"参考块/记忆命中"},{"rule":"scenario-judge","when":"publish+场景判定非通用"}] },
};
const SKILL_RETRIEVE_BUDGETS = [{"budget":0,"roundsChars":0,"smartDocs":0,"smartDepth":0,"smartChars":0,"smartCodeChars":0},{"budget":2000,"roundsChars":1200,"smartDocs":2,"smartDepth":2,"smartChars":1500,"smartCodeChars":1200},{"budget":4000,"roundsChars":2400,"smartDocs":3,"smartDepth":2,"smartChars":3000,"smartCodeChars":2400},{"budget":8000,"roundsChars":4800,"smartDocs":5,"smartDepth":3,"smartChars":5000,"smartCodeChars":4800},{"budget":16000,"roundsChars":9600,"smartDocs":8,"smartDepth":4,"smartChars":8000,"smartCodeChars":8000},{"budget":32000,"roundsChars":16000,"smartDocs":12,"smartDepth":5,"smartChars":12000,"smartCodeChars":12000}];
// ==PROMPTS-END==

// ==PURE-BEGIN==  (unit-testable pure functions; keep free of ctx/harness/pending/module-state)

// ================= v2.2 模式体系 · 常量层与行为表（表驱动，单测切片求值） =================
// 方案「提示词优化方案.md」§4/§6：4 模式 + 记忆独立开关（memoryOn，扩散到所有模式）；
// 记忆模式已删除——记忆块作为叠加模块注入（shouldInjectMemory）。
// v3.1.7（用户需求·上下文预算无限制）：新增 16000 档（最大扫描/注入预算）
// v3.1.8（用户需求·预算真正生效 + 每模式独立档位）：扩展 32000 档；BUDGET_OPTIONS 为全局候选池，
//   每模式可选档位见 MODE_BUDGET_OPTIONS（client 下拉按此渲染、validateConfig 按此校验），
//   默认值见 MODE_BUDGET_DEFAULT（修复旧行为：base/lite 默认被 DEFAULT_BUDGET=4000 覆盖）。
const BUDGET_OPTIONS = [0, 2000, 4000, 8000, 16000, 32000];
// 每模式可用档位（子集；base 无上下文检索 → 仅 0）
const MODE_BUDGET_OPTIONS = {
  base: [0],
  lite: [0, 2000, 4000],
  standard: [0, 2000, 4000, 8000],
  smart: [0, 2000, 4000, 8000, 16000],
  publish: [0, 4000, 8000, 16000, 32000],
};
// 每模式默认预算（base 0 = 无上下文检索；lite 2000 = 默认启用最近 1 轮会话参考（最小检索档，
// 0 = 显式关闭）；standard/smart/publish 默认 4000 = 中等检索）
const MODE_BUDGET_DEFAULT = { base: 0, lite: 2000, standard: 4000, smart: 4000, publish: 4000 };
// v3.1.8（实测驱动·用户指令）：每模式默认 params（超时/Token/字符 = 实测标准档）。
// 2026-08-18 实测（4 模式 × 3 次，默认模板 T1、不带思考，短提示词）：
//   base avg 2.6s / lite 5.7s / standard 2.2s / smart 6.7s（最大 9.8s）；输出 156–336 字符（≈40–85 token）。
// 标准档 = 实测 × 余量（覆盖真实长输入与慢模型）+ 模板语义保真上限（简单 ≤800 字符、复杂可超出）；
// smart 管道含检索/判定/工作区调用 → 标准档更高。
const MODE_PARAMS_DEFAULT = {
  base: { timeoutMs: 30000, maxTokens: 2000, outputLimit: 8000 },
  lite: { timeoutMs: 30000, maxTokens: 2000, outputLimit: 8000 },
  standard: { timeoutMs: 30000, maxTokens: 2000, outputLimit: 8000 },
  smart: { timeoutMs: 60000, maxTokens: 4000, outputLimit: 16000 },
  publish: { timeoutMs: 60000, maxTokens: 4000, outputLimit: 16000 }, // 2026-08-18 实测：默认模板 avg 44.3s max 51.1s / 输出 4348–8593 字符（host 硬覆盖不设限，此值仅结构默认/UI 语义）
};
// 预算 → 扫描上限查表（仅 scanLimit:'by-budget' 模式消费；maxFiles = 注入 Top-N 上限）
const BUDGET_WORKSPACE_TABLE = [
  { budget: 0, maxFiles: 2, depth: 1 },
  { budget: 2000, maxFiles: 2, depth: 1 },
  { budget: 4000, maxFiles: 3, depth: 2 },
  { budget: 8000, maxFiles: 6, depth: 3 },
  { budget: 16000, maxFiles: 10, depth: 4 },
  { budget: 32000, maxFiles: 14, depth: 5 },
];
// 阶段 A/B/C 与默认预算；budgetDefault 仅默认值，运行时以 config.budgetChars 为准
const MODE_TABLE = {
  base: { phaseA: 'none', phaseB: 'none', phaseC: 'none', budgetDefault: 0, scanLimit: 'fixed' },
  lite: { phaseA: 'rule', phaseB: 'none', phaseC: 'none', budgetDefault: 0, scanLimit: 'fixed' },
  standard: { phaseA: 'rule', phaseB: 'file+event', phaseC: 'inject', budgetDefault: 4000, scanLimit: 'fixed' },
  smart: { phaseA: 'llm', phaseB: 'file+event', phaseC: 'inject', budgetDefault: 4000, scanLimit: 'by-budget' },
  // v2.7.0（一键发布）：项目/游戏开发规格生成——完整检索（任务理解+工作区/会话）
  // + 网络检索（依据草稿与改动方向检索同类项目结构参考）+ 专用九章规格 system
  publish: { phaseA: 'llm', phaseB: 'file+event', phaseC: 'inject', budgetDefault: 4000, scanLimit: 'by-budget' },
};
const MODE_KEYS = Object.keys(MODE_TABLE);
const DEFAULT_MODE = 'base';
const DEFAULT_BUDGET = 4000;
// 记忆开关默认（§6.4：缺省 false，行为零变化）
const DEFAULT_MEMORY = false;
// 模式 → 扫描上限（fixed 模式固定值）
const FIXED_SCAN_LIMIT = { maxFiles: 3, depth: 2 };
// V2 阶段超时/上限/截断
const V2_PROGRESS_TIMEOUT_MS = 15000;
const V2_PROGRESS_MAX_TOKENS = 400;
const V2_PROGRESS_OUTPUT_LIMIT = 2000;
const V2_HISTORY_MAX_CHARS = 8000;
const V2_HISTORY_LIMIT = 12;
const V2_MSG_SEQ_SCAN = 16;
// v3.1.3（用户需求·仅结论参考 / 轮次覆盖修正）：结论提取按轮数扫描上限（1 = 最近一轮，
// 对齐 standard 最深窗口 [6,10]；旧 16 事件上限在会话 >8 轮时令 [6,10] 静默滑向旧轮）
const V2_ROUNDS_SCAN_MAX = 10;
// v3.1.3（看门狗 + 延迟连通性预检）：模型响应看门狗阈值 / 单次连通探测超时 / 探测结果缓存 TTL
// v3.3.3（2026-08-27 用户实测·增强易断链修复）：实测链上模型 TTFT——
// 链1 completions/deepseek-v4-flash 2.8s、链2 responses-proxy/muse-spark 9.3s；
// 旧值 5s 看门狗 + 3s 探测会把「慢但健康」模型误判不可达并冻结 30s（增强直调
// llm.stream 无宿主 retryPolicy(always) 兜底）——本组放宽对齐实测；
// 探测失败缓存缩至 5s 免整链冻僵。
const WATCHDOG_TIMEOUT_MS = 15000;
const PROBE_TIMEOUT_MS = 12000;
const PROBE_CACHE_TTL_MS = 5000;
// 2026-08-18（用户需求）：会话历史显式内容提取上限 1200 → 2400（覆盖长回复/表格，避免截断显式内容）
const V2_MSG_TEXT_MAX = 2400;
const V2_WORKSPACE_TIMEOUT_MS = 2000;
const SCAN_FILE_LIST_MAX = 2000;
// v2.7.0（检索质量修复）：名称匹配 0 命中时的内容兜底扫描文件数上限
const CONTENT_FALLBACK_SCAN = 5;
// v2.7.0（一键发布 · 网络检索）：超时/条数/注入预算
const WEB_SEARCH_TIMEOUT_MS = 10000;
const WEB_SEARCH_MAX_RESULTS = 3;
const WEB_REF_MAX = 1600;
// v3.0p（publish 多步检索）：检索主题规划调用预算 + 跨轮要点记忆上限
const WEB_PLAN_TIMEOUT_MS = 10000;
const WEB_PLAN_MAX_TOKENS = 400;
const WEB_PLAN_OUTPUT_LIMIT = 800;
const WEB_MEMO_MAX = 600;
const WEB_MEMO_TOPICS_MAX = 12;
// v3.0p 审查（超时/耗时匹配）：会话事件/历史提取超时保护
const V2_SEARCH_EVENTS_TIMEOUT_MS = 10000;
const KEYWORD_LIMIT = 8;
const SNIPPET_BUDGET = 800;
const CONTEXT_PROGRESS_MAX = 800;
const CONTEXT_EVENT_DIVISOR = 4;
const CONTEXT_FILE_COUNT = 3;
// v2.6.1（记忆链）：发送前多轮迭代记忆——rounds 链（≤MEMORY_ROUNDS_MAX 轮）经
// buildChatMessages 作为真多轮消息注入（user/assistant 交替），每轮输入/输出按预算
// 等分截断；本轮修改摘要（computeEditDelta → buildMemoryDeltaHint）附加到最终 user 消息；
// buildMemoryChainBlock 为文本块构建（回退/日志口径），两者共用预算规则。
const MEMORY_ROUNDS_MAX = 4;            // 记忆链最多保留轮数（client 写入侧同样截断）
const MEMORY_CHAIN_BUDGET_MAX = 2400;   // 记忆链总预算上限（记忆优先占用，模式块用剩余）
const MEMORY_DELTA_MAX = 300;           // 本轮修改摘要字符上限
const MEMORY_DELTA_LINES_MAX = 6;       // 修改摘要增/删各最多展示行数
const MEMORY_CHAIN_HEADER = '【记忆】发送前的多轮优化记录，仅供参考，禁止回显';
const MEMORY_ROUND_TEMPLATE = '【第{n}轮】原始输入：{prevInput}\n优化输出：{prevOutput}';
// v2.3（§7.3）：优化阶段常量——enhance 请求生命周期 stage 标记（progress RPC 读取）
const STAGE_PREPARE = 'prepare';
const STAGE_HISTORY = 'history';
const STAGE_ANALYZE = 'analyze';
const STAGE_FILES = 'files';
const STAGE_EVENTS = 'events';
const STAGE_CONTEXT = 'context';
const STAGE_LLM = 'llm';
const STAGE_DONE = 'done';
const STAGE_SEQUENCE = [STAGE_PREPARE, STAGE_HISTORY, STAGE_ANALYZE, STAGE_FILES, STAGE_EVENTS, STAGE_CONTEXT, STAGE_LLM, STAGE_DONE];
// stage → 文案映射（单测 U24 断言键一致；client 侧 i18n 同键名独立维护）
const STAGE_LABELS = {
  prepare: { zh: '准备中…', en: 'Preparing…' },
  history: { zh: '读取会话…', en: 'Reading history…' },
  analyze: { zh: '分析任务…', en: 'Analyzing task…' },
  files: { zh: '检索文件…', en: 'Searching files…' },
  events: { zh: '检索会话…', en: 'Searching events…' },
  context: { zh: '组装上下文…', en: 'Assembling context…' },
  llm: { zh: 'LLM 优化中…', en: 'Optimizing…' },
  done: { zh: '✓', en: '✓' },
};

// 模式解析（表驱动）：显式 mode 白名单（4 模式）；旧 engine/context.mode 迁移；缺省/非法 → base
// v2.2：记忆模式已删除——'memory' 值由 validateConfig 迁移为 lite + memory:true（此处落到 base 兜底）
function parseMode(mode, engine, legacyMode) {
  if (typeof mode === 'string' && MODE_KEYS.includes(mode)) return mode;
  if (engine === 'v2') {
    if (legacyMode === 'basic') return 'standard';
    if (legacyMode === 'smart') return 'smart';
  }
  return DEFAULT_MODE; // engine v1 / 缺省 / 非法 → base（行为零变化）
}

// 记忆开关解析（v2.2 §6.4）：优先级 memory = (mode==='memory') || autoMemory===true；缺省 false
function parseMemory(mode, autoMemory) {
  return mode === 'memory' || autoMemory === true;
}

// 记忆注入判定（v2.2 §6.5）：开关开 + 有记忆 + 预算>0 → 注入记忆块（叠加模块，所有模式适用）
function shouldInjectMemory(memoryOn, hasMemory, budgetChars) {
  return memoryOn === true && hasMemory === true && typeof budgetChars === 'number' && budgetChars > 0;
}

// 预算白名单校验（缺省 → 该模式默认值）：v3.1.8 起按模式档位（MODE_BUDGET_OPTIONS）校验——
// 非法/越界值回退模式默认（MODE_BUDGET_DEFAULT），修复旧行为（base/lite 被全局 4000 覆盖）。
function parseBudgetChars(value, mode) {
  const opts = MODE_BUDGET_OPTIONS[mode] || MODE_BUDGET_OPTIONS[DEFAULT_MODE];
  if (opts.includes(value)) return value;
  const dflt = MODE_BUDGET_DEFAULT[mode];
  return typeof dflt === 'number' ? dflt : DEFAULT_BUDGET;
}

// 扫描上限解析：by-budget 查联动表（取不超过预算的最大档），fixed 用固定值
function resolveScanLimit(mode, budgetChars) {
  const row = MODE_TABLE[mode] || MODE_TABLE[DEFAULT_MODE];
  if (row.scanLimit === 'by-budget') {
    let pick = BUDGET_WORKSPACE_TABLE[0];
    for (const entry of BUDGET_WORKSPACE_TABLE) {
      if (entry.budget <= budgetChars) pick = entry;
    }
    return pick;
  }
  return FIXED_SCAN_LIMIT;
}

// v3.1.8（用户需求·预算真正生效）：rounds 模式（lite/standard/smart）预算 → 检索注入参数——
// roundsChars = 会话窗口参考注入字符上限（0 = 不注入）；smart 工作区规模（文档数/深度/文档与代码内容上限）。
// 与 BUDGET_WORKSPACE_TABLE 同档位体系（publish 走 v2 管道消费该表）。
const BUDGET_RETRIEVE_TABLE = [
  { budget: 0, roundsChars: 0, smartDocs: 0, smartDepth: 0, smartChars: 0, smartCodeChars: 0 },
  { budget: 2000, roundsChars: 1200, smartDocs: 2, smartDepth: 2, smartChars: 1500, smartCodeChars: 1200 },
  { budget: 4000, roundsChars: 2400, smartDocs: 3, smartDepth: 2, smartChars: 3000, smartCodeChars: 2400 },
  { budget: 8000, roundsChars: 4800, smartDocs: 5, smartDepth: 3, smartChars: 5000, smartCodeChars: 4800 },
  { budget: 16000, roundsChars: 9600, smartDocs: 8, smartDepth: 4, smartChars: 8000, smartCodeChars: 8000 },
  { budget: 32000, roundsChars: 16000, smartDocs: 12, smartDepth: 5, smartChars: 12000, smartCodeChars: 12000 },
];
// 预算 → rounds 检索参数（取不超过预算的最大档；budget 0 → 全 0 = 不注入；base 无检索）
function resolveRetrieveBudget(mode, budgetChars) {
  if (mode === 'base') return BUDGET_RETRIEVE_TABLE[0];
  let pick = BUDGET_RETRIEVE_TABLE[0];
  for (const e of BUDGET_RETRIEVE_TABLE) {
    if (e.budget <= budgetChars) pick = e;
  }
  return pick;
}

// ================= v2.6.1 记忆链 · 纯函数族（PURE，单测切片求值） =================

// 记忆链文本块构建（回退方案/日志口径；主路径为真多轮消息 buildChatMessages）：
// rounds 按时间序 [{input, output}]，最多取最近 MEMORY_ROUNDS_MAX 轮；总预算 =
// min(budget, MEMORY_CHAIN_BUDGET_MAX) 按轮等分，每轮输入 1/3、输出 2/3 截断
// （slice 字符级，不撕裂）；无轮次 / 预算 0 / 轮预算为 0 → 空串。
function buildMemoryChainBlock(rounds, budgetChars) {
  const budget = typeof budgetChars === 'number' && budgetChars > 0 ? budgetChars : 0;
  if (budget <= 0) return '';
  const list = Array.isArray(rounds) ? rounds.filter((r) => r && (r.input || r.output)) : [];
  if (list.length === 0) return '';
  const used = list.slice(-MEMORY_ROUNDS_MAX);
  const total = Math.min(budget, MEMORY_CHAIN_BUDGET_MAX);
  const perRound = Math.floor(total / used.length);
  if (perRound <= 0) return '';
  const inputBudget = Math.floor(perRound / 3);
  const outputBudget = perRound - inputBudget;
  const lines = [MEMORY_CHAIN_HEADER];
  used.forEach((r, i) => {
    const pi = String(r.input || '').slice(0, inputBudget);
    const po = String(r.output || '').slice(0, outputBudget);
    lines.push(MEMORY_ROUND_TEMPLATE.replace('{n}', String(i + 1)).replace('{prevInput}', pi).replace('{prevOutput}', po));
  });
  return lines.join('\n');
}

// 本轮修改摘要（行级 diff）：剥离公共前缀/后缀行后取中段差异，返回 { added, removed }
// （各 ≤ MEMORY_DELTA_LINES_MAX 行）。上一轮输出为空或与当前原文相同 → 空差异。
function computeEditDelta(prevOutput, text) {
  const prev = String(prevOutput || '');
  const cur = String(text || '');
  if (!prev || prev === cur) return { added: [], removed: [] };
  const prevLines = prev.split('\n');
  const curLines = cur.split('\n');
  let start = 0;
  while (start < prevLines.length && start < curLines.length && prevLines[start] === curLines[start]) start++;
  let endPrev = prevLines.length;
  let endCur = curLines.length;
  while (endPrev > start && endCur > start && prevLines[endPrev - 1] === curLines[endCur - 1]) { endPrev--; endCur--; }
  return {
    added: curLines.slice(start, endCur).slice(0, MEMORY_DELTA_LINES_MAX),
    removed: prevLines.slice(start, endPrev).slice(0, MEMORY_DELTA_LINES_MAX),
  };
}

// v2.8.0（一键发布 · 实测修正）：publish 多轮为「补充式」输入——行级 diff 的 removed 侧
// = 上一轮完整规格（用户未复述即全算删除，恰为章节标题），实测曾把「-删除：一、目标概述」
// 喂给模型导致幻觉「用户要求删除该章」。publish 的改动方向由 added 侧（用户本轮文本）承载，
// removed 侧清零（非 publish 模式原样返回，行为不变）。
function filterDeltaForPublish(delta, mode) {
  if (mode !== 'publish' || !delta || !Array.isArray(delta.added)) return delta;
  return { added: delta.added, removed: [] };
}

// 修改摘要格式化：`【本轮修改】相对上一轮优化结果：\n+新增：…\n-删除：…`；
// 单侧为空只列一侧；合计 ≤ MEMORY_DELTA_MAX 字符（字符级截断）；无差异 → 空串。
function buildMemoryDeltaHint(delta) {
  if (!delta || !delta.added || !delta.removed) return '';
  if (delta.added.length === 0 && delta.removed.length === 0) return '';
  const parts = [];
  if (delta.added.length > 0) parts.push('+新增：' + delta.added.join('；'));
  if (delta.removed.length > 0) parts.push('-删除：' + delta.removed.join('；'));
  let hint = '【本轮修改】相对上一轮优化结果：\n' + parts.join('\n');
  if (hint.length > MEMORY_DELTA_MAX) hint = hint.slice(0, MEMORY_DELTA_MAX);
  return hint;
}

// 记忆链 → 真多轮 messages（llm.stream 消息数组）：
// 历史轮次按 role 交替（user 输入 → assistant 输出，时间序），文本按预算等分截断
// （规则同 buildMemoryChainBlock）；finalText 为已组装的最终 user 消息文本（含 delta
// 提示 + 模式块 + 原文包裹）。rounds 空 / 预算 0 → 仅最终 user 消息（与旧单消息一致）。
// 返回 { messages, memChars }（memChars = 注入的历史文本总字符数，供日志）。
function buildChatMessages(rounds, finalText, idPrefix, budgetChars) {
  const prefix = typeof idPrefix === 'string' && idPrefix !== '' ? idPrefix : 'enhance';
  const messages = [];
  const list = Array.isArray(rounds) ? rounds.filter((r) => r && (r.input || r.output)) : [];
  const used = list.slice(-MEMORY_ROUNDS_MAX);
  let memChars = 0;
  if (used.length > 0 && typeof budgetChars === 'number' && budgetChars > 0) {
    const total = Math.min(budgetChars, MEMORY_CHAIN_BUDGET_MAX);
    const perRound = Math.floor(total / used.length);
    if (perRound > 0) {
      const inputBudget = Math.floor(perRound / 3);
      const outputBudget = perRound - inputBudget;
      used.forEach((r, i) => {
        const uText = String(r.input || '').slice(0, inputBudget);
        const aText = String(r.output || '').slice(0, outputBudget);
        memChars += uText.length + aText.length;
        messages.push({ id: prefix + '-m' + (i * 2), role: 'user', content: [{ type: 'text', text: uText }], source: { kind: 'user' } });
        messages.push({ id: prefix + '-m' + (i * 2 + 1), role: 'assistant', content: [{ type: 'text', text: aText }], source: { kind: 'assistant' } });
      });
    }
  }
  messages.push({ id: prefix + '-final', role: 'user', content: [{ type: 'text', text: String(finalText || '') }], source: { kind: 'user' } });
  return { messages, memChars };
}

function wrapUserText(text) {
  return '请优化以下提示词：\n\n"""\n' + text + '\n"""';
}

// v2.8.0（一键发布 · 实测修正）：publish 专用用户输入包装——中性框架（不沿用
// 优化模式的「请优化以下提示词」措辞，避免模型把输入误读为提示词优化任务）。
function wrapPublishText(text) {
  return '【用户输入】\n"""\n' + text + '\n"""';
}

function cleanOutput(raw, originalText) {
  let s = String(raw == null ? '' : raw).trim();
  // v3.2.18（用户反馈「优化结果带出发给模型的提示词」）：循环剥离直至稳定（≤3 轮）。
  // 模型把发给它的完整指令（「请优化以下提示词：\n\"\"\"\n<原文>\n\"\"\"」或 publish「【用户输入】…」）
  // 整段复述进输出时，旧逻辑只剥开头、残留原文在结果里。现按「完整回显」语义处理：
  //   有后续正文 → 取正文；仅回显无正文 → 取包装内原文（剥离包装返回原文，而非把指令原样带出）。
  const LEAD_OUTPUT_PREFIX = [
    '以下是优化后的提示词：', '以下是优化后的提示词:',
    '以下是优化后的内容：', '以下是优化后的内容:',
    '以下是优化后的版本：', '以下是优化后的版本:',
    '优化后的提示词如下：', '优化后的提示词如下:',
    '优化后的内容如下：', '优化后的内容如下:',
    '优化结果如下：', '优化结果如下:',
    '修改后的提示词：', '修改后的提示词:',
    '最终结果：', '最终结果:',
    '结果如下：', '结果如下:',
    '输出如下：', '输出如下:',
  ];
  const TAIL_NOTES = [
    '以上是优化后的提示词', '以上就是优化后的提示词', '以上为优化后的提示词',
    '以上是优化后的内容', '以上是修改后的提示词', '以上是优化结果',
  ];
  for (let round = 0; round < 3; round++) {
    const before = s;
    const echo = /^((?:请优化以下提示词[：:]\s*|优化以下提示词[：:]\s*|【用户输入】\s*\n?)\s*\"\"\"(?:\r?\n)?)([\s\S]*?)(?:\n\"\"\"\s*)([\s\S]*)$/.exec(s);
    if (echo) {
      const inner = (echo[2] || '').trim();
      const after = (echo[3] || '').trim();
      if (after !== '') s = after;
      else if (inner !== '') s = inner;
    }
            // v3.2.19-fix（用户实测：模型只回显「请优化以下提示词：」前缀、无闭合包装时残留）：
    // 孤立前缀剥离——输出以指令前缀开头即视为回显，剥掉前缀行；
    // 传 originalText（用户原文）时精确剥离「原文回显段」（结果更长才剥，结果=原文不误伤）。
    const lonePrefix = /^(?:请优化以下提示词[：:]\s*\n?|优化以下提示词[：:]\s*\n?|【用户输入】\s*\n?)/;
    if (lonePrefix.test(s)) {
      const stripped = s.replace(lonePrefix, '').trim();
      if (stripped !== '') s = stripped;
    }
    if (typeof originalText === 'string' && originalText.trim() !== '') {
      const ot = originalText.trim();
      if (s.startsWith(ot) && s.length > ot.length) {
        s = s.slice(ot.length).replace(/^[，。！？!?\s、]+/, '').trim();
      }
    }
        for (const pf of LEAD_OUTPUT_PREFIX) {
      if (s.startsWith(pf)) { s = s.slice(pf.length).trim(); break; }
    }
    for (const tn of TAIL_NOTES) {
      if (s.endsWith(tn)) { s = s.slice(0, -tn.length).trim(); break; }
    }
    if (s === before) break;
  }
  const pairs = [
    ['\"\"\"', '\"\"\"'],
    ['```', '```'],
    ['"', '"'],
    ["'", "'"],
    ['“', '”'],
    ['‘', '’'],
    ['「', '」'],
    ['『', '』'],
    ['（', '）'],
    ['(', ')'],
    ['【', '】'],
  ];
  for (let i = 0; i < pairs.length; i++) {
    const open = pairs[i][0];
    const close = pairs[i][1];
    if (s.length > open.length + close.length && s.startsWith(open) && s.endsWith(close)) {
      s = s.slice(open.length, s.length - close.length).trim();
      break;
    }
  }
  return s;
}
function friendlyMessage(failure) {
  const code = failure && failure.code ? failure.code : 'LLM_FAILED';
  switch (code) {
    case 'UNKNOWN_MODEL': return 'optimize model unavailable (not in catalog)';
    case 'NO_ADAPTER': return 'LLM provider not enabled';
    case 'INVALID_CREDENTIAL': return 'invalid or missing API key';
    case 'QUOTA': return 'model quota exceeded';
    case 'CONTEXT_WINDOW_EXCEEDED': return 'input exceeds context window';
    case 'EMPTY_RESPONSE': return 'model returned an empty response';
    case 'OUTPUT_TOO_LONG': return 'optimization exceeds length limit';
    case 'TIMEOUT': return 'request timed out, original text restored';
    case 'ABORTED': return 'request cancelled';
    case 'NO_MODEL': return 'no model configured, add models in settings';
    case 'ALL_MODELS_UNAVAILABLE': return 'all models unavailable, check model config and retry';
    default: return failure && failure.message ? failure.message : 'optimize failed';
  }
}

function validateConfig(raw) {
  const src = raw && typeof raw === 'object' ? raw : {};
  // v18：v2 结构（fallback/customModels/order/params/template）；兼容 v1 平铺字段
  // 2026-08-18（用户指令）：main 死配置字段删除（不维护老用户迁移）——host 只用 fallback 模型链
  const p = src.params && typeof src.params === 'object' ? src.params : src;
  const t = src.template && typeof src.template === 'object' ? src.template : src;
  const out = {
    fallback: [],
    customModels: [],
    order: [],
    timeoutMs: DEFAULT_TIMEOUT_MS,
    maxTokens: DEFAULT_MAX_TOKENS,
    outputLimit: DEFAULT_OUTPUT_LIMIT,
    templateMode: 'builtin',
    templateText: '',
    // v2.4.7（每模式独立自定义模板）：4 模式各一份；缺省空串（enhance 按模式回退内置）
    templateTexts: { base: '', lite: '', standard: '', smart: '' },
    // 模板体系扩展（2026-08-17）：每模式选中模板键 + 多自定义模板列表（解析见 validateConfig）
    templatePick: { base: 'default', lite: 'default', standard: 'default', smart: 'default', publish: 'default' },
    templateCustom: { base: [], lite: [], standard: [], smart: [], publish: [] },
    // v2.2（§6.4）：4 模式 + 记忆开关（缺省 false，行为零变化）
    mode: DEFAULT_MODE,
    context: { mode: 'smart', budgetChars: DEFAULT_BUDGET, workspace: { maxFiles: 3, depth: 2 } },
    memory: DEFAULT_MEMORY,
  };
  // fallback 条目 reasoning effort 提取（条目级，非 main）
  const effortOf = (obj) => (obj && typeof obj === 'object' && obj.enabled === true && typeof obj.effort === 'string' && obj.effort.trim() !== '' && obj.effort.trim().length <= 32) ? obj.effort.trim() : '';
  // fallback（独立配置项；数组顺序 = 尝试顺序；每条可带 reasoning）
  if (Array.isArray(src.fallback)) {
    for (const item of src.fallback.slice(0, 8)) {
      if (!item || typeof item !== 'object') continue;
      if (typeof item.provider !== 'string' || typeof item.model !== 'string') continue;
      const provider = item.provider.trim();
      const model = item.model.trim();
      if (!provider || !model) continue;
      if (out.fallback.some((x) => x.provider === provider && x.model === model)) continue;
      const entry = { provider, model };
      const effort = effortOf(item.reasoning);
      if (effort) entry.reasoningEffort = effort;
      out.fallback.push(entry);
    }
  }
  // customModels（自定义条目：provider/model/name；仅已有 provider 路由下的模型 ID）
  if (Array.isArray(src.customModels)) {
    for (const item of src.customModels.slice(0, 20)) {
      if (!item || typeof item !== 'object') continue;
      if (typeof item.provider !== 'string' || typeof item.model !== 'string') continue;
      const provider = item.provider.trim();
      const model = item.model.trim();
      if (!provider || !model) continue;
      out.customModels.push({
        provider,
        model,
        name: typeof item.name === 'string' && item.name.trim() !== '' ? item.name.trim().slice(0, 40) : model,
      });
    }
  }
  // order（仅约束展示顺序）
  if (Array.isArray(src.order)) {
    for (const key of src.order.slice(0, 50)) {
      if (typeof key === 'string' && key.trim() !== '') {
        const k = key.trim();
        if (!out.order.includes(k)) out.order.push(k);
      }
    }
  }
  // v3.1.7（用户需求·参数无限制）：timeoutMs/maxTokens/outputLimit = 0 → 无限制
  // （不设超时 / provider 默认上限 / collectStream 不截断）；其余范围校验不变
  if ((p.timeoutMs === 0) || (Number.isInteger(p.timeoutMs) && p.timeoutMs >= 1000 && p.timeoutMs <= 300000)) out.timeoutMs = p.timeoutMs;
  if ((p.maxTokens === 0) || (Number.isInteger(p.maxTokens) && p.maxTokens >= 100 && p.maxTokens <= 16000)) out.maxTokens = p.maxTokens;
  if ((p.outputLimit === 0) || (Number.isInteger(p.outputLimit) && p.outputLimit >= 500 && p.outputLimit <= 50000)) out.outputLimit = p.outputLimit;
  // template 解析：v2 结构（template.mode/text/texts）与 v1 平铺（templateMode/templateText）双兼容。
  // v2.4.7 修复：此前只读 v1 平铺字段，v2 结构下自定义模板实际从未生效（templateMode 恒为缺省）。
  const templateMode = typeof t.templateMode === 'string' ? t.templateMode : t.mode;
  const templateText = typeof t.templateText === 'string' ? t.templateText : (typeof t.text === 'string' ? t.text : '');
  if (templateMode === 'custom' || templateMode === 'builtin') out.templateMode = templateMode;
  if (templateText.length <= 4000) out.templateText = templateText;
  // v2.4.7（每模式独立自定义模板）：
  // ① 新结构 texts（4 模式键白名单，各 ≤4000）优先（v2 对象内 t.texts；v1 平铺无此字段）；
  // ② 无 texts 时旧全局 templateText 迁移到全部 4 模式（保持"全局一份"语义不丢内容）；
  // ③ 缺省全空 → enhance 按模式回退内置 SYSTEM_PROMPT。
  const textsSrc = t.texts && typeof t.texts === 'object' ? t.texts : null;
  if (textsSrc) {
    for (const key of MODE_KEYS) {
      const v = textsSrc[key];
      if (typeof v === 'string' && v.length <= 4000) out.templateTexts[key] = v;
    }
  } else if (out.templateText !== '') {
    for (const key of MODE_KEYS) out.templateTexts[key] = out.templateText;
  }
  // 模板体系扩展（2026-08-18 修订）：每模式 2 个内置模板（default=默认模板·只清晰化重述 /
  // increment=增量模板·保守补充未说的大逻辑/信息）+ 每模式多自定义模板（custom[mode]=[{name,text}]，各 ≤4000，≤10 条）；
  // 选中键 pick[mode] = default | increment | custom:<index>（缺省 default）。
  // 迁移：旧配置（无 pick 字段）且 templateMode==='custom' 时，texts[mode] 非空 → 迁为
  // custom[mode]=[{name:'自定义模板', text}] 且 pick[mode]='custom:0'（行为等价旧全局自定义）；
  // 旧内置键 supplement/dev（2026-08-17 每模式 3 模板时期）→ 统一迁为 increment。
  const hasNewPick = t.pick && typeof t.pick === 'object';
  const customSrc = t.custom && typeof t.custom === 'object' ? t.custom : null;
  for (const key of MODE_KEYS) {
    const list = Array.isArray(customSrc && customSrc[key]) ? customSrc[key] : [];
    for (const item of list.slice(0, TEMPLATE_CUSTOM_MAX)) {
      if (item && typeof item === 'object' && typeof item.text === 'string' && item.text.trim() !== '' && item.text.length <= TEMPLATE_TEXT_MAX) {
        out.templateCustom[key].push({
          name: typeof item.name === 'string' && item.name.trim() !== '' ? item.name.trim().slice(0, TEMPLATE_NAME_MAX) : '自定义模板 ' + (out.templateCustom[key].length + 1),
          text: item.text,
        });
      }
    }
  }
  if (!hasNewPick && out.templateMode === 'custom') {
    for (const key of MODE_KEYS) {
      const legacy = out.templateTexts[key];
      if (typeof legacy === 'string' && legacy.trim() !== '' && out.templateCustom[key].length === 0) {
        out.templateCustom[key].push({ name: '自定义模板', text: legacy });
        out.templatePick[key] = 'custom:0';
      }
    }
  } else if (hasNewPick) {
    for (const key of MODE_KEYS) {
      const p = typeof t.pick[key] === 'string' ? t.pick[key] : '';
      if (p === 'increment' || p === 'supplement' || p === 'dev') out.templatePick[key] = 'increment';
      else if (p.indexOf('custom:') === 0) {
        const idx = parseInt(p.slice(7), 10);
        if (Number.isInteger(idx) && idx >= 0 && idx < out.templateCustom[key].length) out.templatePick[key] = 'custom:' + idx;
      }
    }
  }
  // v2.2（§6.4）：mode 解析（4 模式白名单；'memory' 历史值 → lite + memory:true）
  const rawMode = src.mode === 'memory' ? 'lite' : src.mode;
  out.mode = parseMode(rawMode, src.engine, src.context && src.context.mode);
  // v3.1.8（实测驱动·用户指令）：模式默认 params（标准档）——仅当用户未显式设置该项时生效；
  // 显式设置（含 0 无限制）优先（上面 params 校验已覆盖，此处只补缺省）。
  const pModeDefault = MODE_PARAMS_DEFAULT[out.mode] || MODE_PARAMS_DEFAULT[DEFAULT_MODE];
  if (p.timeoutMs === undefined) out.timeoutMs = pModeDefault.timeoutMs;
  if (p.maxTokens === undefined) out.maxTokens = pModeDefault.maxTokens;
  if (p.outputLimit === undefined) out.outputLimit = pModeDefault.outputLimit;
  const ctxCfg = src.context && typeof src.context === 'object' ? src.context : {};
  out.context.budgetChars = parseBudgetChars(ctxCfg.budgetChars, out.mode);
  // 旧 workspace 字段仅作上限兼容（不超联动值），不再单独生效（§0.2）
  if (ctxCfg.workspace && typeof ctxCfg.workspace === 'object') {
    const lim = resolveScanLimit(out.mode, out.context.budgetChars);
    if (Number.isInteger(ctxCfg.workspace.maxFiles) && ctxCfg.workspace.maxFiles >= 1 && ctxCfg.workspace.maxFiles <= lim.maxFiles) out.context.workspace.maxFiles = ctxCfg.workspace.maxFiles;
    if (Number.isInteger(ctxCfg.workspace.depth) && ctxCfg.workspace.depth >= 1 && ctxCfg.workspace.depth <= lim.depth) out.context.workspace.depth = ctxCfg.workspace.depth;
  }
  // v2.2（§6.4）：记忆开关——显式 memory 字段（client config.memory）最高优先（含 false 关闭）；
  // 无 memory 字段时回退 mode='memory' / autoMemory 历史值；缺省 false
  out.memory = src.memory === true ? true : (src.memory === false ? false : parseMemory(src.mode, src.autoMemory));
  // v2.8.2 曾一键发布记忆强制开启（host 兜底）；2026-08-18（用户指令）取消——记忆跟随开关，publish 不例外
  return out;
}

// 模板体系扩展（2026-08-18 修订）：模板容量与名称上限常量
const TEMPLATE_CUSTOM_MAX = 10;
const TEMPLATE_TEXT_MAX = 4000;
const TEMPLATE_NAME_MAX = 40;

// 模板体系扩展（2026-08-18 修订）：每模式选中模板解析——pick key（default/increment/custom:<index>）
// → 内置模板或自定义列表条目；旧内置键 supplement/dev（2026-08-17 时期）按 increment 处理（list[1]）；
// 旧配置（无 pick）→ legacy 兼容（custom 且 texts[mode] 非空用该文本）；
// 非法 pick / 越界索引 / 空文本一律回退内置默认（T1），保证不空白、不报错。
// builtins = { mode: [T1, T2] }（调用方按模式提供内置模板数组；T2=增量模板）。
function resolveTemplateSystem(cfg, mode, builtins) {
  const list = builtins && builtins[mode];
  if (!Array.isArray(list) || list.length === 0) return '';
  const pick = cfg && cfg.templatePick && typeof cfg.templatePick === 'object' ? cfg.templatePick[mode] : '';
  if (typeof pick === 'string' && pick !== '') {
    if (pick === 'increment' || pick === 'supplement' || pick === 'dev') return list[1] || list[0];
    if (pick.indexOf('custom:') === 0) {
      const idx = parseInt(pick.slice(7), 10);
      const custom = cfg && cfg.templateCustom && typeof cfg.templateCustom === 'object' ? cfg.templateCustom[mode] : null;
      if (Number.isInteger(idx) && idx >= 0 && Array.isArray(custom) && idx < custom.length) {
        const item = custom[idx];
        const text = item && typeof item === 'object' ? item.text : '';
        if (typeof text === 'string' && text.trim() !== '') return text.trim();
      }
      return list[0];
    }
    return list[0];
  }
  // legacy：无 pick 的旧配置——custom 且当前模式 texts 非空 → 用该模式文本
  if (cfg && cfg.templateMode === 'custom') {
    const perMode = cfg.templateTexts && typeof cfg.templateTexts === 'object' ? cfg.templateTexts[mode] : '';
    if (typeof perMode === 'string' && perMode.trim() !== '') return perMode.trim();
  }
  return list[0];
}

async function collectStream(iterator, outputLimit, onFirst) {
  let text = '';
  let sawDelta = false;
  const blockTexts = [];
  let finish = null;
  let sawAny = false;
  try {
    while (true) {
      const next = await iterator.next();
      if (next.done) break;
      const chunk = next.value;
      if (!sawAny) {
        sawAny = true;
        if (typeof onFirst === 'function') onFirst(chunk);
      }
      if (chunk.type === 'text-delta') {
        // v2.7.0（publish 一键发布）：outputLimit <= 0 表示不限制（规格长文不截断）
        if (outputLimit > 0 && text.length + chunk.text.length > outputLimit) return { kind: 'toolong' };
        sawDelta = true;
        text += chunk.text;
      } else if (chunk.type === 'block-end' && chunk.block.type === 'text') {
        blockTexts.push(chunk.block.text);
      } else if (chunk.type === 'finish') {
        finish = chunk.reason;
        break;
      }
    }
  } catch (e) {
    return { kind: 'cancelled' };
  }
  if (!finish) return { kind: 'cancelled' };
  if (finish.kind === 'stop') {
    return { kind: 'ok', text: sawDelta ? text : blockTexts.join('') };
  }
  if (finish.kind === 'aborted' || finish.kind === 'error' && finish.failure && finish.failure.code === 'ABORTED') {
    return { kind: 'aborted' };
  }
  if (finish.kind === 'error') {
    return { kind: 'error', failure: finish.failure };
  }
  return { kind: 'error', failure: { code: 'EMPTY_RESPONSE', message: 'stream ended without a finish chunk' } };
}

// v23（D6）：模型链构建——按 cfg.fallback 顺序逐一尝试（去重）；
// 链为空时才用自适应/内置链补足；不再有 main 优先概念。
// 2026-08-18（用户需求·删内置兜底链）：模型链完全按用户配置顺序；配置为空 → 空链
//（不再 adaptive/BUILTIN 补足），enhance 报无模型
function buildTryChain(fallback) {
  const chain = [];
  for (const item of fallback || []) {
    if (!item || typeof item !== 'object') continue;
    if (typeof item.provider !== 'string' || typeof item.model !== 'string') continue;
    const provider = item.provider.trim();
    const model = item.model.trim();
    if (!provider || !model) continue;
    if (chain.some((e) => e.provider === provider && e.model === model)) continue;
    const entry = { provider, model };
    if (item.reasoningEffort) entry.reasoningEffort = item.reasoningEffort;
    chain.push(entry);
  }
  return chain;
}

// ================= V2 上下文感知优化 · 纯函数族 =================
// （v2.0.0 方案 §3：阶段 A 规则提取 / 阶段 B 检索排序与摘要 / 阶段 C 预算组装）

// V2 分支判定（表驱动 §4.1）：phaseC 非 none 且预算 > 0 才走注入路径


// 事件 → 文本消息列表（过滤噪音；**从尾部反向遍历**取最近 limit 条——DSH 日志可能达百万级）
// DSH 事件类型形如 'user/message'、'assistant/message'、'assistant/chunk'、'tool/call'；
// 只认 role/kind 前缀为 user|assistant 且 kind 非 chunk 的文本消息；
// 文本在 text/content/payload/message/data 容器（DSH 实际为 ev.data.content[].text）。
function extractHistory(events, limit) {
  const arr = Array.isArray(events) ? events : [];
  const out = [];
  // 候选文本容器（顺序优先；第一个能取出非空文本的胜出）
  const pickText = (container) => {
    if (!container || typeof container !== 'object') return '';
    if (typeof container.text === 'string') return container.text;
    if (Array.isArray(container.content)) {
      return container.content.map((b) => (b && typeof b === 'object' && typeof b.text === 'string') ? b.text : '').join(' ');
    }
    return '';
  };
  for (let i = arr.length - 1; i >= 0 && out.length < limit; i--) {
    const ev = arr[i];
    if (!ev || typeof ev !== 'object') continue;
    const type = String(ev.type || ev.kind || '').toLowerCase();
    let role = ev.role || (ev.payload && ev.payload.role) || (ev.message && ev.message.role) || (ev.data && ev.data.role) || (ev.content && typeof ev.content === 'object' && ev.content.role);
    const slash = type.indexOf('/');
    const typeRole = slash > 0 ? type.slice(0, slash) : type;
    if (typeRole === 'user' || typeRole === 'assistant') {
      if (slash > 0 && type.slice(slash + 1) === 'chunk') continue; // 流片段噪音
      if (role !== 'user' && role !== 'assistant') role = typeRole;
    }
    if (role !== 'user' && role !== 'assistant') continue;
    let text = '';
    if (typeof ev.text === 'string') text = ev.text;
    else text = pickText(ev.content) || pickText(ev.payload) || pickText(ev.message) || pickText(ev.data);
    text = text.trim();
    if (!text) continue;
    if (text.startsWith('/')) continue;
    if (/^(\[工具|tool|function call)/i.test(text)) continue;
    out.push({ type: role, text: text.slice(0, V2_MSG_TEXT_MAX) });
  }
  return out.reverse();
}

// v3.1.3（用户需求·仅结论参考）：块级「结论提取」——入参为 sessionQuery.readSurface 的
// 原始事件（保留 content 块结构；listEvents 记录无文本、filterEvents 文档文本为预拼接
// 语义文本，二者都无法做块级过滤）。只取 user/assistant 消息的 text 块（显式结论/正文），
// 丢弃 reasoning（思考过程）、tool-call（工具名+参数 JSON）、tool-result（工具输出）块，
// 并丢弃 user 消息内 <system-reminder> 系统注入块；输出形状与 extractHistory 一致
// （[{type:'user'|'assistant', text}]，时间序），下游 splitHistoryRounds/judge 零改动。
// 轮次覆盖：按 V2_ROUNDS_SCAN_MAX（10）轮向后扫描，覆盖 standard 最深窗口 [6,10]；
// 每条目 ≤V2_MSG_TEXT_MAX 截断；超出历史 → 窗口判定自然 miss（不注入）。
function extractHistoryConclusions(events) {
  const arr = Array.isArray(events) ? events : [];
  const out = [];
  const isInjection = (b) => /^<system-reminder>/i.test(String((b && b.text) || '').trim());
  const textOf = (blocks) => {
    const parts = [];
    for (const b of blocks || []) {
      if (!b || typeof b !== 'object') continue;
      if (b.type !== 'text') continue;          // 丢弃 reasoning/tool-call/tool-result 等
      if (isInjection(b)) continue;             // 丢弃系统注入块
      const t = String(b.text || '').trim();
      if (t !== '') parts.push(t);
    }
    return parts.join('\n');
  };
  let userSeen = 0;
  for (let i = arr.length - 1; i >= 0; i--) {
    const ev = arr[i];
    if (!ev || typeof ev !== 'object') continue;
    const type = String(ev.type || '').toLowerCase();
    if (type === 'user/message') {
      if (userSeen >= V2_ROUNDS_SCAN_MAX) break;
      userSeen++;
    } else if (type !== 'assistant/message') {
      continue;
    } else if (userSeen >= V2_ROUNDS_SCAN_MAX) {
      break;
    }
    const content = type === 'user/message'
      ? (ev.data && ev.data.content)
      : (ev.data && ev.data.message && ev.data.message.content);
    const text = textOf(content).trim();
    if (text === '') continue;
    if (text.startsWith('/')) continue;
    out.push({ type: type === 'user/message' ? 'user' : 'assistant', text: text.slice(0, V2_MSG_TEXT_MAX) });
  }
  return out.reverse();
}

// v3.1.3（看门狗 + 延迟连通性预检）：探测结果缓存纯读写（注入 Map，单测友好；
// LRU 上限 50，超限驱逐最早插入条目；TTL 过期即失效并删除）
function probeCacheGet(cache, key, now) {
  const hit = cache.get(key);
  if (!hit) return null;
  if (now - hit.at >= PROBE_CACHE_TTL_MS) {
    cache.delete(key);
    return null;
  }
  return hit;
}
function probeCacheSet(cache, key, value, now) {
  if (cache.size >= 50) {
    const oldest = cache.keys().next().value;
    if (oldest !== undefined) cache.delete(oldest);
  }
  cache.set(key, { ok: value.ok === true, code: value.code || '', at: now });
}
// 纯选择：entries 与 results 对齐，返回首个 ok 下标（无 → -1）
function pickReachableIndex(entries, results) {
  for (let i = 0; i < entries.length; i++) {
    const r = results[i];
    if (r && r.ok === true) return i;
  }
  return -1;
}

// 中文主题词切分（v2.7.0 检索质量修复）：连续中文按连接/虚词切段——
// 旧实现 [\u4e00-\u9fa5]{2,8} 按 8 字窗口截断，产生「项目的构建与发布」式碎片，
// 无法命中文件名/内容（V2 文档「遗留观察」中文检索噪音的根因）。
function splitCnSegments(s) {
  const out = [];
  for (const m of String(s || '').matchAll(/[\u4e00-\u9fa5]+/g)) {
    const seg = m[0];
    // 非捕获组切分，分隔符不进入结果
    const tokens = seg.split(/(?:以及|或者|并且|然后|因为|所以|如果|但是|没有|就是|不是|对于|关于|通过|根据|按照|一个|一份|一种|一些|输出|提供|包含|包括|进行|使用|需要|希望|想要|可以|能够|和|与|并|或|及|为|了|在|中|请|对|把|将|等|它|其|这|那|个|是|有|要|让|帮|我|你|他|她|的)/).filter(Boolean);
    for (const t of tokens) out.push(t);
  }
  return out;
}

// 规则版任务焦点提取（basic 模式）：代码/路径/扩展名 token + 中文主题词
function inferFocusRules(historyText) {
  const focus = [];
  const seen = new Set();
  const add = (w) => {
    if (!w || seen.has(w) || w.length < 2) return;
    seen.add(w);
    focus.push(w);
  };
  const s = String(historyText || '');
  // 路径与文件名 token（src/xxx.py、foo_bar.ts、package.json 等）
  // 注意：长扩展名（json/tsx/jsx/yaml）必须在短前缀（js/ts/ya）之前，避免交替误匹配
  for (const m of s.matchAll(/[A-Za-z0-9_\-./\\]+\.(?:json|yaml|yml|tsx|jsx|toml|svelte|python|html|css|typescript|javascript|py|ts|js|md|txt|go|rs|java|cpp|c|h|sh|sql|vue)/g)) {
    const path = m[0];
    const base = path.split(/[\\/]/).pop();
    add(base);
    add(base.replace(/\.[^.]+$/, ''));
  }
  // 中文主题词（v2.7.0：连接虚词切分，避免 8 字窗口碎片化）
  for (const w of splitCnSegments(s)) add(w);
  // 英文主题词（驼峰与下划线词）
  for (const m of s.matchAll(/[A-Za-z][A-Za-z0-9]*(?:_[A-Za-z0-9]+)+/g)) add(m[0]);
  for (const m of s.matchAll(/\b[A-Z][a-z]+(?:[A-Z][a-z]+)+\b/g)) add(m[0]);
  // 去除常见停用词（含连接虚词切分残留与动作词）
  const stop = new Set(['这个', '那个', '我们', '你们', '他们', '可以', '能够', '需要', '希望', '想要', '进行', '使用', '一个', '一份', '一种', '一些', '什么', '怎么', '如何', '如果', '因为', '所以', '但是', '然后', '并且', '或者', '以及', '还是', '没有', '就是', '不是', '对于', '关于', '通过', '根据', '按照', '项目', '文件', '功能', '实现', '添加', '修改', '删除', '创建', '优化', '提示', '词优', '分析', '检查', '输出', '提供', '包含', '包括', '写', '生成', '编写', '设计', '帮助', '我的', '一种', '的', '与', '和', '并', '或', '及', '为', '了', '在', '中', '请', '对', '把', '将', '等', '它', '其', '这', '那', '个', '是', '有', '要', '让', '帮', '我', '你', '他', '她', '用户', '助手', 'the', 'and', 'for', 'with', 'this', 'that', 'from', 'into']);
  const filtered = focus.filter((w) => !stop.has(w.toLowerCase()));
  return filtered.slice(0, KEYWORD_LIMIT);
}

// v2.4.4（lite 规则引擎落地）：prompt 工程要素检查——目标/约束/输出格式/示例 四项缺失检测。
// lite 模式此前 phaseA:'rule' 空转（buildV2ContextBlock 仅 phaseC==='inject' 时执行阶段 A），
// 与 base 行为完全相同；本函数让「本地规则分析」真正生效：零 LLM 成本、零外部上下文，
// 仅依据输入文本检测要素缺失，产出供 system 附加的强化指令。
// v2.4.5（语义保真修正）：suggestions 改为保守措辞——只提示「若可合理推断才明确化」，
// 严禁诱导模型添加原文未提及的新要求（此前"请在优化结果中补充合理约束（如长度上限…）"
// 会引导模型臆造内容，与「语义保真」冲突）。
// 返回 { missing: [{key,label}], suggestions: string[] }；全部具备时 suggestions 为空。


// 主题词提取：提示词关键词 ∪ focus（5–8 词）
function extractKeywords(text, focus) {
  const kw = inferFocusRules(text);
  const seen = new Set();
  const out = [];
  const add = (w) => {
    if (!w || seen.has(w) || w.length < 2) return;
    seen.add(w);
    out.push(w);
  };
  for (const w of kw) add(w);
  for (const w of (focus || [])) add(w);
  return out.slice(0, KEYWORD_LIMIT);
}

// 敏感文件硬过滤（防密钥/凭据注入外发）：.env/密钥/凭据/日志 等
function shouldIgnoreFile(name) {
  const n = String(name || '').toLowerCase();
  if (n.includes('.env')) return true;
  if (/(\.pem|\.key|\.p12|\.pfx|\.jks|\.keystore|\.crt|\.cer)$/.test(n)) return true;
  // 路径任意段命中凭据/密钥类文件名（含目录段，如 config/credentials.json）
  if (/(^|[\\/.])(credentials|secret|secrets|token|id_rsa|id_ed25519|id_ecdsa|\.npmrc|\.pypirc|\.netrc|\.htpasswd)([\\/.]|$)/.test(n)) return true;
  if (/\.log(\.|$)/.test(n)) return true;
  if (/^(node_modules|\.git|dist|build|\.venv|venv|__pycache__|\.next|\.cache|coverage)/.test(n)) return true;
  return false;
}

// 文件排序：名称/路径命中关键词计分（路径深度浅加分）→ Top-K
function rankFiles(files, keywords, topK) {
  const kws = (keywords || []).filter((k) => typeof k === 'string' && k.length >= 2);
  if (kws.length === 0 || !Array.isArray(files)) return [];
  const scored = [];
  for (const f of files) {
    const name = String(f || '');
    if (shouldIgnoreFile(name)) continue;
    const lower = name.toLowerCase();
    let score = 0;
    for (const k of kws) {
      const kl = k.toLowerCase();
      if (lower.includes(kl)) score += 2;          // 名称/路径命中
      const base = lower.split(/[\\/]/).pop();
      if (base.includes(kl)) score += 3;           // 文件名命中权重更高
    }
    if (score > 0) {
      const depth = name.split(/[\\/]/).length - 1;
      scored.push({ path: name, score: score - depth * 0.1 });  // 浅路径优先
    }
  }
  scored.sort((a, b) => b.score - a.score);
  return scored.slice(0, topK || 3);
}

// 行摘要：命中行 ±2 上下文；无命中取头部 40 行；≤预算字符
function snippetFromLines(lines, keywords, budget) {
  const arr = Array.isArray(lines) ? lines : [];
  const b = typeof budget === 'number' && budget > 0 ? budget : SNIPPET_BUDGET;
  const kws = (keywords || []).filter((k) => typeof k === 'string' && k.length >= 2);
  const hits = [];
  if (kws.length > 0) {
    for (let i = 0; i < arr.length; i++) {
      const ln = String(arr[i] || '');
      if (kws.some((k) => ln.toLowerCase().includes(k.toLowerCase()))) {
        hits.push(i);
        if (hits.length >= 8) break;
      }
    }
  }
  let out = [];
  if (hits.length > 0) {
    const picked = new Set();
    for (const h of hits) {
      for (let i = Math.max(0, h - 2); i <= Math.min(arr.length - 1, h + 2); i++) picked.add(i);
    }
    out = [...picked].sort((a, b) => a - b).map((i) => String(arr[i] || ''));
  } else {
    out = arr.slice(0, 40);
  }
  let text = out.join('\n');
  if (text.length > b) text = text.slice(0, b);
  return text;
}

// v2.7.0（一键发布 · 网络检索词构造）：草稿主题词 ∪ delta 增删实词。
// 每次改动后 text 关键词自然变化（基础代入检索条件）；记忆链 delta 显式并入
// （改动方向代入——新增/删除的内容决定下一轮检索方向）。
function buildWebQuery(text, keywords, delta) {
  const parts = [];
  const seen = new Set();
  const add = (w) => {
    if (!w || seen.has(w)) return;
    seen.add(w);
    parts.push(w);
  };
  for (const k of (keywords || [])) add(k);
  const deltas = (delta && Array.isArray(delta.added) ? delta.added : [])
    .concat(delta && Array.isArray(delta.removed) ? delta.removed : []);
  for (const d of deltas) for (const w of splitCnSegments(String(d))) add(w);
  if (parts.length === 0) {
    for (const w of splitCnSegments(String(text || ''))) { add(w); if (parts.length >= 6) break; }
  }
  return parts.slice(0, 8).join(' ');
}

// v2.8.0（一键发布 · 场景路由）：场景判定词表（分级加权：强特征 ×2 / 泛词 ×1 / 英文强 ×1）。
// 中英文词表互不重叠（同一 token 只计一次，避免双计）；英文词匹配前统一小写。
const SCENARIO_WORDS = {
  gameStrong: ['卡牌', '塔防', '肉鸽', '副本', '抽卡', '回合', '射击', '放置', '解谜', '成就', '装备', '技能', '关卡', '战斗'],
  gameWeak: ['游戏', '玩法', '角色', '剧情', '地图', '等级'],
  gameEn: ['game', 'card', 'roguelike', 'level', 'battle', 'boss'],
  softwareStrong: ['数据库', '微服务', '后端', '前端', '权限', '登录', '部署', '中间件', '缓存', '并发'],
  softwareWeak: ['软件', '应用', '系统', '平台', 'saas', '网站', '工具', '管理', '协作', '自动化', '脚本', '插件', '客户端', '移动端', '桌面'],
  softwareEn: ['app', 'website', 'web', 'api', 'database', 'dashboard', 'crm'],
};

// v2.8.0（一键发布 · 场景路由）：依据输入判定项目场景（game/software/generic）。
// 计分制：加权求和、多者胜；平局/双零 → generic。keywords 可选（缺省等价空数组）。
function detectScenario(text, keywords) {
  const joined = String(text || '').toLowerCase() + ' ' +
    String(Array.isArray(keywords) ? keywords.join(' ') : '').toLowerCase();
  let game = 0;
  let software = 0;
  const count = (list, weight, side) => {
    for (const w of list) {
      if (joined.indexOf(w) !== -1) {
        if (side === 'g') game += weight; else software += weight;
      }
    }
  };
  count(SCENARIO_WORDS.gameStrong, 2, 'g');
  count(SCENARIO_WORDS.gameWeak, 1, 'g');
  count(SCENARIO_WORDS.gameEn, 1, 'g');
  count(SCENARIO_WORDS.softwareStrong, 2, 's');
  count(SCENARIO_WORDS.softwareWeak, 1, 's');
  count(SCENARIO_WORDS.softwareEn, 1, 's');
  if (game === software) return 'generic';
  return game > software ? 'game' : 'software';
}

// v2.8.0（一键发布 · 实测修正）：剥离输出首部的【场景判定】回显行——模型常复述
// system 注入的判定行（完整行或截断行两种形态）；指令式「请勿复述」约束不可靠，
// 改用确定性剥离（仅剥离首部一行 + 跟随空行，正文不受影响）。
function stripScenarioEcho(text) {
  const s = String(text || '');
  const re = /^【场景判定】本次场景判定：(?:game|software)[^\n]*\n?/;
  return s.replace(re, '').replace(/^\n+/, '');
}

// 上下文块组装：任务进度(≤800) + 文件(≤3×800) + 事件(≤800)；
// 截断优先级：进度 > 文件 > 事件 > 原文完整（原文由调用方保证不截断）
function buildContextBlock(progress, files, events, budgetChars) {
  const budget = typeof budgetChars === 'number' && budgetChars > 0 ? budgetChars : 0;
  if (budget <= 0) return '';
  const MAX_PROGRESS = Math.min(CONTEXT_PROGRESS_MAX, budget);
  const MAX_EVENT = Math.min(CONTEXT_PROGRESS_MAX, Math.floor(budget / CONTEXT_EVENT_DIVISOR));
  const MAX_FILE = budget > CONTEXT_PROGRESS_MAX ? Math.min(CONTEXT_PROGRESS_MAX, Math.floor((budget - Math.min(CONTEXT_PROGRESS_MAX, MAX_PROGRESS) - MAX_EVENT) / CONTEXT_FILE_COUNT)) : 0;
  const parts = [];
  let used = 0;
  // 1) 任务进度（最高优先级）
  if (progress && (progress.task || progress.currentStep || (progress.completed && progress.completed.length))) {
    const lines = [];
    if (progress.task) lines.push('任务：' + progress.task);
    if (progress.currentStep) lines.push('当前步骤：' + progress.currentStep);
    if (Array.isArray(progress.completed) && progress.completed.length) lines.push('已完成：' + progress.completed.join('；'));
    const text = lines.join('\n').slice(0, MAX_PROGRESS);
    if (text) {
      parts.push('【任务进度】\n' + text);
      used += text.length + 20;
    }
  }
  // 2) 相关文件（其次）
  if (MAX_FILE > 0 && Array.isArray(files) && files.length) {
    const segs = [];
    for (const f of files.slice(0, 3)) {
      if (!f || typeof f !== 'object') continue;
      const snip = String(f.snippet || '').slice(0, MAX_FILE);
      if (snip) segs.push('📄 ' + (f.path || '?') + '\n' + snip);
    }
    if (segs.length) {
      const text = segs.join('\n\n').slice(0, Math.max(0, budget - used));
      if (text) {
        parts.push('【相关项目文件】\n' + text);
        used += text.length + 20;
      }
    }
  }
  // 3) 相关会话片段（最后，预算余量）
  if (Array.isArray(events) && events.length) {
    const text = String(events.slice(0, 3).map((e) => typeof e === 'string' ? e : '').join('\n'))
      .slice(0, Math.max(0, Math.min(MAX_EVENT, budget - used)));
    if (text) parts.push('【相关会话片段】\n' + text);
  }
  return parts.join('\n\n');
}

// smart 模式 JSON 容错解析（剥离 ```json 代码块与前后缀噪音）
function parseTaskProgress(raw) {
  if (typeof raw !== 'string') return null;
  let s = raw.trim();
  const fence = s.match(/```(?:json)?\s*([\s\S]*?)```/);
  if (fence) s = fence[1].trim();
  const start = s.indexOf('{');
  const end = s.lastIndexOf('}');
  if (start === -1 || end <= start) return null;
  s = s.slice(start, end + 1);
  let obj;
  try {
    obj = JSON.parse(s);
  } catch (e) {
    return null;
  }
  if (!obj || typeof obj !== 'object') return null;
  const out = {};
  if (typeof obj.task === 'string' && obj.task.trim()) out.task = obj.task.trim().slice(0, 200);
  if (typeof obj.currentStep === 'string' && obj.currentStep.trim()) out.currentStep = obj.currentStep.trim().slice(0, 200);
  if (Array.isArray(obj.completed)) {
    out.completed = obj.completed.filter((c) => typeof c === 'string' && c.trim()).map((c) => c.trim().slice(0, 120)).slice(0, 5);
  }
  if (Array.isArray(obj.focus)) {
    out.focus = obj.focus.filter((f) => typeof f === 'string' && f.trim()).map((f) => f.trim().slice(0, 40)).slice(0, 4);
  }
  if (!out.task && !out.currentStep && !out.completed) return null;
  return out;
}
// ================= V2 纯函数族结束 =================

// v17.3：长文连通性探测（实测 TTFT + 吞吐；usage 优先，缺失按字符/4 估算）
// v3.1.7（方案 2 · 用户确认）：探测从「short 200-token」改为「长文 ~1200-token」——
// 此前 prompt 要求 short + maxTokens=200，模型只吐几个 token、decode 窗口仅 ~2ms，
// tps = token/极短时间 被数学放大（实测虚高到 4500 tok/s，真实量级 75–267）。
// 现要求写长文，decode 窗口拉到秒级，tps 才有意义；探测默认不传 reasoningEffort
// （未开思考，TTFT 不含思考时间、更贴近纯生成速度）。
async function pingStream(llmService, entry, ref) {
  const startedAt = Date.now();
  let ttftMs = -1;
  let sawFirst = false;
  let firstTokenAt = null;
  let finishedAt = null;
  let outputChars = 0;
  let usageOutputTokens = null;
  let stream;
  try {
    stream = llmService.stream({
      provider: entry.provider,
      model: entry.model,
      ...(entry.reasoningEffort ? { reasoningEffort: entry.reasoningEffort } : {}),
      maxTokens: 1200,
      system: 'You are a connectivity probe. Write a continuous plain-paragraph essay of about 800 words on any topic. Do not use markdown, headings, lists or bullet points; just plain flowing text.',
      messages: [{
        id: 'enhance-ping',
        role: 'user',
        content: [{ type: 'text', text: 'Write a continuous plain-paragraph essay of about 800 words on a topic of your choice. No headings, no lists — plain flowing prose only.' }],
        source: { kind: 'user' },
      }],
    });
  } catch (e) {
    return { ok: false, code: e && e.code ? String(e.code) : 'LLM_FAILED', message: String(e && e.message ? e.message : e) };
  }
  const iterator = stream[Symbol.asyncIterator]();
  if (ref) ref.current = iterator;
  let finish = null;
  try {
    while (true) {
      const next = await iterator.next();
      if (next.done) break;
      const chunk = next.value;
      if (chunk.type === 'text-delta') {
        if (!sawFirst) {
          sawFirst = true;
          ttftMs = Date.now() - startedAt;
          firstTokenAt = Date.now();
        }
        outputChars += typeof chunk.text === 'string' ? chunk.text.length : 0;
      } else if (!sawFirst && chunk.type === 'block-end' && chunk.block && chunk.block.type === 'text') {
        sawFirst = true;
        ttftMs = Date.now() - startedAt;
        firstTokenAt = Date.now();
      }
      if (chunk.type === 'usage' && chunk.usage && typeof chunk.usage.outputTokens === 'number') {
        usageOutputTokens = chunk.usage.outputTokens;
      }
      if (chunk.type === 'finish') {
        finish = chunk.reason;
        finishedAt = Date.now();
        break;
      }
    }
  } catch (e) {
    return { ok: false, code: 'ABORTED', message: 'probe aborted' };
  }
  const endAt = finishedAt || Date.now();
  const latencyMs = endAt - startedAt;
  const decodeMs = sawFirst ? Math.max(0, endAt - firstTokenAt) : null;
  const outputTokens = usageOutputTokens != null ? usageOutputTokens : Math.round(outputChars / 4);
  const tokensPerSecond = decodeMs != null && decodeMs > 0 ? outputTokens / (decodeMs / 1000) : null;
  const base = {
    ok: true,
    latencyMs,
    ttftMs: sawFirst ? ttftMs : latencyMs,
    model: entry.model,
    outputChars,
    outputTokens,
    decodeMs,
    tokensPerSecond,
  };
  // 容错：部分网关对极短请求直接结束流不发 finish——收到过文本即视为连通
  if (!finish) {
    if (sawFirst) return base;
    return { ok: false, code: 'EMPTY_RESPONSE', message: 'probe returned no output' };
  }
  if (finish.kind === 'stop') {
    return base;
  }
  if (finish.kind === 'aborted' || (finish.kind === 'error' && finish.failure && finish.failure.code === 'ABORTED')) {
    return { ok: false, code: 'ABORTED', message: 'probe aborted' };
  }
  if (finish.kind === 'error') {
    const code = finish.failure && finish.failure.code ? String(finish.failure.code) : 'LLM_FAILED';
    return { ok: false, code, message: friendlyMessage(finish.failure) };
  }
  // v17.2：其余 finish（length/content-filter/tool-calls 等）——端点已响应即视为连通（探测目标是可达性）
  return base;
}



// ================= 模型历史统计聚合（2026-08-17 · 连通性测试预计耗时） =================
// 从会话原始日志提取最后出现的 provider/model 路由（request/header 或 request/context）。
function extractModelRouteFromEvents(events) {
  let route = null;
  if (!Array.isArray(events)) return route;
  for (const ev of events) {
    if (!ev || typeof ev !== 'object') continue;
    if (ev.type === 'request/header' && ev.data && ev.data.header && ev.data.header.config) {
      const c = ev.data.header.config;
      if (c && typeof c.provider === 'string' && c.provider && typeof c.model === 'string' && c.model) {
        route = { provider: c.provider, model: c.model };
      }
    } else if (ev.type === 'request/context' && ev.data && ev.data.provider && ev.data.model) {
      route = { provider: String(ev.data.provider), model: String(ev.data.model) };
    }
  }
  return route;
}

// 累加一个会话的 sessionStats/tokenUsage 投影值（缺失/异常字段按 0 跳过）。
function accumulateProjectionStats(acc, projectionSnapshot) {
  const values = projectionSnapshot && projectionSnapshot.values ? projectionSnapshot.values : {};
  const s = values.sessionStats;
  if (!s || typeof s !== 'object') return;
  acc.ttftMs += Number(s.ttftMs) || 0;
  acc.ttftSteps += Number(s.ttftSteps) || 0;
  acc.decodeMs += Number(s.decodeMs) || 0;
  acc.decodeTokens += Number(s.decodeTokens) || 0;
  acc.sessions += 1;
}

// 聚合结果 → 平均 TTFT 与解码吞吐；无样本时对应字段为 null。
function summarizeModelStats(acc) {
  return {
    sessions: acc.sessions,
    ttftMs: acc.ttftSteps > 0 ? acc.ttftMs / acc.ttftSteps : null,
    tokensPerSecond: acc.decodeMs > 0 ? acc.decodeTokens / (acc.decodeMs / 1000) : null,
  };
}

// 基础模式默认模板预计耗时：输出 token 按输入字符数 /4 动态估算，最低 200 token。
const BASE_EST_CHARS_PER_TOKEN = 4;
const BASE_EST_MIN_OUTPUT_TOKENS = 200;
function estimateBaseModeSeconds(ttftMs, tokensPerSecond, inputChars) {
  if (typeof ttftMs !== 'number' || !Number.isFinite(ttftMs) ||
      typeof tokensPerSecond !== 'number' || !Number.isFinite(tokensPerSecond) || tokensPerSecond <= 0) {
    return null;
  }
  const chars = Number.isFinite(inputChars) && inputChars > 0 ? inputChars : 0;
  const outputTokens = Math.max(BASE_EST_MIN_OUTPUT_TOKENS, Math.ceil(chars / BASE_EST_CHARS_PER_TOKEN));
  return (ttftMs / 1000) + (outputTokens / tokensPerSecond);
}

// 轻量模式预计耗时（v3.1.6 口径修正）：v3.0 模式重构后 lite = rounds 检索——
// 读取最近 1 轮会话 + LLM 关联判定（RELEVANCE 调用，输出预算 RELEVANCE_MAX_TOKENS=400），
// 命中则注入参考。因此相对基础模式**多一次关联判定 LLM 调用**（自身 TTFT + 判定输出吞吐），
// 预计耗时更长；不再是 v3.1.5 的「输出更短→更快」错误口径。
function estimateLiteModeSeconds(ttftMs, tokensPerSecond, inputChars) {
  const base = estimateBaseModeSeconds(ttftMs, tokensPerSecond, inputChars);
  if (base === null) return null;
  const judgeSeconds = (ttftMs / 1000) + (RELEVANCE_MAX_TOKENS / tokensPerSecond);
  return base + judgeSeconds;
}

// ================= v2.4.0 版本检测与一键更新 · 纯函数族 =================
// 方案「插件版本检测与一键更新方案.md」§1-§3：检测目标 / 版本比较 / 更新流程。
// 本地版本单一事实源（发布时 bump；client 不另存副本，统一经 update/check 读取）
// v3.2.1-t（架构调整·版本单一事实源）：PLUGIN_VERSION 由 build-host.mjs 从 package.json 构建注入
// （'3.4.0' 占位符替换）——源码不再硬编码版本，杜绝「发版忘 bump → 检测永远旧版」漂移。
// 测试/动态形态下占位符未替换 → typeof 未定义 → 回退 0.0.0（仅格式占位，构建产物始终为真实版本）。
const PLUGIN_VERSION = typeof '3.4.0' !== 'undefined' ? '3.4.0' : '0.0.0';
// 一键拉取的文件清单（发布仓库根目录，raw.githubusercontent.com 按 tag 拉取）
const UPDATE_MANIFEST = ['plugin-host.js', 'README.md', 'README.en.md', 'cordis.patch.yml'];
// update/check 结果缓存 TTL（未鉴权 GitHub API 限流 60 次/时）
const UPDATE_CACHE_TTL_MS = 300000;

// 版本解析：去 v/V 前缀 → 去 +build 元数据 → 主/次/补丁 3 段数值（缺段补 0）→ - 预发布段
// （数值段转 number，其余保留字符串）。返回 { ok, seg, pre, raw }；无法解析 → { ok:false }。
function parseVersion(str) {
  if (typeof str !== 'string') return { ok: false };
  const s0 = str.trim();
  if (s0 === '') return { ok: false };
  let s = s0.replace(/^[vV]/, '');
  const plus = s.indexOf('+');
  if (plus !== -1) s = s.slice(0, plus);
  let pre = null;
  const dash = s.indexOf('-');
  if (dash !== -1) {
    const preRaw = s.slice(dash + 1);
    s = s.slice(0, dash);
    if (preRaw === '') return { ok: false };
    pre = preRaw.split('.').map((seg) => (/^\d+$/.test(seg) ? Number(seg) : seg));
  }
  const parts = s.split('.');
  if (parts.length < 1 || parts.length > 3) return { ok: false };
  const seg = [];
  for (const p of parts) {
    if (!/^\d+$/.test(p)) return { ok: false };
    seg.push(Number(p));
  }
  while (seg.length < 3) seg.push(0);
  return { ok: true, seg, pre, raw: s0 };
}

// semver 比较：-1 | 0 | 1；任一侧无法解析 → null。
// 主/次/补丁数值逐段比；无预发布 > 有预发布；预发布逐段比（数值<字符串；前缀相同短者更小）。
function compareVersions(a, b) {
  const pa = parseVersion(a);
  const pb = parseVersion(b);
  if (!pa.ok || !pb.ok) return null;
  for (let i = 0; i < 3; i++) {
    if (pa.seg[i] !== pb.seg[i]) return pa.seg[i] > pb.seg[i] ? 1 : -1;
  }
  if (pa.pre === null && pb.pre === null) return 0;
  if (pa.pre === null) return 1;
  if (pb.pre === null) return -1;
  const len = Math.max(pa.pre.length, pb.pre.length);
  for (let i = 0; i < len; i++) {
    const x = pa.pre[i];
    const y = pb.pre[i];
    if (x === undefined) return -1;
    if (y === undefined) return 1;
    if (typeof x === 'number' && typeof y === 'number') {
      if (x !== y) return x > y ? 1 : -1;
    } else if (typeof x === 'string' && typeof y === 'string') {
      if (x !== y) return x > y ? 1 : -1;
    } else {
      return typeof x === 'number' ? -1 : 1;
    }
  }
  return 0;
}

// 状态判定：remote > local → 'outdated'；否则 'current'；任一侧无法解析 → 'unknown'
function versionStatus(local, remote) {
  const pa = parseVersion(local);
  const pb = parseVersion(remote);
  if (!pa.ok || !pb.ok) return 'unknown';
  return compareVersions(remote, local) > 0 ? 'outdated' : 'current';
}

// 仓库归一化校验：owner/name，仅 [A-Za-z0-9_.-]，长度 ≤100，拒绝 '..'；非法 → null
function normalizeRepo(input) {
  if (typeof input !== 'string') return null;
  const s = input.trim();
  if (s.length === 0 || s.length > 100) return null;
  if (!/^[A-Za-z0-9_.-]+\/[A-Za-z0-9_.-]+$/.test(s)) return null;
  if (s.indexOf('..') !== -1) return null;
  return s;
}

// tag 白名单校验（update/pull 入参，防 URL/路径注入）：/^v?[0-9A-Za-z._-]+$/ 且不含 '..'
function isValidTag(tag) {
  return typeof tag === 'string' && tag !== '' && tag.length <= 100 &&
    /^v?[0-9A-Za-z._-]+$/.test(tag) && tag.indexOf('..') === -1;
}

// tags 列表（GitHub /tags 响应数组）取最大可解析版本：返回 { raw, version } | null；
// 不可解析的 tag 直接排除；全部不可解析/空列表 → null（host 映射 NO_REMOTE_VERSION）
function pickMaxTag(tags) {
  if (!Array.isArray(tags)) return null;
  let best = null;
  for (const t of tags) {
    const name = t && typeof t === 'object' && typeof t.name === 'string' ? t.name
      : (typeof t === 'string' ? t : '');
    if (name === '') continue;
    const pv = parseVersion(name);
    if (!pv.ok) continue;
    if (!best || compareVersions(name, best.raw) > 0) {
      best = { raw: name, version: pv.seg.join('.') + (pv.pre ? '-' + pv.pre.join('.') : '') };
    }
  }
  return best;
}

// 默认目标目录：<workspaceRoot>/dsh-prompt-enhancer-<tag>/；workspaceRoot 缺失 → ''
// 下载统一走 GitHub contents API（§9-T6 实测回填：本机 raw.githubusercontent.com 被 DNS 屏蔽 0.0.0.0，
// api.github.com 浏览器 CORS 可直连；host 不再出网，URL 由 client 构建）
function defaultDirFor(workspaceRoot, tag) {
  if (typeof workspaceRoot !== 'string' || workspaceRoot.trim() === '') return '';
  return workspaceRoot.replace(/[\\/]+$/, '') + '/' + 'dsh-prompt-enhancer-' + tag;
}

// tags/releases 载荷解析（客户端浏览器 fetch 得到原始 JSON 文本，host 侧解析）
// 返回数组或 null（非 JSON / 非数组）
function parseTagsPayload(payload) {
  if (typeof payload !== 'string' || payload === '') return null;
  try {
    const data = JSON.parse(payload);
    return Array.isArray(data) ? data : null;
  } catch (e) {
    return null;
  }
}

// 拉取文件清单校验（update/pull 入参）：必须恰好覆盖 UPDATE_MANIFEST 全部 4 个文件，
// 无重复/无多余；content 为已解码文本且 ≤1MB。返回 { ok, files } | { ok:false, message }
function validateManifestFiles(files) {
  if (!Array.isArray(files)) return { ok: false, message: 'files array required' };
  const seen = new Set();
  const out = [];
  for (const f of files) {
    if (!f || typeof f !== 'object') return { ok: false, message: 'malformed file entry' };
    const name = typeof f.name === 'string' ? f.name : '';
    const content = typeof f.content === 'string' ? f.content : '';
    if (!UPDATE_MANIFEST.includes(name)) return { ok: false, message: 'unexpected file: ' + name };
    if (seen.has(name)) return { ok: false, message: 'duplicate file: ' + name };
    if (content.length > 1000000) return { ok: false, message: 'file too large: ' + name };
    seen.add(name);
    out.push({ name, content });
  }
  for (const name of UPDATE_MANIFEST) {
    if (!seen.has(name)) return { ok: false, message: 'missing file: ' + name };
  }
  return { ok: true, files: out };
}

// ================= v2.5.0 一键更新并重启 · 纯函数族 =================
// 方案「一键更新并重启方案.md」：命令构造与探测计划保持 PURE（可单测、单一事实源）。
// lib/index.cjs 会切片本区段复用 mergeEnvPath（注入用户 PATH 用），勿在此引入
// ctx/harness/module 状态。

// 环境探测计划（key 与 lib/index.cjs probeEnv 返回的 key 一一对应；
// level 供 client 渲染与一键更新前置校验：block=硬阻断 / warn=风险提示。
// v2.5.2 收敛：移除安装链与形态类检查（net/mode/pnpmInfo）。
// v2.7.0 收敛：仅保留执行器重启阶段真实依赖——account（与 sc start 无关）、
// restart（KillProcessTree 不影响独立执行器）与 port 占用（标准场景不可达，
// no-port 并入 exec-port）已删；新增 tools（重启命令工具）与 svc-bin 降级链。）
const ENV_PROBE_KEYS = [
  // v3.2.1-v（用户审核·去重收敛）：service/svc-type/svc-bin 三项信息已被 port-mode 状态机覆盖——
  //   service↔监听者会话0、svc-type↔是否在跑、svc-bin↔能否接管——全部由 port-mode 的
  //   service/service-stopped/default/no-listener 四态表达，删除三项避免重复检测。
  { key: 'tools', level: 'block' },    // 重启链系统工具可用（sc/netstat/reg）
  { key: 'net', level: 'warn' },       // GitHub 可达性（安装依赖，v2.7.1 恢复）
  { key: 'port-mode', level: 'warn' }, // v3.2.1-o：端口托管模式（nssm 服务会话0/前台默认/无监听）
  { key: 'port-pid', level: 'warn' },  // v3.2.1-o：3080 实际监听者 PID
];

// 安装命令构造：node <dshBin> plugin --profile <profile> add github:Fishsb/dsh-prompt-enhancer#<tag>
// dshBin 由 lib/index.cjs 从 process.argv[1] 注入（host 沙箱无 process）；
// 参数级白名单校验在 lib 层 isInstallArgs 二次把关。
function buildInstallArgs(dshBin, tag, profile) {
  return [dshBin, 'plugin', '--profile', profile, 'add', 'github:Fishsb/dsh-prompt-enhancer#' + tag];
}

// GitHub Release 资产下载地址（npm pack 产物，staging 阶段在线拉取，服务不中断）。
// 注意：不能用 codeload 源码 tar.gz——pnpm add 本地包需要 npm pack 格式（顶层 package/）。
function buildTarballUrl(repo, tag) {
  const ver = String(tag).replace(/^v/i, '');
  return 'https://github.com/' + repo + '/releases/download/' + tag + '/dsh-prompt-enhancer-' + ver + '.tgz';
}

// 本地 staging tarball 安装命令：node <dshBin> plugin --profile <profile> add <local.tgz>
// lib 层 isLocalTarballInstallArgs 会二次校验 tarball 必须在 staging 目录内。
function buildLocalInstallArgs(dshBin, profile, tarball) {
  return [dshBin, 'plugin', '--profile', profile, 'add', tarball];
}

// 重启计划构造（v2.6.0：独立执行器使用——不再拼接 cmd 链，改参数对象；
// 执行器内以 node setTimeout 可靠等待 + 端口健康检查 + 自动重试。
// v2.5.4 教训：cmd 链的 timeout 在非交互环境（stdio ignore）立即返回，缓冲从未生效。）
function buildRestartPlan(serviceName, port, maxAttempts) {
  return { serviceName, port, maxAttempts };
}

// PATH 合并（系统 PATH + 用户 PATH，大小写不敏感去重，保留顺序）；空段忽略
function mergeEnvPath(sysPath, userPath) {
  const seen = new Set();
  const out = [];
  for (const seg of String(sysPath + ';' + userPath).split(';')) {
    if (seg === '') continue;
    const key = seg.toLowerCase();
    if (seen.has(key)) continue;
    seen.add(key);
    out.push(seg);
  }
  return out.join(';');
}


// ================= v3.0 模式重构 · 会话轮次窗口与关联判定 =================

// 会话轮次窗口切分：events 时间序（extractHistory 输出 [{type:'user'|'assistant', text}]），
// 以 user 消息为轮锚点（轮 = 一个 user 消息及其后的 assistant 回复）；fromRound/toRound
// 1-based（1 = 最近一轮）。返回窗口内消息文本（[用户]/[助手] 前缀）；窗口越界/无锚点
// 时按可用轮数 clamp，不可用 → []。
function splitHistoryRounds(events, fromRound, toRound) {
  const list = Array.isArray(events) ? events.filter((e) => e && typeof e.text === 'string') : [];
  if (list.length === 0) return [];
  const from = Number.isInteger(fromRound) && fromRound >= 1 ? fromRound : 1;
  const to = Number.isInteger(toRound) && toRound >= from ? toRound : from;
  const anchors = [];
  for (let i = list.length - 1; i >= 0; i--) {
    if (list[i].type === 'user') anchors.push(i);
  }
  const fmt = (e) => (e.type === 'user' ? '[用户] ' : '[助手] ') + e.text;
  if (anchors.length === 0) {
    const start = Math.max(0, list.length - to);
    const end = Math.max(start, list.length - from + 1);
    return list.slice(start, end).map(fmt);
  }
  if (from > anchors.length) return [];
  const toEff = Math.min(to, anchors.length);
  const startIdx = anchors[toEff - 1];
  const endIdx = from === 1 ? list.length : anchors[from - 2];
  if (endIdx <= startIdx) return [];
  return list.slice(startIdx, endIdx).map(fmt);
}

// 关联判定 JSON 容错解析（{related, reason}）——剥离 json 代码块与前后缀噪音，
// related 归一化（true/'true'/1 → true）；不可解析 → null（调用方按不相关降级）。
function parseRelevance(raw) {
  if (typeof raw !== 'string') return null;
  let s = raw.trim();
  const fence = s.match(/```(?:json)?\s*([\s\S]*?)```/);
  if (fence) s = fence[1].trim();
  const start = s.indexOf('{');
  const end = s.lastIndexOf('}');
  if (start === -1 || end <= start) return null;
  s = s.slice(start, end + 1);
  let obj;
  try { obj = JSON.parse(s); } catch (e) { return null; }
  if (!obj || typeof obj !== 'object') return null;
  const related = obj.related === true || obj.related === 'true' || obj.related === 1 || obj.related === '1';
  return { related, reason: typeof obj.reason === 'string' ? obj.reason.slice(0, 80) : '' };
}

// ================= v3.0 模式重构 · 检索策略表 =================
// kind: none（不检索）/ rounds（会话轮次窗口 LLM 关联判定，命中即停）/ v2（旧管道：文件/事件/网络）
// windows: [from, to] 1-based 轮次窗口（1 = 最近一轮；轮 = user 消息锚点及其回复）
// v3.2.23（技能集合化）：检索策略声明事实源已迁移到 skills/enhance/*/SKILL.md frontmatter
// （SKILL_MANIFEST 生成，enhance-handlers 已改读 SKILL_MANIFEST）；此处保留仅供 PURE 区段
// 单测切片 + U40c 一致性断言防漂移——改检索策略请改 SKILL.md，勿在此手改。
const RETRIEVE_TABLE = {
  base: { kind: 'none', windows: [] },
  lite: { kind: 'rounds', windows: [[1, 1]] },
  standard: { kind: 'rounds', windows: [[1, 2], [3, 5], [6, 10]] },
  smart: { kind: 'rounds', windows: [[1, 1], [2, 3]] },
  publish: { kind: 'v2', windows: [] },
};
// v3.0：关联判定调用预算（独立小调用，失败降级不阻断主流程）
const RELEVANCE_TIMEOUT_MS = 10000;
const RELEVANCE_MAX_TOKENS = 400;
const RELEVANCE_OUTPUT_LIMIT = 800;

// v3.0（模式重构 S3）：开发环境判定 JSON 容错解析（{isDevEnv, reason}）——同 parseRelevance 模式。
// v3.0v2（修订）：提示词开发意向判定 JSON 容错解析（{isDevIntent, reason}）。
function parseIntent(raw) {
  if (typeof raw !== 'string') return null;
  let s = raw.trim();
  const fence = s.match(/```(?:json)?\s*([\s\S]*?)```/);
  if (fence) s = fence[1].trim();
  const start = s.indexOf('{');
  const end = s.lastIndexOf('}');
  if (start === -1 || end <= start) return null;
  s = s.slice(start, end + 1);
  let obj;
  try { obj = JSON.parse(s); } catch (e) { return null; }
  if (!obj || typeof obj !== 'object') return null;
  const isDevIntent = obj.isDevIntent === true || obj.isDevIntent === 'true' || obj.isDevIntent === 1 || obj.isDevIntent === '1';
  return { isDevIntent, reason: typeof obj.reason === 'string' ? obj.reason.slice(0, 80) : '' };
}

// v3.0v2（修订）：文档检索/项目地图合并分析 JSON 容错解析
// （{relatedDocs:[{path,excerpt}], hasProjectMap, codePaths:[], reason}）。
function parseDocsAnalysis(raw) {
  if (typeof raw !== 'string') return null;
  let s = raw.trim();
  const fence = s.match(/```(?:json)?\s*([\s\S]*?)```/);
  if (fence) s = fence[1].trim();
  const start = s.indexOf('{');
  const end = s.lastIndexOf('}');
  if (start === -1 || end <= start) return null;
  s = s.slice(start, end + 1);
  let obj;
  try { obj = JSON.parse(s); } catch (e) { return null; }
  if (!obj || typeof obj !== 'object') return null;
  const relatedDocs = Array.isArray(obj.relatedDocs) ? obj.relatedDocs.slice(0, 3).map((d) => ({
    path: String(d && d.path || '').slice(0, 200),
    excerpt: String(d && d.excerpt || '').slice(0, 240),
  })).filter((d) => d.path !== '') : [];
  const codePaths = Array.isArray(obj.codePaths) ? obj.codePaths.map((p) => String(p || '').replace(/^\/+|\/+$/g, '')).filter((p) => p !== '').slice(0, 5) : [];
  const hasProjectMap = obj.hasProjectMap === true || obj.hasProjectMap === 'true' || obj.hasProjectMap === 1 || obj.hasProjectMap === '1';
  return { relatedDocs, hasProjectMap, codePaths, reason: typeof obj.reason === 'string' ? obj.reason.slice(0, 100) : '' };
}

// v3.0p（publish 多步检索）：检索主题规划 JSON 容错解析
// （{topics:[{query,note}]}，2~3 个差异化检索主题）——同 parseDocsAnalysis 模式。
function parseSearchPlan(raw) {
  if (typeof raw !== 'string') return null;
  let s = raw.trim();
  const fence = s.match(/```(?:json)?\s*([\s\S]*?)```/);
  if (fence) s = fence[1].trim();
  const start = s.indexOf('{');
  const end = s.lastIndexOf('}');
  if (start === -1 || end <= start) return null;
  s = s.slice(start, end + 1);
  let obj;
  try { obj = JSON.parse(s); } catch (e) { return null; }
  if (!obj || typeof obj !== 'object') return null;
  const topics = Array.isArray(obj.topics) ? obj.topics.map((t) => ({
    query: String(t && t.query || '').trim().slice(0, 40),
    note: String(t && t.note || '').slice(0, 120),
  })).filter((t) => t.query !== '') : [];
  if (topics.length === 0) return null;
  return { topics: topics.slice(0, 3) };
}
// ==PURE-END==

// ================= V2 上下文感知优化 · 运行时（阶段 A/B/C） =================
// v2.0.0 方案 §3：阶段 A 任务进度（smart LLM / basic 规则）→ 阶段 B 相关性检索
// （工作区文件 + 会话事件）→ 阶段 C 预算组装注入。各阶段独立降级，不阻断优化。
// v2.4.6：TASK_ANALYSIS_PROMPT / CONTEXT_GUARD 已外置 prompts/*.md（见生成标记区）。

const V2_WORKSPACE_IGNORE_DIRS = new Set(['node_modules', '.git', 'dist', 'build', '.venv', 'venv', '__pycache__', '.next', '.cache', 'coverage', 'target', '.idea', '.vscode']);

// 阶段 A：任务进度理解（表驱动 phaseA：none 跳过 / rule 正则 / llm 智能（失败降级 rule）/ memory 读记忆对）
// 返回值：{ progress, mode }；mode: 'smart'|'rule'|'memory'|'none'
async function v2ResolveProgress(services, historyText, cfg) {
  const row = MODE_TABLE[cfg.mode] || MODE_TABLE[DEFAULT_MODE];
  if (row.phaseA === 'llm' && services.llm && services.chain && services.chain.length > 0 && historyText.trim() !== '') {
    const entry = services.chain[0];
    let timedOut = false;
    const timer = services.timer.timeout(() => { timedOut = true; }, V2_PROGRESS_TIMEOUT_MS);
    try {
      const stream = services.llm.stream({
        provider: entry.provider,
        model: entry.model,
        // v3.0p 审查（超时/耗时匹配）：阶段 A 任务分析剥离 reasoningEffort（同 judge 策略）
        maxTokens: V2_PROGRESS_MAX_TOKENS,
        system: TASK_ANALYSIS_PROMPT,
        messages: [{
          id: 'enhance-task-progress',
          role: 'user',
          content: [{ type: 'text', text: historyText.slice(0, V2_HISTORY_MAX_CHARS) }],
          source: { kind: 'user' },
        }],
      });
      const iterator = stream[Symbol.asyncIterator]();
      const result = await collectStream(iterator, V2_PROGRESS_OUTPUT_LIMIT);
      if (!timedOut && result.kind === 'ok') {
        const parsed = parseTaskProgress(result.text);
        if (parsed) {
          hlog('[enhance] v2 smart progress ok');
          return { progress: parsed, mode: 'smart' };
        }
        hlog('[enhance] v2 smart progress bad-json');
      } else {
        hlog('[enhance] v2 smart progress ' + (timedOut ? 'timeout' : 'empty'));
      }
    } catch (e) {
      hlog('[enhance] v2 smart progress failed', e && e.message ? e.message : e);
    } finally {
      timer();
    }
  }
  // rule 规则提取（零成本；无历史时返回空 focus）——phaseA: rule / llm 降级 / memory 无记忆时兜底
  const focus = inferFocusRules(historyText);
  if (focus.length > 0) return { progress: { focus }, mode: 'rule' };
  return { progress: null, mode: 'rule' };
}

// 阶段 B：工作区文件检索（fs 扫描 + 名称/内容命中 → Top-3 摘要；2s 超时降级）
// v2.0.7：fs 契约修正——listDir/readText 接收 FsTarget（resolve 产出），条目 shape 为
// {name, type:'file'|'directory', target}；不再传字符串路径。
async function v2SearchWorkspace(services, keywords, cfg) {
  const fsSvc = services.fs;
  const root = services.sandboxPolicy && services.sandboxPolicy.workspaceRoot;
  if (!fsSvc || !root || typeof fsSvc.listDir !== 'function' || typeof fsSvc.readText !== 'function' || typeof fsSvc.resolve !== 'function') return [];
  // 表驱动：phaseB 非 file+event 跳过；扫描上限按 scanLimit（by-budget 查联动表 / fixed 固定）
  const row = MODE_TABLE[cfg.mode] || MODE_TABLE[DEFAULT_MODE];
  if (row.phaseB !== 'file+event') return [];
  const lim = resolveScanLimit(cfg.mode, cfg.context.budgetChars);
  const depth = lim.depth;
  const maxFiles = lim.maxFiles;
  let aborted = false;
  const timer = services.timer.timeout(() => { aborted = true; }, V2_WORKSPACE_TIMEOUT_MS);
  try {
    let rootTarget;
    try {
      rootTarget = await fsSvc.resolve(root);
    } catch (e) {
      hlog('[enhance] v2 workspace resolve-root failed', e && e.message ? e.message : e);
      return [];
    }
    const files = [];
    const walk = async (target, rel, level) => {
      if (aborted || files.length >= SCAN_FILE_LIST_MAX || level > depth) return;
      let entries;
      try {
        entries = await fsSvc.listDir(target);
      } catch (e) { return; }
      for (const en of entries || []) {
        if (aborted) return;
        const name = en && en.name;
        if (!name) continue;
        if (V2_WORKSPACE_IGNORE_DIRS.has(name)) continue;
        const relPath = rel ? rel + '/' + name : name;
        if (en.type === 'directory') {
          await walk(en.target, relPath, level + 1);
        } else if (en.type === 'file') {
          files.push(relPath);
        }
      }
    };
    await walk(rootTarget, '', 1);
    if (aborted) { hlog('[enhance] v2 workspace scan timeout'); return []; }
    hlog('[enhance] v2 workspace scanned files=' + files.length);
    // 名称匹配 → 候选；v2.7.0：名称 0 命中时内容兜底（中文场景名称匹配几乎必然落空——
    // 中文关键词 vs 英文文件名；降级取前 N 个文本文件按内容关键词命中，救活
    // 「文件内容相关但文件名无关」场景；读取受 V2_WORKSPACE_TIMEOUT_MS 总超时保护）
    const candidates = rankFiles(files, keywords, 10).map((c) => c.path);
    if (candidates.length === 0) {
      const textRe = /\.(?:md|txt|py|ts|js|tsx|jsx|json|yaml|yml|toml|css|html|go|rs|java|cpp|c|h|sh|sql|vue)$/i;
      // 文档类（md/txt）优先——描述性内容最可能命中主题词
      const docFirst = files.slice().sort((a, b) => {
        const da = /\.(?:md|txt)$/i.test(a) ? 0 : 1;
        const db = /\.(?:md|txt)$/i.test(b) ? 0 : 1;
        return da - db;
      });
      const kws = (keywords || []).filter((k) => typeof k === 'string' && k.length >= 2);
      const contentScored = [];
      let scanned = 0;
      for (const rel of docFirst) {
        if (aborted || scanned >= CONTENT_FALLBACK_SCAN) break;
        if (!textRe.test(rel) || shouldIgnoreFile(rel)) continue;
        scanned++;
        let text = '';
        try {
          const target = await fsSvc.resolve(rel, { cwd: root });
          text = await fsSvc.readText(target);
        } catch (e) { continue; } // 只读权限/读取失败 → 跳过该文件
        const lines = text.split('\n');
        let contentHits = 0;
        for (const ln of lines) {
          if (kws.some((k) => ln.toLowerCase().includes(k.toLowerCase()))) contentHits++;
          if (contentHits >= 8) break;
        }
        if (contentHits > 0) contentScored.push({ path: rel, lines, contentHits });
      }
      hlog('[enhance] v2 workspace content-fallback scanned=' + scanned + ' hits=' + contentScored.length + ' kws=' + JSON.stringify(keywords));
      if (contentScored.length === 0) {
        hlog('[enhance] v2 workspace no-name-match files=' + files.length + ' kws=' + JSON.stringify(keywords));
        return [];
      }
      contentScored.sort((a, b) => b.contentHits - a.contentHits);
      const top = contentScored.slice(0, maxFiles);
      return top.map((f) => ({ path: f.path, snippet: snippetFromLines(f.lines, keywords, SNIPPET_BUDGET) }));
    }
    // 内容命中加分 → 排序 Top-maxFiles
    const scored = [];
    for (const rel of candidates) {
      if (aborted) break;
      if (shouldIgnoreFile(rel)) continue;
      let text = '';
      try {
        const target = await fsSvc.resolve(rel, { cwd: root });
        text = await fsSvc.readText(target);
      } catch (e) { continue; } // 只读权限/读取失败 → 跳过该文件
      const lines = text.split('\n');
      let contentHits = 0;
      const kws = (keywords || []).filter((k) => typeof k === 'string' && k.length >= 2);
      for (const ln of lines) {
        if (kws.some((k) => ln.toLowerCase().includes(k.toLowerCase()))) contentHits++;
        if (contentHits >= 8) break;
      }
      if (contentHits > 0) scored.push({ path: rel, lines, contentHits });
    }
    if (scored.length === 0) {
      hlog('[enhance] v2 workspace no-content-match candidates=' + candidates.length + ' kws=' + JSON.stringify(keywords));
      return [];
    }
    scored.sort((a, b) => b.contentHits - a.contentHits);
    const top = scored.slice(0, maxFiles);
    return top.map((f) => ({ path: f.path, snippet: snippetFromLines(f.lines, keywords, SNIPPET_BUDGET) }));
  } finally {
    timer();
  }
}

// 阶段 B3：会话事件检索（增强；searchEvents 契约：{sessionId,query,limit} → {items:[{snippet}]}；失败跳过）
async function v2SearchEvents(services, sessionId, keywords, cfg) {
  const sq = services.sessionQuery;
  const kws = (keywords || []).filter((k) => typeof k === 'string' && k.trim() !== '');
  // 表驱动：phaseB 非 file+event 跳过
  const row = MODE_TABLE[cfg.mode] || MODE_TABLE[DEFAULT_MODE];
  if (row.phaseB !== 'file+event') return [];
  if (!sq || typeof sq.searchEvents !== 'function' || kws.length === 0) {
    hlog('[enhance] v2 searchEvents skipped kws=' + kws.length);
    return [];
  }
  // v3.0p 审查（超时/耗时匹配）：searchEvents 补超时保护（10s，超时/失败 → [] 不阻断）
  let timedOut = false;
  const timer = services.timer.timeout(() => { timedOut = true; }, V2_SEARCH_EVENTS_TIMEOUT_MS);
  try {
    const page = await sq.searchEvents({ sessionId, query: kws.join(' '), limit: 3 });
    if (timedOut) { hlog('[enhance] v2 searchEvents timeout'); return []; }
    const hits = page && Array.isArray(page.items) ? page.items : [];
    hlog('[enhance] v2 searchEvents kws=' + JSON.stringify(kws) + ' hits=' + hits.length);
    if (hits.length === 0) return [];
    return hits.slice(0, 3).map((h) => {
      const txt = h && (typeof h.snippet === 'string' ? h.snippet : (typeof h.text === 'string' ? h.text : ''));
      return txt ? txt.slice(0, 300) : '';
    }).filter(Boolean);
  } catch (e) {
    hlog('[enhance] v2 searchEvents failed', e && e.message ? e.message : e);
    return []; // 服务缺失/请求形状不符/失败 → 跳过事件段
  } finally {
    timer();
  }
}

// 阶段 A/B/C 汇总：返回 { block, log }（全部不可用 → { block: '', log: 'none' }）
// v2.2（§6.5）：4 模式管道（base/lite 空块 / standard/smart 检索）。
// v2.6.1（记忆链）：记忆不再作为文本块叠加（改由 enhance 入口以真多轮消息注入，
// 见 buildChatMessages），模式块独享预算；log 仅含模式块口径（记忆见入口 memory 日志）。
// v2.3（§7.3）：onStage 回调（由 enhance handler 注入，写 pending 记录的 stage 字段；
// 纯函数本体不接触模块状态，回调缺省为 no-op 保持 PURE 区段可切片）。
async function buildV2ContextBlock(services, sessionId, text, cfg, onStage) {
  const mark = typeof onStage === 'function' ? onStage : () => {};
  const row = MODE_TABLE[cfg.mode] || MODE_TABLE[DEFAULT_MODE];
  const budget = cfg.context.budgetChars || 0;
  // ===== 模式管道（base/lite：phaseB='none' 无检索 → 空模式块）=====
  let modeBlock = '';
  let modeLog = 'none';
  // v3.0p（publish 多步检索）：web 段结果透出（跨轮记忆用；无检索时为空）
  let webTopics = [];
  let webMemoText = '';
  if (row.phaseC === 'inject') {
    // 历史 + 阶段A + 阶段B
    let events = [];
    let historyText = '';
    const sq = services.sessionQuery;
    if (sq && typeof sq.listEvents === 'function' && typeof sq.filterEvents === 'function') {
      try {
        mark(STAGE_HISTORY);
        // v3.1.3（用户需求·仅结论参考）：同 v3 路径——readSurface 块级结论提取，旧路径兜底
        if (typeof sq.readSurface === 'function') {
          const surface = await sq.readSurface(sessionId);
          events = extractHistoryConclusions(surface && surface.events);
        } else {
          const records = await sq.listEvents(sessionId);
          // 尾部反向找最近的消息事件 seq（listEvents 升序，无文本；seq 用于 filterEvents 范围过滤）
          const msgSeqs = [];
          for (let i = records.length - 1; i >= 0 && msgSeqs.length < V2_MSG_SEQ_SCAN; i--) {
            const r = records[i];
            const t = String(r && r.type || '');
            if (t === 'user/message' || t === 'assistant/message') msgSeqs.push(r && r.seq);
          }
          if (msgSeqs.length > 0) {
            const minSeq = msgSeqs[msgSeqs.length - 1];
            const docs = await sq.filterEvents(sessionId, [{ kind: 'seq', from: minSeq }]);
            events = extractHistory(docs, V2_HISTORY_LIMIT);
          }
        }
        historyText = events.map((e) => (e.type === 'user' ? '[用户] ' : '[助手] ') + e.text).join('\n');
        hlog('[enhance] v2 history events=' + events.length + ' chars=' + historyText.length + (typeof sq.readSurface === 'function' ? ' conclusion-only' : ''));
      } catch (e) {
        hlog('[enhance] v2 history failed', e && e.message ? e.message : e);
      }
    } else {
      hlog('[enhance] v2 sessionQuery unavailable');
    }
    const root = services.sandboxPolicy && services.sandboxPolicy.workspaceRoot;
    hlog('[enhance] v2 workspaceRoot=' + (root || '(none)') + ' fs=' + (services.fs ? 'yes' : 'no'));
    // 阶段 A（表驱动 phaseA：llm 智能 / rule 正则 / none 跳过）
    mark(STAGE_ANALYZE);
    const { progress, mode } = await v2ResolveProgress(services, historyText, cfg);
    // 阶段 B（表驱动 phaseB：file+event 全量 / none 跳过）
    const focus = progress && Array.isArray(progress.focus) ? progress.focus : [];
    const keywords = extractKeywords(text, focus);
    mark(STAGE_FILES, { key: 'scan' });
    const files = await v2SearchWorkspace(services, keywords, cfg);
    mark(STAGE_EVENTS);
    const eventsHits = await v2SearchEvents(services, sessionId, keywords, cfg);
    // 阶段 C（inject）：模式块独享预算（记忆链不占文本块）
    modeBlock = buildContextBlock(progress, files, eventsHits, budget);
    // v2.7.0（一键发布 · 网络检索）：publish 专属——依据草稿主题词 + 记忆链 delta
    // 改动方向构造检索词，经 ctx.web 搜索同类项目结构参考，注入模式块（预算余量内）。
    // 独立超时/降级：搜索失败/服务缺失 → 跳过，不阻断规格生成。
    let webLog = 'none';
    if (cfg.mode === 'publish' && budget > 0) {
      mark(STAGE_FILES); // 复用 files 阶段标记（检索类）
      // v3.0p（publish 多步检索）：跨轮要点回注——历史【已获取网络参考】前置（预算内），
      // 让模型「记住」此前检索到的信息，只补新缺口（配合 publishWebMemo 会话级记忆）
      if (services.webMemo) {
        const memoBlock = '【已获取网络参考】\n' + String(services.webMemo).slice(0, WEB_MEMO_MAX);
        modeBlock = modeBlock ? modeBlock + '\n\n' + memoBlock : memoBlock;
      }
      // v3.0p（publish 多步检索）：LLM 规划的差异化主题逐主题搜索（services.webPlan 非空时）；
      // 无规划 / 规划失败 → 降级旧单 query 路径（buildWebQuery + 场景方向词，行为不变）
      const web = services.web;
      const planQueries = Array.isArray(services.webPlan) && services.webPlan.length > 0
        ? services.webPlan.map((t) => String(t && t.query || '').trim()).filter((q) => q !== '').slice(0, 3)
        : [];
      const queries = planQueries.length > 0 ? planQueries : [null];
      const topicLogs = [];
      if (web && typeof web.search === 'function') {
        const refParts = [];
        const memoParts = [];
        for (let qi = 0; qi < queries.length; qi++) {
          const q = queries[qi];
          let query = q;
          if (query === null) {
            query = buildWebQuery(text, keywords, services.delta || null);
            if (services.scenario === 'game') query = (query ? query + ' 游戏实现' : '游戏实现');
            else if (services.scenario === 'software') query = (query ? query + ' 软件架构' : '软件架构');
          }
          // v3.0r（细粒度进度反馈）：逐主题搜索进度（detailKey=search，client 渲染「正在搜索 i/n：query」）
          mark(STAGE_FILES, { key: 'search', args: { query }, step: qi + 1, total: queries.length });
          let timedOut = false;
          const timer = services.timer.timeout(() => { timedOut = true; }, WEB_SEARCH_TIMEOUT_MS);
          try {
            const res = await web.search({ query, maxResults: WEB_SEARCH_MAX_RESULTS });
            if (!timedOut && res && Array.isArray(res.sources) && res.sources.length > 0) {
              const lines = res.sources.slice(0, WEB_SEARCH_MAX_RESULTS).map((s) => {
                const title = s && s.title ? String(s.title) : '';
                const url = s && s.url ? String(s.url) : '';
                const summary = s && s.summary ? String(s.summary).slice(0, 200) : '';
                return '- ' + title + (url ? ' (' + url + ')' : '') + (summary ? '\n  ' + summary : '');
              }).join('\n');
              refParts.push('### 检索主题：' + query + '\n' + lines);
              memoParts.push('『' + query + '』：' + res.sources.slice(0, 2).map((s) => String(s && s.title || '') + (s && s.summary ? '——' + String(s.summary).slice(0, 60) : '')).join('；'));
              topicLogs.push({ query, hits: res.sources.length });
            } else {
              topicLogs.push({ query, hits: timedOut ? -2 : 0 });
            }
          } catch (e) {
            topicLogs.push({ query, hits: -1 });
          } finally {
            timer();
          }
        }
        const refText = refParts.join('\n\n');
        if (refText) {
          const webBlock = '【网络参考】\n' + refText.slice(0, Math.min(WEB_REF_MAX, Math.max(0, budget - modeBlock.length)));
          if (webBlock.length > 20) {
            modeBlock = modeBlock ? modeBlock + '\n\n' + webBlock : webBlock;
            webLog = 'web=' + topicLogs.filter((t) => t.hits > 0).length + '/' + topicLogs.length + ' chars=' + webBlock.length;
          } else {
            webLog = 'web=0';
          }
        } else {
          webLog = 'web=0';
        }
        webTopics = topicLogs;
        webMemoText = memoParts.join('\n').slice(0, WEB_MEMO_MAX);
      } else {
        webLog = 'web=none';
      }
      hlog('[enhance] v2 web ' + webLog + ' queries=' + topicLogs.map((t) => t.query + ':' + t.hits).join('|').slice(0, 200));
    }    modeLog = modeBlock === '' ? 'none' : (mode + ' files=' + files.length + ' events=' + eventsHits.length + (webLog !== 'none' ? ' ' + webLog : '') + ' chars=' + modeBlock.length);
  }
  // ===== 汇总（记忆链由 enhance 入口以多轮消息注入，不占文本块）=====
  mark(STAGE_CONTEXT);
  return { block: modeBlock, log: modeBlock === '' ? 'none' : modeLog, webTopics, webMemoText };
}

function selfState(reference) {
  const status = reference.latestRun && reference.latestRun.status;
  if (status === 'awaiting-approval') return 'awaiting-approval';
  if (status === 'client-pending' || status === 'starting-host') return 'client-pending';
  if (status === 'failed' || status === 'rejected' || status === 'cancelled') return 'failed';
  if (status === 'waiting') return 'waiting';
  if (status === 'running') return 'running';
  if (reference.activeRun !== undefined) return 'running';
  return reference.currentPackageId === undefined ? 'defined' : 'stopped';
}

function summarize(reference) {
  const latest = reference.latestRun;
  const state = selfState(reference);
  return {
    pluginId: String(reference.pluginId),
    name: reference.name,
    state,
    // v2.4.3-fix：透传每包半部完整性——client 版本下拉据此禁用残缺包（防误切致 UI 消失）
    ...(reference.packages ? { packages: reference.packages.map((p) => ({ packageId: String(p.packageId), name: p.name, purpose: p.purpose || '', hasHostHalf: p.hasHostHalf === true, hasClientHalf: p.hasClientHalf === true })) } : { packages: [] }),
    ...(reference.currentPackageId === undefined ? {} : { currentPackageId: String(reference.currentPackageId) }),
    ...(reference.nextPackageId === undefined ? {} : { nextPackageId: String(reference.nextPackageId) }),
    ...(reference.activeRun === undefined ? {} : { activeRun: { pluginRunId: String(reference.activeRun.pluginRunId), packageId: String(reference.activeRun.packageId) } }),
    ...(latest && latest.status === 'awaiting-approval' ? { pendingApproval: { pluginRunId: String(latest.pluginRunId), packageId: String(latest.packageId), mode: latest.mode } } : {}),
  };
}

return {
  // #3 修复（2026-08-21）：llm 服务声明为 inject 依赖，消除启动竞态下 ctx.get('llm') 为 undefined → enhance NO_LLM
  inject: ['llm', 'timer'],
  apply(ctx) {
    const llm = ctx.get('llm');
    const pending = new Map();
    // v2.8.0（一键发布 · 场景路由）：会话级场景缓存（sessionId → game/software/generic）。
    // 首轮判定写入、后续轮读取不重判（场景决定九章骨架，翻转会推翻已确认设计）；
    // 生命周期 = 插件进程，进程重启后缓存 miss → 用记忆链最早轮输入兜底判定（enhance 内判定源规则）
    const publishScenarioCache = new Map();
    // v3.0p（publish 多步检索）：会话级网络检索记忆（sessionId → {topics[], memo}）。
    // topics = 已检索主题（去重，≤WEB_MEMO_TOPICS_MAX）；memo = 已获取要点摘要（≤WEB_MEMO_MAX，
    // 跨轮以【已获取网络参考】回注，让模型记住已查信息、只补新缺口）。
    // 生命周期 = 插件进程（同 publishScenarioCache）。
    const publishWebMemo = new Map();

    function requestKey(sessionId, seq) {
      return String(sessionId) + ':' + String(seq);
    }

    function markAndAbort(key, flag) {
      const rec = pending.get(key);
      if (!rec) return;
      rec[flag] = true;
      if (rec.iterator && typeof rec.iterator.return === 'function') {
        try { rec.iterator.return(); } catch (e) { /* 忽略 */ }
      }
    }

    function resolveAgent(sessionId) {
      const agents = ctx.get('agents');
      if (!agents || typeof agents.get !== 'function') return null;
      return agents.get(sessionId) || null;
    }

    harness.handle('models/list', async () => {
      const llmService = ctx.get('llm');
      if (!llmService || typeof llmService.listProviders !== 'function') {
        return { ok: false, code: 'NO_LLM', message: 'llm service unavailable' };
      }
      try {
        const providers = llmService.listProviders();
        const out = [];
        for (const p of providers) {
          let models = [];
          try {
            const list = await llmService.listModels(p.id);
            models = (list || []).map((m) => ({ id: m.id, name: m.name || m.id }));
          } catch (e) { models = []; }
          out.push({ provider: p.id, name: p.name, models });
        }
        return { ok: true, providers: out };
      } catch (e) {
        herr('[enhance] models/list failed', e);
        return { ok: false, code: 'MODELS_FAILED', message: String(e && e.message ? e.message : e) };
      }
    });

    harness.handle('models/resolve', async (args) => {
      const provider = args && typeof args.provider === 'string' ? args.provider : '';
      const model = args && typeof args.model === 'string' ? args.model : '';
      const llmService = ctx.get('llm');
      if (!llmService || typeof llmService.resolveModelInfo !== 'function') {
        return { ok: false, code: 'NO_LLM', message: 'llm service unavailable' };
      }
      if (!provider || !model) return { ok: false, code: 'BAD_ARGS', message: 'provider and model required' };
      try {
        // v21（P1-4）：走 TTL 缓存，避免重复适配器能力查询
        // F1（配置卫生）：noCache 旁路 TTL——client effort 纠偏场景需要新鲜能力表
        const info = await resolveModelInfoCached(llmService, provider, model, !!(args && args.noCache === true));
        const reasoning = info && info.reasoning ? {
          efforts: info.reasoning.efforts.map((e) => ({
            id: String(e.id),
            name: e.name,
            ...(e.description ? { description: e.description } : {}),
          })),
          ...(info.reasoning.defaultEffort ? { defaultEffort: String(info.reasoning.defaultEffort) } : {}),
        } : undefined;
        return {
          ok: true,
          provider,
          model,
          ...(reasoning ? { reasoning } : {}),
          ...(info && info.context ? { context: info.context } : {}),
          ...(info && info.defaultMaxTokens ? { defaultMaxTokens: info.defaultMaxTokens } : {}),
        };
      } catch (e) {
        herr('[enhance] models/resolve failed', e);
        return { ok: false, code: 'RESOLVE_FAILED', message: String(e && e.message ? e.message : e) };
      }
    });

    harness.handle('models/current', async () => {
      const adm = ctx.get('agentDefaultModel');
      if (!adm || typeof adm.currentSelection !== 'function') {
        return { ok: false, code: 'NO_SERVICE', message: 'agentDefaultModel unavailable' };
      }
      try {
        const sel = adm.currentSelection();
        if (!sel || typeof sel.provider !== 'string' || typeof sel.model !== 'string' || !sel.provider || !sel.model) {
          return { ok: false, code: 'EMPTY', message: 'no current selection' };
        }
        return {
          ok: true,
          provider: sel.provider,
          model: sel.model,
          ...(typeof sel.reasoningEffort === 'string' && sel.reasoningEffort ? { reasoningEffort: sel.reasoningEffort } : {}),
        };
      } catch (e) {
        herr('[enhance] models/current failed', e);
        return { ok: false, code: 'CURRENT_FAILED', message: String(e && e.message ? e.message : e) };
      }
    });

    // v19：暴露自适应解析出的默认兜底链（供 client 首装继承 / 恢复默认使用，不再硬编码 provider）
    harness.handle('models/autochain', async (args) => {
      const llmSvc = ctx.get('llm');
      const adm = ctx.get('agentDefaultModel');
      try {
        // F8（配置卫生）：noCache 旁路 TTL——「恢复默认」始终取最新自适应链
        const chain = await resolveAdaptiveChain(llmSvc, adm, !!(args && args.noCache === true));
        return { ok: true, chain };
      } catch (e) {
        herr('[enhance] models/autochain failed', e);
        return { ok: false, code: 'AUTOCHAIN_FAILED', message: String(e && e.message ? e.message : e) };
      }
    });

    harness.handle('models/test', async (args) => {
      const provider = args && typeof args.provider === 'string' ? args.provider : '';
      const model = args && typeof args.model === 'string' ? args.model : '';
      const reasoningEffort = args && typeof args.reasoningEffort === 'string' && args.reasoningEffort !== '' ? args.reasoningEffort : undefined;
      const inputChars = args && typeof args.inputChars === 'number' && Number.isFinite(args.inputChars) ? args.inputChars : 0;
      const llmService = ctx.get('llm');
      if (!llmService || typeof llmService.stream !== 'function') {
        return { ok: false, code: 'NO_LLM', message: 'llm service unavailable' };
      }
      if (!provider || !model) return { ok: false, code: 'BAD_ARGS', message: 'provider and model required' };
      const entry = { provider, model, ...(reasoningEffort ? { reasoningEffort } : {}) };
      // 预校验：失败不阻断（目录 advisory，端点最终裁决；结果附 precheck 供展示）
      let precheck = null;
      if (llmService.resolveCallConfig) {
        try {
          await llmService.resolveCallConfig({ provider, model, ...(reasoningEffort ? { reasoningEffort } : {}), maxTokens: 1 });
        } catch (e) {
          precheck = { code: e && e.code ? String(e.code) : 'PRECHECK_FAILED', message: String(e && e.message ? e.message : e) };
        }
      }
      const ref = { current: null };
      let timedOut = false;
      // v3.1.7（方案 2）：探测改长文（~1200 token）后，真实吞吐 75–267 tok/s 需 4–16s + TTFT；
      // 15s 超时不够，放宽到 30s（最慢档 75 tok/s × 1200 token ≈ 16s，留足余量）。
      const timer = ctx.timer.timeout(() => {
        timedOut = true;
        if (ref.current && typeof ref.current.return === 'function') {
          try { ref.current.return(); } catch (e) { /* 忽略 */ }
        }
      }, 30000);
      let r;
      try {
        r = await pingStream(llmService, entry, ref);
      } finally {
        timer();
      }
      if (timedOut) {
        hlog('[enhance] test provider=' + provider + ' model=' + model + (reasoningEffort ? ' effort=' + reasoningEffort : '') + ' → timeout');
        return { ok: false, code: 'TIMEOUT', message: friendlyMessage({ code: 'TIMEOUT' }) };
      }
      hlog('[enhance] test provider=' + provider + ' model=' + model + (reasoningEffort ? ' effort=' + reasoningEffort : '') + ' → ' + (r.ok ? 'ok ' + r.latencyMs + 'ms' : r.code));
      if (r.ok && typeof r.tokensPerSecond === 'number' && Number.isFinite(r.tokensPerSecond) && typeof r.ttftMs === 'number' && Number.isFinite(r.ttftMs)) {
        r.estimatedBaseSeconds = estimateBaseModeSeconds(r.ttftMs, r.tokensPerSecond, inputChars);
        r.estimatedLiteSeconds = estimateLiteModeSeconds(r.ttftMs, r.tokensPerSecond, inputChars);
      }
      return { ...r, ...(precheck && r.ok ? { precheck } : {}) };
    });


    // 2026-08-17（连通性测试预计耗时）：按模型名实时聚合 DSH 会话投影统计。
    // 性能约束：持久化会话全量日志读取很重，不能每次/每个会话都 readSession。
    // 策略：live 会话每次走内存（快）；persisted 会话首次最多扫描 SCAN_LIMIT 个，
    // 命中后把 model route + projection 缓存，后续调用只聚合缓存，避免卡 UI。
    // 匹配口径：只按模型名匹配（用户要求），不区分 provider 路由。
    const sessionModelCache = new Map();        // sid -> { route, at }
    const sessionProjectionCache = new Map();   // sid -> { values, at }
    const SESSION_MODEL_TTL_MS = 600000;        // persisted route 缓存 10 分钟
    const SESSION_PROJECTION_TTL_MS = 600000;   // persisted projection 缓存 10 分钟
    const MODEL_STATS_SCAN_LIMIT = 5;           // 单次调用最多新读多少个 persisted 会话（5 个足够命中同模型历史，避免冷启动超时）

    function cachedSessionModel(sid) {
      const hit = sessionModelCache.get(sid);
      if (hit && Date.now() - hit.at < SESSION_MODEL_TTL_MS) return hit.route;
      return undefined;
    }

    async function loadPersistedSession(sq, proj, sid) {
      const snap = await sq.readSession(sid);
      const route = extractModelRouteFromEvents(snap && snap.events);
      sessionModelCache.set(sid, { route, at: Date.now() });
      let projection = null;
      try {
        const sessionLike = { seq: (snap.events || []).length, events: snap.events || [] };
        const pval = proj.snapshot(sessionLike);
        projection = pval && pval.values ? pval.values : {};
        sessionProjectionCache.set(sid, { values: projection, at: Date.now() });
      } catch (e) {
        projection = null;
      }
      return { route, projection };
    }

    async function resolveModelStats(provider, model) {
      const sq = ctx.get('sessionQuery');
      const proj = ctx.get('sessionProjections');
      if (!sq || typeof sq.listSessions !== 'function' || !proj || typeof proj.snapshot !== 'function') {
        return { ok: false, code: 'NO_STATS', message: 'session stats unavailable' };
      }
      const acc = { ttftMs: 0, ttftSteps: 0, decodeMs: 0, decodeTokens: 0, sessions: 0 };
      try {
        const records = await sq.listSessions();
        let scanBudget = MODEL_STATS_SCAN_LIMIT;
        for (const rec of records) {
          const sid = rec && rec.header && rec.header.id;
          if (!sid) continue;
          let route;
          let projection;
          if (rec.live === true) {
            // live 会话：直接走内存 Session，不落持久化缓存
            const sessions = ctx.get('sessions');
            const live = sessions && typeof sessions.get === 'function' ? sessions.get(sid) : undefined;
            if (!live) continue;
            route = extractModelRouteFromEvents(live.events);
            try {
              const pval = proj.snapshot(live);
              projection = pval && pval.values ? pval.values : {};
            } catch (e) {
              projection = null;
            }
          } else {
            const cachedRoute = cachedSessionModel(sid);
            if (cachedRoute !== undefined) {
              route = cachedRoute;
              const pc = sessionProjectionCache.get(sid);
              if (pc && Date.now() - pc.at < SESSION_PROJECTION_TTL_MS) projection = pc.values;
            } else if (scanBudget > 0) {
              scanBudget -= 1;
              try {
                const loaded = await loadPersistedSession(sq, proj, sid);
                route = loaded.route;
                projection = loaded.projection;
              } catch (e) {
                route = null;
                projection = null;
              }
            }
          }
          if (!route || route.model !== model) continue;
          if (projection) {
            // 按用户要求：只按模型名匹配，不区分 provider 路由
            accumulateProjectionStats(acc, { values: projection });
          }
        }
      } catch (e) {
        herr('[enhance] models/stats failed', e);
        return { ok: false, code: 'STATS_FAILED', message: String(e && e.message ? e.message : e) };
      }
      return { ok: true, ...summarizeModelStats(acc) };
    }

    harness.handle('models/stats', async (args) => {
      const provider = args && typeof args.provider === 'string' ? args.provider : '';
      const model = args && typeof args.model === 'string' ? args.model : '';
      const inputChars = args && typeof args.inputChars === 'number' && Number.isFinite(args.inputChars) ? args.inputChars : 0;
      if (!provider || !model) return { ok: false, code: 'BAD_ARGS', message: 'provider and model required' };
      const stats = await resolveModelStats(provider, model);
      if (!stats.ok) return stats;
      const estimatedBaseSeconds = estimateBaseModeSeconds(stats.ttftMs, stats.tokensPerSecond, inputChars);
      const estimatedLiteSeconds = estimateLiteModeSeconds(stats.ttftMs, stats.tokensPerSecond, inputChars);
      return { ...stats, estimatedBaseSeconds, estimatedLiteSeconds };
    });

    harness.handle('plugins/inventory', async (args) => {
      const sessionId = args && typeof args.sessionId === 'string' ? args.sessionId : '';
      const runner = ctx.get('dynamicCordisRunner');
      if (!runner || typeof runner.listPlugins !== 'function') {
        return { ok: false, code: 'NO_SERVICE', message: 'plugin runner unavailable' };
      }
      const agent = resolveAgent(sessionId);
      if (!agent) return { ok: false, code: 'NO_AGENT', message: 'session agent unavailable' };
      try {
        const plugins = runner.listPlugins(agent).map(summarize);
        return { ok: true, plugins };
      } catch (e) {
        herr('[enhance] inventory failed', e);
        return { ok: false, code: 'INVENTORY_FAILED', message: String(e && e.message ? e.message : e) };
      }
    });

    harness.handle('plugins/run', async (args) => {
      const sessionId = args && typeof args.sessionId === 'string' ? args.sessionId : '';
      const pluginId = args && typeof args.pluginId === 'string' ? args.pluginId : '';
      const packageId = args && typeof args.packageId === 'string' ? args.packageId : '';
      const mode = args && args.mode === 'update' ? 'update' : 'run';
      const runner = ctx.get('dynamicCordisRunner');
      if (!runner || typeof runner.run !== 'function') {
        return { ok: false, code: 'NO_SERVICE', message: 'plugin runner unavailable' };
      }
      const agent = resolveAgent(sessionId);
      if (!agent) return { ok: false, code: 'NO_AGENT', message: 'session agent unavailable' };
      try {
        const result = await runner.run(agent, pluginId, packageId, mode, undefined);
        if (result && result.ok) {
          return { ok: true, status: result.status, pluginRunId: result.pluginRunId || '' };
        }
        return { ok: false, code: result && result.reason ? result.reason : 'RUN_FAILED', message: result && result.message ? result.message : 'run failed' };
      } catch (e) {
        herr('[enhance] run failed', e);
        return { ok: false, code: 'RUN_FAILED', message: String(e && e.message ? e.message : e) };
      }
    });

    harness.handle('plugins/stop', async (args) => {
      const sessionId = args && typeof args.sessionId === 'string' ? args.sessionId : '';
      const pluginId = args && typeof args.pluginId === 'string' ? args.pluginId : '';
      const runner = ctx.get('dynamicCordisRunner');
      if (!runner || typeof runner.stop !== 'function') {
        return { ok: false, code: 'NO_SERVICE', message: 'plugin runner unavailable' };
      }
      const agent = resolveAgent(sessionId);
      if (!agent) return { ok: false, code: 'NO_AGENT', message: 'session agent unavailable' };
      try {
        const result = await runner.stop(agent, pluginId);
        return { ok: !result || result.ok !== false, ...(result && result.message ? { message: result.message } : {}) };
      } catch (e) {
        herr('[enhance] stop failed', e);
        return { ok: false, code: 'STOP_FAILED', message: String(e && e.message ? e.message : e) };
      }
    });

    harness.handle('plugins/undefine', async (args) => {
      const sessionId = args && typeof args.sessionId === 'string' ? args.sessionId : '';
      const pluginId = args && typeof args.pluginId === 'string' ? args.pluginId : '';
      const runner = ctx.get('dynamicCordisRunner');
      if (!runner || typeof runner.undefine !== 'function') {
        return { ok: false, code: 'NO_SERVICE', message: 'plugin runner unavailable' };
      }
      const agent = resolveAgent(sessionId);
      if (!agent) return { ok: false, code: 'NO_AGENT', message: 'session agent unavailable' };
      try {
        const result = await runner.undefine(agent, pluginId);
        return { ok: !result || result.ok !== false, ...(result && result.message ? { message: result.message } : {}) };
      } catch (e) {
        herr('[enhance] undefine failed', e);
        return { ok: false, code: 'UNDEFINE_FAILED', message: String(e && e.message ? e.message : e) };
      }
    });

    harness.handle('logs/last', async () => ({ ok: true, lines: LOG_RING.slice() }));

    // v2.4.7（每模式独立自定义模板）：返回各模式默认提示词——client 首次切换
    // 「自定义模板」且当前模式无内容时预填用（client 侧无内置模板文本，须 host 提供）。
    // v2.8.0（实测修正）：补 publish 键 = SYSTEM_PUBLISH_PROMPT——此前只返回 4 模式，
    // publish 切自定义模板预填拿到 undefined → 内容为空（用户实测反馈）。
    // 当前 5 模式 base/lite/standard/smart 同值 = SYSTEM_PROMPT；若未来内置按模式拆分
    // （prompts/system-<mode>.md），此 RPC 返回随生成区自动变化，client 无需改动。
    harness.handle('template/default', async () => ({
      ok: true,
      // 兼容保留：defaults[mode] = T1（现有默认模板，行为不变）
      defaults: {
        // v3.1.8（用户指令·每模式默认模板按场景定制）：T1 默认指向模式专属模板
        base: SYSTEM_PROMPT,
        lite: SYSTEM_LITE_PROMPT,
        standard: SYSTEM_STANDARD_PROMPT,
        smart: SYSTEM_SMART_PROMPT,
        publish: SYSTEM_PUBLISH_PROMPT,
      },
      // 模板体系扩展（2026-08-18 修订）：catalog[mode] = [T1, T2]——每模式 2 个内置模板，
      // 供 client 模板选择下拉与新建自定义模板预填（T2=增量模板·保守补充未说的大逻辑/信息，每模式方向不同）
      catalog: {
        base: [SYSTEM_PROMPT, SYSTEM_INCREMENT_PROMPT],
        lite: [SYSTEM_LITE_PROMPT, SYSTEM_INCREMENT_LITE_PROMPT],
        standard: [SYSTEM_STANDARD_PROMPT, SYSTEM_INCREMENT_STANDARD_PROMPT],
        smart: [SYSTEM_SMART_PROMPT, SYSTEM_INCREMENT_SMART_PROMPT],
        publish: [SYSTEM_PUBLISH_PROMPT, SYSTEM_INCREMENT_PUBLISH_PROMPT],
      },
    }));

    // ================= v2.4.0 版本检测与一键更新 RPC =================
    // 方案「插件版本检测与一键更新方案.md」§3：检测（update/check）→ 一键拉取（update/pull）。
    // v2.4.1（实测回填 §9-T5/T6）架构：**host 不再出网**——本部署 web.fetch 无可用 provider
    // （实机抛 WEB_PROVIDER_UNAVAILABLE）；改由 client 浏览器直连 GitHub API（CORS 实测 200），
    // host 只做解析/校验/比较/写入（能力均已实机验证可用）。
    const updateCache = new Map();      // repo → { at, value }（TTL UPDATE_CACHE_TTL_MS）
    const pullInFlight = new Set();     // repo → 拉取中（防重入 PULL_BUSY）

    // v2.4.1（实测回填 §9-T5）：策略必须**带会话解析**——无会话时 workspaceRoot 回退到 DSH
    // 安装目录且写工作区返回 FS_SANDBOX_DENIED（实机验证）；带 session 后 mode=会话预设
    // （如 danger-full-access）、root=会话工作区（实机验证写 ok）。
    function resolveSessionPolicy(sessionId) {
      const sp = ctx.get('sandboxPolicy');
      if (!sp || typeof sp.resolve !== 'function') return null;
      let session;
      try {
        const ss = ctx.get('sessions');
        if (ss && typeof ss.get === 'function' && typeof sessionId === 'string' && sessionId) {
          session = ss.get(sessionId) || undefined;
        }
      } catch (e) { /* 会话解析失败 → 回退无会话策略 */ }
      try {
        return sp.resolve(session ? { session } : undefined);
      } catch (e) {
        return null;
      }
    }

    harness.handle('update/check', async (args) => {
      const repo = normalizeRepo(args && args.repo);
      const sessionId = args && typeof args.sessionId === 'string' ? args.sessionId : '';
      const tagsPayload = args && typeof args.tagsPayload === 'string' ? args.tagsPayload : '';
      const releasePayload = args && typeof args.releasePayload === 'string' ? args.releasePayload : '';
      if (!repo) return { ok: false, code: 'BAD_REPO', message: 'repo must be "owner/name" (letters, digits, . _ -)' };
      if (tagsPayload === '') {
        return { ok: false, code: 'BAD_ARGS', message: 'tagsPayload required (client fetches GitHub tags, host evaluates)' };
      }
      // v3.1.x（用户报告·检测不到新版本）：手动「检测版本」总是取最新——noCache 跳过 5 分钟缓存
      // （刚发布的 tag 曾被缓存旧值覆盖，导致新版本检测不到）
      const hit = updateCache.get(repo);
      if (args && args.noCache === true) {
        updateCache.delete(repo);
      } else if (hit && Date.now() - hit.at < UPDATE_CACHE_TTL_MS) {
        return { ...hit.value, cached: true };
      }
      try {
        // 主路径：tags 载荷（客户端已校验 HTTP 200）取最大可解析版本（§1.2-1）
        const tags = parseTagsPayload(tagsPayload);
        if (!tags) return { ok: false, code: 'BAD_ARGS', message: 'tagsPayload is not a valid JSON array' };
        const best = pickMaxTag(tags);
        if (!best) return { ok: false, code: 'NO_REMOTE_VERSION', message: 'no version-like tags found' };
        // v3.2.1-r（根因修复·版本检测失真）：本地版本运行时读取（lib 注入 readPluginVersion，
// 读运行环境 package.json；动态形态/读取失败回退 PLUGIN_VERSION 常量）
        const localVer = (typeof harness.readPluginVersion === 'function' ? harness.readPluginVersion() : '') || PLUGIN_VERSION;
        const status = versionStatus(localVer, best.version);
        // v2.4.1：默认目录基于**会话策略**的 workspaceRoot（无会话回退配置根）
        const policy = resolveSessionPolicy(sessionId);
        // v3.2.1-r（幽灵目录修正）：一键更新走 tgz 安装，defaultDir 改为真实运行环境目录（实际安装目标）
        const defaultDir = typeof harness.pluginRuntimeDir === 'function'
          ? harness.pluginRuntimeDir()
          : defaultDirFor(policy && policy.workspaceRoot, best.raw);
        // 附加展示元数据：仅当 release 的 tag 与最大 tag 同名（§1.2-2；缺失/失败不阻断）
        let releaseMeta = null;
        if (releasePayload !== '') {
          try {
            const rel = JSON.parse(releasePayload);
            if (rel && typeof rel === 'object' && rel.tag_name === best.raw) {
              releaseMeta = {
                releaseName: typeof rel.name === 'string' ? rel.name : '',
                publishedAt: typeof rel.published_at === 'string' ? rel.published_at : '',
                body: typeof rel.body === 'string' ? rel.body.slice(0, 500) : '',
              };
            }
          } catch (e) { /* release 元数据失败不阻断检测 */ }
        }
        const value = {
          ok: true,
          repo,
          local: localVer,
          remote: best.version,
          remoteTag: best.raw,
          status,
          ahead: status === 'current' && compareVersions(localVer, best.version) > 0,
          defaultDir,
          source: 'tag',
          ...(releaseMeta ? releaseMeta : {}),
          checkedAt: Date.now(),
        };
        updateCache.set(repo, { at: Date.now(), value });
        hlog('[enhance] update/check repo=' + repo + ' local=' + localVer + ' remote=' + best.version + ' tag=' + best.raw + ' status=' + status + ' dir=' + defaultDir);
        return value;
      } catch (e) {
        herr('[enhance] update/check failed', e);
        return { ok: false, code: 'CHECK_FAILED', message: String(e && e.message ? e.message : e) };
      }
    });

    harness.handle('update/pull', async (args) => {
      const repo = normalizeRepo(args && args.repo);
      const tag = args && typeof args.tag === 'string' ? args.tag.trim() : '';
      const sessionId = args && typeof args.sessionId === 'string' ? args.sessionId : '';
      let dir = args && typeof args.dir === 'string' ? args.dir.trim() : '';
      if (!repo) return { ok: false, code: 'BAD_REPO', message: 'repo must be "owner/name"' };
      if (!isValidTag(tag)) return { ok: false, code: 'BAD_TAG', message: 'invalid tag' };
      const manifestCheck = validateManifestFiles(args && args.files);
      if (!manifestCheck.ok) return { ok: false, code: 'BAD_FILES', message: manifestCheck.message };
      const fsSvc = ctx.get('fs');
      if (!fsSvc || typeof fsSvc.resolve !== 'function' || typeof fsSvc.writeText !== 'function') {
        return { ok: false, code: 'FS_UNAVAILABLE', message: 'fs service unavailable' };
      }
      if (pullInFlight.has(repo)) {
        return { ok: false, code: 'PULL_BUSY', message: 'a pull is already in progress' };
      }
      pullInFlight.add(repo);
      try {
        // v2.4.1：会话策略（写入边界 = 会话工作区；无会话回退配置根）
        const policy = resolveSessionPolicy(sessionId);
        // dir 空串 → 默认目录（§3.2-0/4）
        if (dir === '') {
          dir = defaultDirFor(policy && policy.workspaceRoot, tag);
        }
        if (dir === '') return { ok: false, code: 'NO_DIR', message: 'target directory required' };
        // 逐文件写入（文件内容由客户端经浏览器直连 contents API 获取并解码——§9-T6；
        // 本侧仅校验清单完整性 + 会话策略写入；失败返回已写清单）
        const sandboxPolicy = policy;
        const written = [];
        for (const f of manifestCheck.files) {
          try {
            const fileTarget = await fsSvc.resolve(f.name, { cwd: dir });
            await fsSvc.writeText(fileTarget, f.content, undefined, undefined, sandboxPolicy);
            written.push({ name: f.name, bytes: new TextEncoder().encode(f.content).length });
          } catch (e) {
            herr('[enhance] update/pull write failed', f.name, e);
            const code = e && typeof e.code === 'string' && e.code ? e.code : 'PULL_WRITE_FAILED';
            return { ok: false, code, file: f.name, written: written.map((w) => w.name), message: String(e && e.message ? e.message : e) };
          }
        }
        hlog('[enhance] update/pull ok repo=' + repo + ' tag=' + tag + ' dir=' + dir + ' files=' + written.length);
        return { ok: true, repo, tag, dir, files: written };
      } finally {
        pullInFlight.delete(repo);
      }
    });

    // v2.5.0（方案「一键更新并重启方案.md」）：环境检测——只读探测 4 项（批次C 异步化）；
    // 探测执行在 lib/index.cjs（probeEnv，bundle 形态注入）；本侧仅转发 + 合并展示元数据。
    harness.handle('update/envcheck', async (args) => {
      if (!harness.probeEnv) {
        return { ok: false, code: 'UNSUPPORTED', message: '动态安装不支持环境检测，请用 bundle 安装' };
      }
      try {
        const serviceName = args && typeof args.serviceName === 'string' && /^[A-Za-z0-9_-]+$/.test(args.serviceName)
          ? args.serviceName : 'dsh-web';
        // v2.7.0：透传执行器端口（client updater.executorPort，缺省 3081）供 exec-port 检查
        const executorPort = args && Number.isInteger(args.executorPort) ? args.executorPort : undefined;
        const items = await harness.probeEnv(serviceName, executorPort);
        const meta = new Map(ENV_PROBE_KEYS.map((e) => [e.key, e]));
        const out = items.map((it) => ({
          key: it.key,
          ok: it.ok === true,
          warn: it.warn === true,
          detail: typeof it.detail === 'string' ? it.detail : '',
          // v2.7.1：probeEnv 可携带 item 级 level 覆盖（工具不可达降级 warn / svc-bin no-service）
          level: it.level || (meta.has(it.key) ? meta.get(it.key).level : 'warn'),
        }));
        const blockMissing = out.filter((it) => it.level === 'block' && !it.ok).map((it) => it.key);
        hlog('[enhance] update/envcheck ok svc=' + serviceName + ' items=' + out.length + ' blockMissing=' + (blockMissing.join(',') || '-'));
        return { ok: true, items: out, blockMissing, checkedAt: Date.now() };
      } catch (e) {
        herr('[enhance] update/envcheck failed', e);
        return { ok: false, code: 'ENVCHECK_FAILED', message: String(e && e.message ? e.message : e) };
      }
    });

    // v2.6.0：update/apply 与 update/restart 已移除——更新执行迁至独立执行器
    // （lib/updater-host.cjs，127.0.0.1:EXECUTOR_PORT；client 经 update/executorEnsure
    // 获取执行器地址后直连）。原因：重启/重试必须脱离 dsh-web 进程（服务停 = host 死，
    // 依赖 host 的重试必然无法送达——v2.5.5 out.log 无 update/restart 日志为证）。

    // ================= M2 深化：增强管道（bundle 内 Pipeline，契约同已退役的 M2 骨架（P1b 2026-09-19））=================
    // 新增增强模式 / 检索源 = registerEnhanceStage 注册 stage handler，而非修改 enhance 主流程。
    // 2026-08-18（模板体系修订）：每模式 2 个内置模板（default/increment）+ 多自定义模板——
    // system 组装改走 pure.js resolveTemplateSystem（pick/custom 解析 + 旧配置兼容迁移）。
    const enhancePipelineHandlers = new Map();
    function registerEnhanceStage(name, handler, options) {
      const priority = options && Number.isInteger(options.priority) ? options.priority : 0;
      const list = enhancePipelineHandlers.get(name) || [];
      list.push({ handler, priority });
      list.sort((a, b) => a.priority - b.priority);
      enhancePipelineHandlers.set(name, list);
    }
    async function runEnhanceStages(name, value) {
      const list = enhancePipelineHandlers.get(name) || [];
      let v = value;
      for (const { handler } of list) {
        v = await handler(v);
      }
      return v;
    }

    // v2.3（§7.3）：优化进度轮询 RPC——从 pending Map 读 stage（纯展示，失败静默降级）
    harness.handle('enhance/progress', async (args) => {
      const sessionId = args && typeof args.sessionId === 'string' ? args.sessionId : '';
      const seq = args && typeof args.seq === 'number' ? args.seq : -1;
      const rec = pending.get(requestKey(sessionId, seq));
      if (!rec) return { ok: false, code: 'NO_RECORD' };
      // v3.0r（细粒度进度反馈）：返回 stage + 子步骤详情 + 已用时间（纯展示，失败静默降级）
      return {
        ok: true,
        stage: rec.stage || STAGE_PREPARE,
        detailKey: rec.detailKey || '',
        detailArgs: rec.detailArgs || null,
        step: rec.step || 0,
        total: rec.total || 0,
        elapsedMs: rec.startedAt ? Date.now() - rec.startedAt : 0,
      };
    });

    // v3.0r（细粒度进度反馈）：写进度详情（stage/detailKey/detailArgs/step/total）
    function setProgress(rec, stage, key, args, step, total) {
      rec.stage = stage;
      rec.detailKey = key || '';
      rec.detailArgs = args || null;
      rec.step = step || 0;
      rec.total = total || 0;
    }

    // ---- stage: analyze——配置解析 / 模型链 / system 组装 / 记忆与场景准备 ----
    async function enhanceStageAnalyze(state) {
      const args = state.args;
      const sessionId = state.sessionId;
      const text = state.text;
      // 2026-08-18（用户需求·每模式步骤补充）：analyze 阶段开始即上报进度「分析任务」
      if (state.rec) setProgress(state.rec, STAGE_ANALYZE);
      const cfg = validateConfig(args && args.config);
      // v2.1（§2.2）：client 已判定实际模式（显式/auto/seed），请求 mode 覆盖解析值
      if (args && typeof args.mode === 'string' && MODE_KEYS.includes(args.mode)) cfg.mode = args.mode;
      // v2.8.2 曾一键发布记忆强制开启；2026-08-18（用户指令）取消——记忆跟随开关，publish 不例外
      // v23（D6）：模型链 = cfg.fallback 按序（每条独立 reasoningEffort）；
      // 2026-08-18（用户需求·删内置兜底链）：完全按用户配置顺序——链为空 → 空链（不自适应/BUILTIN 补足），llm 阶段报无模型
      const chain = buildTryChain(cfg.fallback);
      // 模板体系扩展（2026-08-18 修订）：每模式 2 个内置模板（default=默认模板·只清晰化重述 /
      // increment=增量模板·保守补充未说的大逻辑/信息，每模式方向不同）+ 每模式多自定义模板
      // （pick=custom:<index>）；解析见 pure.js resolveTemplateSystem——旧配置（无 pick）自动迁移
      // （custom+texts 非空仍生效），行为与旧版等价；旧内置键 supplement/dev 按 increment 处理；非法选中回退 T1。
      // v2.7.0（一键发布）：publish 模式内置专用九章规格 system（custom 模板仍可覆盖）
      // v3.2.23（技能集合化）：模式→模板映射由 SKILL_MANIFEST 驱动（skills/enhance/*/SKILL.md frontmatter 声明）
      const BUILTIN_TEMPLATES = {};
      for (const _m of Object.keys(SKILL_MANIFEST)) {
        BUILTIN_TEMPLATES[_m] = [SKILL_MANIFEST[_m].templates.t1, SKILL_MANIFEST[_m].templates.t2];
      }
      let system = resolveTemplateSystem(cfg, cfg.mode, BUILTIN_TEMPLATES);
      // v3.2.19（用户需求·纪律约束模板）：输出纪律无条件追加——所有模式（含自定义模板）
      // 都必带，从源头约束「只输出优化后的提示词本体」（与 cleanOutput 事后清理互补）
      system = system + '\n\n' + DISCIPLINE_PROMPT;
      // v3.0（模式重构）：lite 规则检查移除——lite 改为「前 1 轮会话关联参考」（见 retrieve stage）
      // v2.6.1（记忆链）：rounds 数组（时间序 [{input,output}]，≤MEMORY_ROUNDS_MAX 轮）→
      // 真多轮消息注入；hasMemory = rounds 非空；开关关 / 预算 0 → 不注入（行为不变）。
      const memRounds = args && args.memory && Array.isArray(args.memory.rounds)
        ? args.memory.rounds.filter((r) => r && (r.input || r.output)).slice(-MEMORY_ROUNDS_MAX)
        : [];
      const hasMemory = memRounds.length > 0;
      const memoryActive = shouldInjectMemory(cfg.memory, hasMemory, cfg.context.budgetChars);
      // v2.7.0（一键发布 · 改动方向代入检索）：delta 提前计算（记忆链轮次时），
      // 供 publish 网络检索词构造（buildWebQuery）使用；无记忆链 → null（检索词仅主题词）
      // v2.8.0（实测修正）：publish 的 removed 侧清零（补充式输入下 removed=上一轮规格，
      // 污染检索词并误导模型——见 filterDeltaForPublish）；hint 与检索词均用过滤后 delta
      let memDelta = null;
      if (memoryActive) {
        const lastOutput = memRounds[memRounds.length - 1].output;
        memDelta = filterDeltaForPublish(computeEditDelta(lastOutput, text), cfg.mode);
      }
      // v2.8.0（一键发布 · 场景路由）：publish 场景判定——会话级缓存固定（首轮判定写入、
      // 后续轮读取不重判）。判定源：缓存 miss 且记忆链非空 → 最早轮输入（进程重启续作兜底，
      // 判定源与注入轮次同源，避免增量文本导致场景翻转）；无记忆链 → 当轮 text。
      // 判定+写入在 system 注入前完成，与 LLM 调用成败无关（scenario 仅依赖判定源）。
      let scenario = 'generic';
      if (cfg.mode === 'publish') {
        const cached = publishScenarioCache.get(sessionId);
        if (cached) {
          scenario = cached;
        } else {
          const src = memRounds.length > 0 ? memRounds[0].input : text;
          scenario = detectScenario(src);
          publishScenarioCache.set(sessionId, scenario);
        }
        if (scenario !== 'generic') {
          system = system + '\n\n【场景判定】本次场景判定：' + scenario + '（依据用户输入自动判定；章节适配要求见「场景适配」段；请勿复述本判定行）';
        }
        hlog('[enhance] scenario=' + scenario + (cached ? ' cached' : ' judged src=' + (memRounds.length > 0 ? 'rounds[0]' : 'text')));
      }
      // v2.7.0（publish 一键发布）：规格长文生成不设限制——maxTokens 省略（provider 默认上限）、
      // outputLimit=0（collectStream 不截断）、超时放宽至 ≥120s（长文生成耗时）
      // v2.8.0（实测修正）：120s → 240s——带记忆链 rounds 的多轮规格生成（重负载路径）
      // 实测 120s 超时（round-2 完整九章 + 融入补充 + 自评），240s 覆盖 4 轮链最坏情况
      const isPublish = cfg.mode === 'publish';
      state.cfg = cfg;
      state.chain = chain;
      state.system = system;
      state.memRounds = memRounds;
      state.hasMemory = hasMemory;
      state.memoryActive = memoryActive;
      state.memDelta = memDelta;
      state.isContinuation = memoryActive && memRounds.length > 0 && memDelta !== null && (memDelta.added.length > 0 || memDelta.removed.length > 0);
      state.scenario = scenario;
      state.isPublish = isPublish;
      // v3.0p 审查（超时/耗时匹配）：链含 effort 时超时自动放宽 ≥120s——实测 effort=max 长草稿
      // （1500 字）完整生成 42s > 默认 30s → 必超时；无 effort 行为不变
      // v2.7.0 曾 publish 强制超时 ≥240s / maxTokens=0 / outputLimit=0（规格长文不截断）；
      // 2026-08-18（用户指令）取消强制——publish 三项参数可调（默认档 60s/4k/16k 按实测覆盖）
      // v3.1.7（用户需求·超时无限制）：cfg.timeoutMs=0（无限制）不被 effort 放宽覆盖——保持 0 不挂超时
      state.timeoutMs = cfg.timeoutMs > 0 ? (chain.some((e) => e && e.reasoningEffort) ? Math.max(cfg.timeoutMs, 120000) : cfg.timeoutMs) : 0;
      state.maxTokens = cfg.maxTokens;
      state.outputLimit = cfg.outputLimit;
      return state;
    }

    // ---- stage: retrieve——V2 上下文构建 / 检索 ----
    // ---- v3.0：会话历史提取（listEvents/filterEvents → extractHistory；失败/不可用 → []）----
    async function fetchSessionHistory(sessionId) {
      const sq = ctx.get('sessionQuery');
      if (!sq || typeof sq.listEvents !== 'function' || typeof sq.filterEvents !== 'function') return [];
      // v3.0p 审查（超时/耗时匹配）：会话历史提取补超时保护（10s，超时/失败 → [] 不阻断）
      let histTimedOut = false;
      const histTimer = ctx.timer.timeout(() => { histTimedOut = true; }, V2_SEARCH_EVENTS_TIMEOUT_MS);
      try {
        // v3.1.3（用户需求·仅结论参考）：优先 readSurface（完整事件，保留 content 块结构）
        // → 结论提取（仅 text 块，丢弃思考/工具调用/系统注入）；旧 listEvents+filterEvents
        // 路径的文档文本为预拼接语义文本（含工具参数），仅作 readSurface 缺失时的兜底。
        if (typeof sq.readSurface === 'function') {
          const surface = await sq.readSurface(sessionId);
          if (histTimedOut) { hlog('[enhance] v3 history timeout'); return []; }
          const events = extractHistoryConclusions(surface && surface.events);
          hlog('[enhance] v3 history events=' + events.length + ' conclusion-only');
          return events;
        }
        const records = await sq.listEvents(sessionId);
        const msgSeqs = [];
        for (let i = records.length - 1; i >= 0 && msgSeqs.length < V2_MSG_SEQ_SCAN; i--) {
          const r = records[i];
          const t = String(r && r.type || '');
          if (t === 'user/message' || t === 'assistant/message') msgSeqs.push(r && r.seq);
        }
        if (histTimedOut) { hlog('[enhance] v3 history timeout'); return []; }
        if (msgSeqs.length === 0) return [];
        const minSeq = msgSeqs[msgSeqs.length - 1];
        const docs = await sq.filterEvents(sessionId, [{ kind: 'seq', from: minSeq }]);
        const events = extractHistory(docs, V2_HISTORY_LIMIT);
        hlog('[enhance] v3 history events=' + events.length);
        return events;
      } catch (e) {
        hlog('[enhance] v3 history failed', e && e.message ? e.message : e);
        return [];
      } finally {
        histTimer();
      }
    }

    // ---- v3.0：LLM 关联性判定（RELEVANCE_PROMPT 占位替换；10s 超时；失败/解析失败 → 不相关）----
    async function judgeRelevance(historyText, currentText, entry) {
      if (llm === undefined || !entry) return { related: false, reason: 'no-llm' };
      const system = RELEVANCE_PROMPT
        .replace('{history}', String(historyText || ''))
        .replace('{current}', String(currentText || ''));
      let timedOut = false;
      const timer = ctx.timer.timeout(() => { timedOut = true; }, RELEVANCE_TIMEOUT_MS);
      try {
        const stream = llm.stream({
          provider: entry.provider,
          model: entry.model,
          // v3.0p 审查（超时/耗时匹配）：judge 类小调用剥离 reasoningEffort——判定/规划任务无需深度思考；effort 思考会消耗小预算输出（空流风险）并放大耗时
          maxTokens: RELEVANCE_MAX_TOKENS,
          system,
          messages: [{
            id: 'enhance-relevance',
            role: 'user',
            content: [{ type: 'text', text: '请依据上述规则判断关联性。' }],
            source: { kind: 'user' },
          }],
        });
        const iterator = stream[Symbol.asyncIterator]();
        const result = await collectStream(iterator, RELEVANCE_OUTPUT_LIMIT);
        if (!timedOut && result.kind === 'ok') {
          const parsed = parseRelevance(result.text);
          if (parsed) return parsed;
        }
        hlog('[enhance] v3 relevance ' + (timedOut ? 'timeout' : 'unparsed'));
      } catch (e) {
        hlog('[enhance] v3 relevance failed', e && e.message ? e.message : e);
      } finally {
        timer();
      }
      return { related: false, reason: 'judge-failed' };
    }


    // ---- v3.0p（publish 多步检索）：LLM 检索主题规划（WEBSEARCH_PLAN_PROMPT 占位替换；
    // 10s 超时；失败/解析失败 → null → retrieve 降级 buildWebQuery 单 query）----
    async function planWebSearch(text, memoText, scenario, entry) {
      if (llm === undefined || !entry) return null;
      const system = WEBSEARCH_PLAN_PROMPT
        .replace('{text}', String(text || ''))
        .replace('{memo}', String(memoText || '（暂无）'))
        .replace('{scenario}', String(scenario || 'generic'));
      let timedOut = false;
      const timer = ctx.timer.timeout(() => { timedOut = true; }, WEB_PLAN_TIMEOUT_MS);
      try {
        const stream = llm.stream({
          provider: entry.provider,
          model: entry.model,
          // v3.0p 审查（超时/耗时匹配）：judge 类小调用剥离 reasoningEffort——判定/规划任务无需深度思考；effort 思考会消耗小预算输出（空流风险）并放大耗时
          maxTokens: WEB_PLAN_MAX_TOKENS,
          system,
          messages: [{
            id: 'enhance-webplan',
            role: 'user',
            content: [{ type: 'text', text: '请输出检索主题规划。' }],
            source: { kind: 'user' },
          }],
        });
        const iterator = stream[Symbol.asyncIterator]();
        const result = await collectStream(iterator, WEB_PLAN_OUTPUT_LIMIT);
        if (!timedOut && result.kind === 'ok') {
          const parsed = parseSearchPlan(result.text);
          if (parsed) return parsed;
        }
        hlog('[enhance] v3p web-plan ' + (timedOut ? 'timeout' : 'unparsed'));
      } catch (e) {
        hlog('[enhance] v3p web-plan failed', e && e.message ? e.message : e);
      } finally {
        timer();
      }
      return null;
    }
    // ---- v3.0（S3）：工作区文件检索（扩展名过滤 + 关键词排序 + 摘要；2s 超时降级）----
    const DOC_FILE_RE = /\.md$/i;
    const CODE_FILE_RE = /\.(?:js|ts|jsx|tsx|py|go|rs|java|cpp|c|h|cs|rb|php|sql|sh|vue|html|css|json|yaml|yml|toml)$/i;
    async function searchWorkspaceFiles(extRe, keywords, maxFiles, maxDepth, paths) {
      const fsSvc = ctx.get('fs');
      const sp = ctx.get('sandboxPolicy');
      const root = sp && sp.workspaceRoot;
      if (!fsSvc || !root || typeof fsSvc.listDir !== 'function' || typeof fsSvc.resolve !== 'function' || typeof fsSvc.readText !== 'function') return [];
      let aborted = false;
      const timer = ctx.timer.timeout(() => { aborted = true; }, V2_WORKSPACE_TIMEOUT_MS);
      try {
        let rootTarget;
        try { rootTarget = await fsSvc.resolve(root); } catch (e) { return []; }
        const files = [];
        const walk = async (target, rel, level) => {
          if (aborted || files.length >= SCAN_FILE_LIST_MAX || level > maxDepth) return;
          let entries;
          try { entries = await fsSvc.listDir(target); } catch (e) { return; }
          for (const en of entries || []) {
            if (aborted) return;
            const name = en && en.name;
            if (!name) continue;
            if (en.type === 'directory') {
              if (V2_WORKSPACE_IGNORE_DIRS.has(name)) continue;
              const sub = en.target;
              if (sub) await walk(sub, rel ? rel + '/' + name : name, level + 1);
              continue;
            }
            const p = rel ? rel + '/' + name : name;
            if (extRe.test(name) && !shouldIgnoreFile(p)) files.push(p);
          }
        };
        await walk(rootTarget, '', 1);
        const kws = (keywords || []).filter((k) => typeof k === 'string' && k.length >= 2);
        // 关键词为空（短文本提不出词）→ 不排序直接取前 maxFiles；
        // 关键词非空但名称 0 命中 → 内容兜底扫描（中文关键词 vs 英文文件名的老问题，
        // 与 v2SearchWorkspace content-fallback 同策略）
        let ranked = kws.length > 0 ? rankFiles(files, kws, 10).map((c) => c.path) : files.slice(0, maxFiles);
        if (ranked.length === 0 && kws.length > 0) {
          const scored = [];
          // 中文组合词分段匹配（「登录功能」→「登录」/「功能」），避免 4 字组合词 vs 内容子串失配
          const kwSegs = [];
          for (const k of kws) {
            const kl = String(k).toLowerCase();
            kwSegs.push(kl);
            for (const sg of splitCnSegments(kl)) {
              if (sg.length >= 2 && !kwSegs.includes(sg)) kwSegs.push(sg);
            }
            // 2 字前缀兜底（组合词切不开时，如「登录功能」→「登录」）
            if (kl.length >= 2 && !kwSegs.includes(kl.slice(0, 2))) kwSegs.push(kl.slice(0, 2));
          }
          for (const rel of files.slice(0, 8)) {
            try {
              const target = await fsSvc.resolve(rel, { cwd: root });
              const text = await fsSvc.readText(target);
              let hits = 0;
              for (const ln of text.split('\n')) {
                if (kwSegs.length > 0 && kwSegs.some((sg) => ln.toLowerCase().includes(sg))) hits++;
                if (hits >= 8) break;
              }
              if (hits > 0) scored.push({ path: rel, hits });
            } catch (e) { /* 跳过 */ }
          }
          scored.sort((a, b) => b.hits - a.hits);
          ranked = scored.slice(0, maxFiles).map((s) => s.path);
        }
        // 最终兜底：内容仍不匹配（中文关键词 vs 英文代码/文件名的语义鸿沟）→ 取前 N 个
        // （开发环境已确认或 md 文档场景下文件有限，全取参考优于空参考）
        if (ranked.length === 0) ranked = files.slice(0, maxFiles);
        // v3.0v2：项目地图指向路径优先（codePaths 前缀命中排前，不足补全量）
        if (paths && paths.length > 0 && files.length > 0) {
          const prefixed = files.filter((f) => paths.some((p) => f === p || f.startsWith(p + '/')));
          const rest = files.filter((f) => !prefixed.includes(f));
          ranked = prefixed.concat(rest).slice(0, maxFiles);
        }
        const out = [];
        for (const rel of ranked.slice(0, maxFiles)) {
          try {
            const target = await fsSvc.resolve(rel, { cwd: root });
            const text = await fsSvc.readText(target);
            out.push({ path: rel, snippet: snippetFromLines(text.split('\n'), kws, SNIPPET_BUDGET) });
          } catch (e) { /* 跳过 */ }
        }
        return out;
      } finally {
        timer();
      }
    }

    // ---- v3.0v2：smart 工作区阶段（门槛：有 .md + 相关文档命中 + 项目地图结构）----
    // 返回 { block, enteredThird }——enteredThird 决定 SMART_TAIL（调整方案）是否追加。
    async function enhanceSmartWorkspace(state, text, chain0, rbudget) {
      // v3.1.8（用户需求·预算真正生效）：smart 工作区规模随预算伸缩（BUDGET_RETRIEVE_TABLE）——
      // 文档扫描数/深度、文档与代码内容上限随档位递增；预算 0 由调用方跳过（不进入本函数）。
      const kws = extractKeywords(text, []);
      const docs = await searchWorkspaceFiles(DOC_FILE_RE, kws, rbudget.smartDocs, rbudget.smartDepth);
      if (docs.length === 0) {
        hlog('[enhance] v3 smart no-md');
        return { block: state.v2Block, enteredThird: false };
      }
      const docsText = docs.map((d) => '📄 ' + d.path + '\n' + d.snippet).join('\n\n').slice(0, rbudget.smartChars);
      // B+C 合并：一次 LLM 调用——检索相关文档 + 分析项目地图结构
      if (state.rec) setProgress(state.rec, STAGE_FILES, 'docs');
      const analysis = await analyzeDocsRelevance(docsText, text, state.v2Block || '', chain0);
      if (!analysis || analysis.relatedDocs.length === 0) {
        hlog('[enhance] v3 smart no-related-doc');
        return { block: state.v2Block, enteredThird: false };
      }
      const docPart = (state.v2Block ? state.v2Block + '\n\n' : '') + '【项目文档参考】\n' +
        analysis.relatedDocs.map((d) => '📄 ' + d.path + '\n' + d.excerpt).join('\n\n');
      if (!analysis.hasProjectMap) {
        hlog('[enhance] v3 smart no-project-map reason=' + analysis.reason);
        return { block: docPart, enteredThird: false };
      }
      // 第三步：代码文档检索（项目地图指向路径优先）
      hlog('[enhance] v3 smart project-map paths=' + JSON.stringify(analysis.codePaths));
      if (state.rec) setProgress(state.rec, STAGE_FILES, 'code');
      const codes = await searchWorkspaceFiles(CODE_FILE_RE, kws, 3, 3, analysis.codePaths);
      const codeText = codes.map((c) => '📄 ' + c.path + '\n' + c.snippet).join('\n\n').slice(0, rbudget.smartCodeChars);
      return { block: codeText ? docPart + '\n\n【相关代码参考】\n' + codeText : docPart, enteredThird: true };
    }

    // ---- v3.0v2：提示词开发意向判定（DEV_INTENT_PROMPT；10s 超时降级非开发意向）----
    async function judgeDevIntent(text, sessionRef, entry) {
      if (llm === undefined || !entry) return { isDevIntent: false, reason: 'no-llm' };
      const system = DEV_INTENT_PROMPT
        .replace('{prompt}', String(text || ''))
        .replace('{session}', String(sessionRef || ''));
      let timedOut = false;
      const timer = ctx.timer.timeout(() => { timedOut = true; }, RELEVANCE_TIMEOUT_MS);
      try {
        const stream = llm.stream({
          provider: entry.provider,
          model: entry.model,
          // v3.0p 审查（超时/耗时匹配）：judge 类小调用剥离 reasoningEffort——判定/规划任务无需深度思考；effort 思考会消耗小预算输出（空流风险）并放大耗时
          maxTokens: RELEVANCE_MAX_TOKENS,
          system,
          messages: [{
            id: 'enhance-intent',
            role: 'user',
            content: [{ type: 'text', text: '请依据上述规则判断。' }],
            source: { kind: 'user' },
          }],
        });
        const iterator = stream[Symbol.asyncIterator]();
        const result = await collectStream(iterator, RELEVANCE_OUTPUT_LIMIT);
        if (!timedOut && result.kind === 'ok') {
          const parsed = parseIntent(result.text);
          if (parsed) return parsed;
        }
        hlog('[enhance] v3 intent ' + (timedOut ? 'timeout' : 'unparsed'));
      } catch (e) {
        hlog('[enhance] v3 intent failed', e && e.message ? e.message : e);
      } finally {
        timer();
      }
      return { isDevIntent: false, reason: 'judge-failed' };
    }

    // ---- v3.0v2：文档检索 + 项目地图合并分析（DOC_ANALYSIS_PROMPT 一次调用）----
    async function analyzeDocsRelevance(docsText, text, sessionRef, entry) {
      if (llm === undefined || !entry) return null;
      const system = DOC_ANALYSIS_PROMPT
        .replace('{docs}', String(docsText || ''))
        .replace('{prompt}', String(text || ''))
        .replace('{session}', String(sessionRef || ''));
      let timedOut = false;
      const timer = ctx.timer.timeout(() => { timedOut = true; }, RELEVANCE_TIMEOUT_MS);
      try {
        const stream = llm.stream({
          provider: entry.provider,
          model: entry.model,
          // v3.0p 审查（超时/耗时匹配）：judge 类小调用剥离 reasoningEffort——判定/规划任务无需深度思考；effort 思考会消耗小预算输出（空流风险）并放大耗时
          maxTokens: RELEVANCE_MAX_TOKENS,
          system,
          messages: [{
            id: 'enhance-docanalysis',
            role: 'user',
            content: [{ type: 'text', text: '请依据上述规则分析。' }],
            source: { kind: 'user' },
          }],
        });
        const iterator = stream[Symbol.asyncIterator]();
        const result = await collectStream(iterator, RELEVANCE_OUTPUT_LIMIT);
        if (!timedOut && result.kind === 'ok') {
          const parsed = parseDocsAnalysis(result.text);
          if (parsed) return parsed;
        }
        hlog('[enhance] v3 doc-analysis ' + (timedOut ? 'timeout' : 'unparsed'));
      } catch (e) {
        hlog('[enhance] v3 doc-analysis failed', e && e.message ? e.message : e);
      } finally {
        timer();
      }
      return null;
    }
    // ---- stage: retrieve——v3.0 检索策略分发（rounds 窗口判定 / v2 旧管道 / none）----
    async function enhanceStageRetrieve(state) {
      const cfg = state.cfg;
      const text = state.text;
      const sessionId = state.sessionId;
      const rec = state.rec;
      const row = SKILL_MANIFEST[cfg.mode].retrieve || SKILL_MANIFEST[DEFAULT_MODE].retrieve; // v3.2.23 retrieve 声明化（skills/*/SKILL.md）
      state.v2Block = '';
      state.v2Log = 'none';
      if (state.isContinuation) {
        state.v2Log = 'continuation-skip';
        return state;
      }
      if (row.kind === 'rounds' && row.windows.length > 0) {
        // v3.1.8（用户需求·预算真正生效）：rounds 模式消费上下文预算——budget 0 = 跳过检索
        // （不注入参考，等价 base 直发）；budget > 0 时注入上限随档位伸缩（BUDGET_RETRIEVE_TABLE）。
        const rbudget = resolveRetrieveBudget(cfg.mode, cfg.context && cfg.context.budgetChars ? cfg.context.budgetChars : 0);
        if (rbudget.roundsChars > 0) {
          // v3.0：会话轮次窗口关联检索——逐窗口 LLM 判定，命中即停并注入参考；
          // 窗口越界（历史不足）跳过；全不中 → 无参考（不阻断主流程）。
          // 2026-08-18（用户需求·步骤补充）：rounds 模式补「读取会话」进度（与 publish 一致）
          if (state.rec) setProgress(state.rec, STAGE_HISTORY);
          const events = await fetchSessionHistory(sessionId);
          const chain0 = state.chain && state.chain.length > 0 ? state.chain[0] : null;
          for (let wi = 0; wi < row.windows.length; wi++) {
            const win = row.windows[wi];
            if (rec.cancelled || rec.timedOut) break;
            const winText = splitHistoryRounds(events, win[0], win[1]).join('\n');
            if (winText === '') {
              hlog('[enhance] v3 window empty rounds=' + win[0] + '-' + win[1]);
              continue;
            }
            setProgress(rec, STAGE_ANALYZE, 'judge', null, wi + 1, row.windows.length);
            const judge = await judgeRelevance(winText, text, chain0);
            if (judge.related) {
              state.v2Block = '【相关会话参考（仅吸收明确需求，禁止复述）】\n' + winText.slice(0, rbudget.roundsChars);
              state.v2Log = 'rounds=' + win[0] + '-' + win[1] + ' chars=' + winText.length + ' inject=' + Math.min(winText.length, rbudget.roundsChars) + ' reason=' + judge.reason;
              hlog('[enhance] v3 window hit rounds=' + win[0] + '-' + win[1] + ' reason=' + judge.reason);
              break;
            }
            hlog('[enhance] v3 window miss rounds=' + win[0] + '-' + win[1] + ' reason=' + judge.reason);
          }
          // v3.0v2：smart——窗口判定后先判开发意向（非开发意向直接停，不进工作区）；
          // 开发意向 → 工作区阶段（.md + 相关文档命中 + 项目地图三门槛），
          // 仅进入第三步（代码阶段）才追加 SMART_TAIL（调整方案输出）
          if (SKILL_MANIFEST[cfg.mode].sources.indexOf('workspace') !== -1) {
            setProgress(rec, STAGE_ANALYZE, 'intent');
            const intent = await judgeDevIntent(text, state.v2Block || '', chain0);
            if (!intent.isDevIntent) {
              hlog('[enhance] v3 smart not-dev-intent reason=' + intent.reason);
            } else {
              hlog('[enhance] v3 smart dev-intent reason=' + intent.reason);
              const ws = await enhanceSmartWorkspace(state, text, chain0, rbudget);
              state.v2Block = ws.block;
              if (ws.enteredThird) {
                state.system = state.system + '\n\n' + SMART_TAIL_PROMPT;
                hlog('[enhance] v3 smart tail injected');
              }
            }
          }
        } else {
          // 预算 0：rounds 检索跳过（不注入参考；smart 工作区同样不进入）
          state.v2Log = 'rounds-skip budget=0';
        }
      } else if (row.kind === 'v2') {
        // publish：v2 旧管道 + v3.0p 多步网络检索（LLM 规划主题 → 逐主题搜索 → 跨轮记忆回注）
        const memoRec = publishWebMemo.get(sessionId);
        const memoText = memoRec && memoRec.memo ? memoRec.memo : '';
        const memoTopics = memoRec && Array.isArray(memoRec.topics) ? memoRec.topics : [];
        const chain0 = state.chain && state.chain.length > 0 ? state.chain[0] : null;
        const budget = cfg.context && cfg.context.budgetChars ? cfg.context.budgetChars : 0;
        // v3.0p：预算 > 0 才启用检索（与「上下文预算需 > 0 才启用检索」联动语义一致）
        setProgress(rec, STAGE_FILES, 'plan');
        const plan = budget > 0 ? await planWebSearch(text, memoText, state.scenario, chain0) : null;
        hlog('[enhance] v3p web-plan ' + (budget <= 0 ? 'skipped budget=0' : (plan ? 'topics=' + plan.topics.map((t) => t.query).join('|') : 'none→legacy')));
        // v3.0p：规划主题与已检索主题去重（LLM 未遵从提示词时的硬过滤；过滤后为空 → 降级单 query）
        const freshPlan = plan && budget > 0 ? plan.topics.filter((t) => memoTopics.indexOf(String(t && t.query || '').trim()) === -1) : null;
        const v2 = await buildV2ContextBlock({
          llm: ctx.get('llm'),
          sessionQuery: ctx.get('sessionQuery'),
          sandboxPolicy: ctx.get('sandboxPolicy'),
          fs: ctx.get('fs'),
          timer: ctx.timer,
          chain: state.chain,
          web: ctx.get('web'),
          delta: state.memDelta,
          scenario: state.scenario,
          webPlan: freshPlan && freshPlan.length > 0 ? freshPlan : null,
          webMemo: memoText,
        }, sessionId, text, cfg, (st, detail) => {
          rec.stage = st;
          if (detail && typeof detail === 'object') {
            rec.detailKey = detail.key || '';
            rec.detailArgs = detail.args || null;
            rec.step = detail.step || 0;
            rec.total = detail.total || 0;
          } else {
            rec.detailKey = '';
            rec.detailArgs = null;
            rec.step = 0;
            rec.total = 0;
          }
        });
        state.v2Block = v2.block;
        state.v2Log = v2.log;
        // v3.0p：跨轮检索记忆更新——仅成功命中的主题入已检索列表（失败/超时不占位），
        // 主题去重（≤WEB_MEMO_TOPICS_MAX）+ 要点摘要累积（≤WEB_MEMO_MAX，保留最新）
        const hitTopics = Array.isArray(v2.webTopics) ? v2.webTopics.filter((t) => t && t.hits > 0) : [];
        if (hitTopics.length > 0) {
          const merged = memoTopics.concat(hitTopics.map((t) => t.query)).filter((q, i, a) => q && a.indexOf(q) === i).slice(-WEB_MEMO_TOPICS_MAX);
          const memo = (memoText ? memoText + '\n' : '') + (v2.webMemoText || '');
          publishWebMemo.set(sessionId, { topics: merged, memo: memo.slice(-WEB_MEMO_MAX) });
          hlog('[enhance] v3p memo topics=' + merged.length + ' chars=' + memo.slice(-WEB_MEMO_MAX).length);
        }
      }
      // v3.2.24（规则落点 L2）：参考使用规则改为条件注入——参考块/记忆命中才追加（v3.2.20 并入 discipline 无条件，
      // 现拆为独立 REFERENCE_GUIDE 技能文件按需加载，无参考场景省 tokens）；L0 纪律保持精简。
      if (state.v2Block !== '' || state.memoryActive) state.system = state.system + '\n\n' + REFERENCE_GUIDE;
      return state;
    }    // ---- stage: assemble——消息组装 / 日志 ----
    async function enhanceStageAssemble(state) {
      const args = state.args;
      const sessionId = state.sessionId;
      const seq = state.seq;
      const text = state.text;
      // 2026-08-18（用户需求·每模式步骤补充）：assemble 阶段开始即上报进度「组装上下文」
      if (state.rec) setProgress(state.rec, STAGE_CONTEXT);
      const cfg = state.cfg;
      const chain = state.chain;
      const system = state.system;
      const memRounds = state.memRounds;
      const memoryActive = state.memoryActive;
      const memDelta = state.memDelta;
      const v2Block = state.v2Block;
      const v2Log = state.v2Log;
      const isContinuation = state.isContinuation === true;
      const timeoutMs = state.timeoutMs;
      const maxTokens = state.maxTokens;
      const outputLimit = state.outputLimit;
      // v2.6.1：消息组装——记忆链经 buildChatMessages 成为真多轮 user/assistant 消息，
      // 最终 user 消息 = 本轮修改摘要 + 模式块 + 原文包裹；无记忆 → 单 user 消息（旧行为）。
      let memoryLog = '';
      // v2.8.0（一键发布 · 实测修正）：publish 用中性用户包装（wrapPublishText），
      // 其余模式沿用「请优化以下提示词」包装（行为不变）
      const wrappedText = state.isPublish ? wrapPublishText(text) : wrapUserText(text);
      let finalText = v2Block !== '' ? v2Block + '\n\n' + wrappedText : wrappedText;
      if (isContinuation) {
        state.system = system + '\n\n' + CONTINUE_PROMPT;
        const hint = buildMemoryDeltaHint(memDelta);
        finalText = '【本轮修改】（主要优化方向）\n' + (hint !== '' ? hint : '请基于当前草稿继续完善。') + '\n\n【当前提示词】\n"""\n' + text + '\n"""\n\n请基于以上修改继续完善，输出完整更新后的提示词。';
      }
      let messages;
      if (memoryActive) {
        const hint = buildMemoryDeltaHint(memDelta);
        if (hint !== '' && !isContinuation) finalText = hint + '\n\n' + finalText;
        const built = buildChatMessages(memRounds, finalText, 'enhance-' + sessionId + '-' + seq, cfg.context.budgetChars);
        messages = built.messages;
        memoryLog = 'memory rounds=' + memRounds.length + ' chars=' + built.memChars + ' delta=' + (hint !== '' ? 'yes' : 'none');
      } else {
        messages = [{ id: 'enhance-' + sessionId + '-' + seq, role: 'user', content: [{ type: 'text', text: finalText }], source: { kind: 'user' } }];
      }
      const modeTag = args && args.seed === true ? cfg.mode + '(seed)' : cfg.mode;
      const ctxLog = [v2Log === 'none' ? '' : v2Log, memoryLog].filter((s) => s !== '').join('+') || 'none';
      hlog('[enhance] cfg session=' + sessionId + ' mode=' + modeTag + ' ctx=' + ctxLog + ' chain=' + (chain.length > 0 ? chain.map((f) => f.provider + '/' + f.model).join(',') : '-') + ' timeout=' + timeoutMs + ' maxTokens=' + maxTokens + ' outputLimit=' + outputLimit + ' template=' + (cfg.templatePick && typeof cfg.templatePick === 'object' && cfg.templatePick[cfg.mode] ? cfg.templatePick[cfg.mode] : (cfg.templateMode === 'custom' ? 'custom' : 'builtin')));
      state.messages = messages;
      return state;
    }

    // ---- stage: llm——模型链调用 / 流式收集 / 结果清理 ----
    async function enhanceStageLlm(state) {
      const sessionId = state.sessionId;
      const seq = state.seq;
      const rec = state.rec;
      let chain = state.chain.slice();
      const system = state.system;
      const messages = state.messages;
      const isPublish = state.isPublish;
      const maxTokens = state.maxTokens;
      const outputLimit = state.outputLimit;
      let lastFailure = null;
      // 2026-08-18（用户需求·删内置兜底链）：模型链为空 → 明确报「未配置模型」（不自适应补足）
      if (chain.length === 0) {
        state.result = { ok: false, code: 'NO_MODEL', message: friendlyMessage({ code: 'NO_MODEL' }) };
        return state;
      }
      // v3.3.3（2026-08-27 用户实测·增强易断链修复）：外层 pass 循环——首轮整链耗尽后
      // 整体重试一次（最多 2 pass），瞬时限流/慢首字/网络抖动自愈；重试轮不再挂看门狗、
      // 不走探测缓存（直接真实生成），杜绝「探测判死 → 冻结整链 → 连续失败」；
      // 总预算仍由外层 timeoutMs 计时器兜底（rec.timedOut 立即中断，不无限重试）。
      for (let pass = 0; pass < 2; pass++) {
      for (let i = 0; i < chain.length; i++) {
        const entry = chain[i];
        if (rec.cancelled || rec.timedOut) {
          state.result = { ok: false, code: rec.timedOut ? 'TIMEOUT' : 'ABORTED', message: friendlyMessage({ code: rec.timedOut ? 'TIMEOUT' : 'ABORTED' }) };
          return state;
        }
        hlog('[enhance] try session=' + sessionId + ' provider=' + entry.provider + ' model=' + entry.model + (entry.reasoningEffort ? ' effort=' + entry.reasoningEffort : '') + ' seq=' + seq);
        // v3.2.4（用户需求）：模型序号 = 用户设置（fallback 链）中的位置——buildTryChain
        // 会去重/过滤导致 chain 索引偏移；用 fallback 原始序号，设置里第 N 条就显示 N（自动随数量）
        const fb = state.cfg && Array.isArray(state.cfg.fallback) ? state.cfg.fallback : [];
        let stepNum = i + 1;
        for (let fi = 0; fi < fb.length; fi++) {
          const it = fb[fi];
          if (it && typeof it === 'object' && String(it.provider).trim() === entry.provider && String(it.model).trim() === entry.model) {
            stepNum = fi + 1;
            break;
          }
        }
        setProgress(rec, STAGE_LLM, i > 0 ? 'retry' : '', null, stepNum, fb.length > 0 ? fb.length : chain.length);
        let stream;
        try {
          stream = llm.stream({
            provider: entry.provider,
            model: entry.model,
            ...(entry.reasoningEffort ? { reasoningEffort: entry.reasoningEffort } : {}),
            system,
            // v2.7.0（publish）：maxTokens<=0 省略字段 → provider 默认上限（不设限制）
            // v2.9.0-fix（实测确证）：reasoning 模式（带 effort）下思考过程消耗 maxTokens
            // 预算——配置的 2000 在长输入 + effort=max 时耗尽 → 空流（EMPTY_RESPONSE）；
            // 自动放宽到 >=8000（保守版；publish 九章规格长文同策略放宽——实测 maxTokens=2000 时空流，v3.2.27 修复）
            ...(entry.reasoningEffort ? { maxTokens: Math.max(maxTokens, 8000) } : (isPublish ? { maxTokens: Math.max(maxTokens, 8000) } : (maxTokens > 0 ? { maxTokens } : {}))),
            // v3.2.21（用户需求·优化结果稳定）：确定性重述任务压低 temperature——不传时用平台默认
            // （随机性高，同一输入每次结果不同）；reasoning 链节由 effort 控制，不传 temperature
            ...(entry.reasoningEffort ? {} : { temperature: 0.3 }),
            messages,
          });
        } catch (e) {
          // v3.1.3（看门狗 + 延迟连通性预检）：llm.stream 同步抛错 = 模型不可达 → 记失败走下一条
          hlog('[enhance] fail session=' + sessionId + ' model=' + entry.model + ' code=STREAM_THROW');
          lastFailure = { code: e && e.code ? String(e.code) : 'STREAM_THROW', message: String(e && e.message ? e.message : e) };
          continue;
        }
        const iterator = stream[Symbol.asyncIterator]();
        rec.iterator = iterator;
        // v3.1.3（看门狗 + 延迟连通性预检）：仅首个尝试挂响应看门狗——WATCHDOG_TIMEOUT_MS 内
        // 无任何 chunk → 判首模型不通，中断当前流并进入连通性探测（探测选模型重启生成，
        // 重启不再挂看门狗，防「慢但健康」模型被反复打断成死循环）
        let watchTimer = null;
        let watchFired = false;
        if (i === 0 && !state.watchdogDone) {
          watchTimer = ctx.timer.timeout(() => {
            watchFired = true;
            if (rec.iterator && typeof rec.iterator.return === 'function') {
              try { rec.iterator.return(); } catch (e) { /* 忽略 */ }
            }
          }, WATCHDOG_TIMEOUT_MS);
        }
        let result;
        try {
          result = await collectStream(iterator, outputLimit, () => {
            if (watchTimer) { watchTimer(); watchTimer = null; }
          });
        } finally {
          if (watchTimer) watchTimer();
          rec.iterator = null;
        }
        if (watchFired) {
          state.watchdogDone = true;
          hlog('[enhance] watchdog no-response in ' + WATCHDOG_TIMEOUT_MS + 'ms on ' + entry.provider + '/' + entry.model + ' seq=' + seq);
          setProgress(rec, STAGE_LLM, 'probe', null, 1, chain.length);
          // 连通性探测：跳过刚挂起的首条，探测剩余链（缓存优先，每条 PROBE_TIMEOUT_MS 超时）
          const rest = chain.slice(i + 1);
          const pick = await probeFirstReachable(rest);
          if (!pick) {
            // 剩余全不通 → 回头探测挂起首条：通畅（只是慢）→ 重试一次；不通 → 全不可用
            const headOk = await probeEntry(entry);
            if (!headOk) {
              hlog('[enhance] chain all unavailable session=' + sessionId + ' last code=' + (lastFailure ? lastFailure.code : '?'));
              // v3.3.3（设计决策）：探测窗口已按实测放大（PROBE_TIMEOUT_MS=12s）——探测全败
              // = 实测全不可达（慢模型不再被误杀），立即报错不进入重试轮（避免无望的二次等待）；
              // 重试轮只覆盖生成级失败耗尽（EMPTY/STREAM_THROW/QUOTA/瞬时错误等自愈场景）。
              state.result = { ok: false, code: 'ALL_MODELS_UNAVAILABLE', message: friendlyMessage({ code: 'ALL_MODELS_UNAVAILABLE' }) };
              return state;
            }
            hlog('[enhance] watchdog retry head via ' + entry.provider + '/' + entry.model + ' seq=' + seq);
            chain = [entry];
            i = -1;
            continue;
          }
          hlog('[enhance] watchdog restart via ' + pick.provider + '/' + pick.model + ' seq=' + seq);
          chain = [pick].concat(chain.filter((e) => e !== pick && e !== entry));
          i = -1;
          continue;
        }
        if (result.kind === 'ok') {
          // v2.8.0（实测修正）：publish 剥离输出首部【场景判定】回显行（确定性，不依赖模型遵从）
          const cleaned = isPublish ? stripScenarioEcho(cleanOutput(result.text, state.text)) : cleanOutput(result.text, state.text);
          if (cleaned === '') {
            lastFailure = { code: 'EMPTY_RESPONSE', message: 'model returned empty text' };
            continue;
          }
          hlog('[enhance] ok session=' + sessionId + ' via ' + entry.model);
          state.result = { ok: true, text: cleaned, model: entry.model, ...(state.watchdogDone === true || i > 0 ? { fallbackUsed: true } : {}) };
          return state;
        }
        if (result.kind === 'toolong') {
          lastFailure = { code: 'OUTPUT_TOO_LONG', message: 'output exceeded limit' };
          continue;
        }
        if (result.kind === 'cancelled' || result.kind === 'aborted') {
          state.result = { ok: false, code: rec.timedOut ? 'TIMEOUT' : 'ABORTED', message: friendlyMessage({ code: rec.timedOut ? 'TIMEOUT' : 'ABORTED' }) };
          return state;
        }
        hlog('[enhance] fail session=' + sessionId + ' model=' + entry.model + ' code=' + (result.failure ? result.failure.code : '?'));
        lastFailure = result.failure || { code: 'LLM_FAILED', message: 'unknown failure' };
      }
      // v3.3.3（链耗尽）：pass 0 且未被取消/超时 → 整链重试一次（watchdogDone=true 免看门狗）
      if (pass === 0 && !rec.cancelled && !rec.timedOut) {
        hlog('[enhance] chain exhausted retry pass=2 session=' + sessionId + ' last code=' + (lastFailure ? lastFailure.code : '?'));
        state.watchdogDone = true;
        continue;
      }
      hlog('[enhance] chain exhausted session=' + sessionId + ' last code=' + (lastFailure ? lastFailure.code : '?'));
      state.result = { ok: false, code: lastFailure.code || 'LLM_FAILED', message: friendlyMessage(lastFailure) };
      return state;
      }
    }

    // v3.1.3（看门狗 + 延迟连通性预检）：连通性探测——缓存优先，pingStream 短输入探测
    // （可达性：QUOTA/网络/auth/超时；探测 ≠ 生成成功，生成级失败仍由原回退链兜底）
    const probeCache = new Map();
    async function probeEntry(entry) {
      if (!entry) return false;
      const key = entry.provider + '/' + entry.model;
      const now = Date.now();
      const hit = probeCacheGet(probeCache, key, now);
      if (hit) return hit.ok === true;
      if (llm === undefined) return false;
      const ref = { current: null };
      let timedOut = false;
      const timer = ctx.timer.timeout(() => {
        timedOut = true;
        if (ref.current && typeof ref.current.return === 'function') {
          try { ref.current.return(); } catch (e) { /* 忽略 */ }
        }
      }, PROBE_TIMEOUT_MS);
      let r;
      try {
        r = await pingStream(llm, entry, ref);
      } finally {
        timer();
      }
      const ok = !timedOut && r && r.ok === true;
      probeCacheSet(probeCache, key, { ok, code: timedOut ? 'TIMEOUT' : (r ? r.code : 'LLM_FAILED') }, now);
      hlog('[enhance] probe ' + entry.provider + '/' + entry.model + ' → ' + (ok ? 'ok ' + (r ? r.latencyMs : '?') + 'ms' : (timedOut ? 'timeout' : (r ? r.code : 'LLM_FAILED'))));
      return ok;
    }
    async function probeFirstReachable(entries) {
      for (const entry of entries) {
        if (await probeEntry(entry)) return entry;
      }
      return null;
    }

    registerEnhanceStage('analyze', enhanceStageAnalyze, { priority: 100 });
    registerEnhanceStage('retrieve', enhanceStageRetrieve, { priority: 100 });
    registerEnhanceStage('assemble', enhanceStageAssemble, { priority: 100 });
    registerEnhanceStage('llm', enhanceStageLlm, { priority: 100 });

    harness.handle('enhance', async (args) => {
      const sessionId = args && typeof args.sessionId === 'string' ? args.sessionId : 'unknown';
      const seq = args && typeof args.seq === 'number' ? args.seq : -1;
      const text = args && typeof args.text === 'string' ? args.text : '';
      const key = requestKey(sessionId, seq);
      if (text.trim() === '' || text.startsWith('/')) {
        return { ok: false, code: 'GUARD', message: friendlyMessage({ code: 'GUARD' }) };
      }
      if (llm === undefined) {
        return { ok: false, code: 'NO_LLM', message: friendlyMessage({ code: 'NO_LLM' }) };
      }

      // v2.3（§7.3）：记录提前创建（入参校验后）——stage 从 prepare 起可被 progress RPC 轮询
      // v3.0r（细粒度进度反馈）：rec 扩展——startedAt/detailKey/detailArgs/step/total
      const rec = { cancelled: false, timedOut: false, iterator: null, stage: STAGE_PREPARE, startedAt: Date.now(), detailKey: '', detailArgs: null, step: 0, total: 0 };
      pending.set(key, rec);

      // M2 深化：请求状态经 Pipeline 四阶段链式传递（analyze→retrieve→assemble→llm）。
      // 准备阶段在 try 外（与原逻辑一致：准备期异常冒泡为 RPC 错误而非 LLM_FAILED）；
      // 超时计时自 llm 阶段起（与原逻辑一致：准备阶段不设防）。
      const state = { args, sessionId, seq, text, key, rec };
      await runEnhanceStages('analyze', state);
      await runEnhanceStages('retrieve', state);
      await runEnhanceStages('assemble', state);
      // v3.1.7（用户需求·超时无限制）：timeoutMs<=0 不挂超时计时器（0 = 无限制，等待模型自然完成）
      const timeoutDisposer = state.timeoutMs > 0 ? ctx.timer.timeout(() => {
        markAndAbort(key, 'timedOut');
      }, state.timeoutMs) : null;

      try {
        await runEnhanceStages('llm', state);
        return state.result;
      } catch (e) {
        herr('[enhance] unexpected error session=' + sessionId + ' seq=' + seq, e);
        return { ok: false, code: 'LLM_FAILED', message: friendlyMessage({ code: 'LLM_FAILED' }) };
      } finally {
        if (timeoutDisposer) timeoutDisposer();
        pending.delete(key);
      }
    });

    harness.handle('cancel', async (args) => {
      const sessionId = args && typeof args.sessionId === 'string' ? args.sessionId : 'unknown';
      const seq = args && typeof args.seq === 'number' ? args.seq : -1;
      markAndAbort(requestKey(sessionId, seq), 'cancelled');
      return { ok: true };
    });


    ctx.effect(() => () => {
      for (const key of [...pending.keys()]) {
        const rec = pending.get(key);
        if (rec && rec.iterator && typeof rec.iterator.return === 'function') {
          try { rec.iterator.return(); } catch (e) { /* 忽略 */ }
        }
      }
    });
  },
};